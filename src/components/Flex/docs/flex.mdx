---
name: Flex
menu: Utilities
route: /components/Flex
---

import { Playground, Props } from 'docz';
import { Flex, StyledFlex } from 'components/Flex';
import { Looper } from 'components/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { FlexBox } from './helpers/FlexBox';

<PageTitle src="components/Flex" title="Flex" />

## Container row

### Justify content space between, reverse

<Playground>
  <Flex reverse direction="row" justifyContent="space-between">
    <Looper
      end={4}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40 + i * 10} width={40}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Justify content space around

<Playground>
  <Flex direction="row" justifyContent="space-around">
    <Looper
      end={4}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40 + i * 10} width={40}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Justify content space evenly

<Playground>
  <Flex direction="row" justifyContent="space-evenly">
    <Looper
      end={4}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40 + i * 10} width={40}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Justify content center, align items center

<Playground>
  <Flex alignItems="center" direction="row" justifyContent="center">
    <Looper
      end={4}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40 + i * 10} width={40}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Justify content start, align items start

<Playground>
  <Flex alignItems="flex-start" direction="row" justifyContent="flex-start">
    <Looper
      end={4}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40 + i * 10} width={40}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Justify content end, align items end

<Playground>
  <Flex alignItems="flex-end" direction="row" justifyContent="flex-end">
    <Looper
      end={4}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40 + i * 10} width={40}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Align content center, wrap

<Playground>
  <Flex wrap alignContent="center" direction="row" style={{ height: '320px' }}>
    <Looper
      end={30}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40} width={40}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Align content space between, wrap

<Playground>
  <Flex wrap alignContent="space-between" direction="row" style={{ height: '320px' }}>
    <Looper
      end={30}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40} width={40}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

## Container column

### Justify content space between, reverse

<Playground>
  <Flex reverse direction="column" justifyContent="space-between" style={{ height: '320px' }}>
    <Looper
      end={4}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40} width={40 + i * 10}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Justify content space around

<Playground>
  <Flex direction="column" justifyContent="space-around" style={{ height: '320px' }}>
    <Looper
      end={4}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40} width={40 + i * 10}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Justify content space evenly

<Playground>
  <Flex direction="column" justifyContent="space-evenly" style={{ height: '320px' }}>
    <Looper
      end={4}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40} width={40 + i * 10}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Justify content center, align items center

<Playground>
  <Flex alignItems="center" direction="column" justifyContent="center" style={{ height: '320px' }}>
    <Looper
      end={4}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40} width={40 + i * 10}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Justify content start, align items start

<Playground>
  <Flex alignItems="flex-start" direction="column" justifyContent="flex-start" style={{ height: '320px' }}>
    <Looper
      end={4}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40} width={40 + i * 10}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Justify content end, align items end

<Playground>
  <Flex alignItems="flex-end" direction="column" justifyContent="flex-end" style={{ height: '320px' }}>
    <Looper
      end={4}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40} width={40 + i * 10}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Align content center, wrap

<Playground>
  <Flex wrap alignContent="center" direction="column" style={{ height: '320px' }}>
    <Looper
      end={10}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40} width={40}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Align content space between, wrap

<Playground>
  <Flex wrap alignContent="space-between" direction="column" style={{ height: '320px' }}>
    <Looper
      end={10}
      render={i => (
        <Flex key={i}>
          <FlexBox height={40} width={40}>
            {i}
          </FlexBox>
        </Flex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Styled flex grow

<Playground>
  <Flex wrap direction="row">
    <Looper
      end={10}
      render={i => (
        <StyledFlex flexGrow={i} key={i}>
          <FlexBox height={40} width="100%">
            {i}
          </FlexBox>
        </StyledFlex>
      )}
      start={0}
    />
  </Flex>
</Playground>

### Styled flex shorthand

<Playground>
  <Flex direction="row" wrap={false}>
    <Looper
      end={10}
      render={i => (
        <StyledFlex flex={`${i} 0 40px`} key={i}>
          <FlexBox height={40} width="100%">
            {i}
          </FlexBox>
        </StyledFlex>
      )}
      start={0}
    />
  </Flex>
</Playground>

## \[props\]

<Props of={Flex} />
