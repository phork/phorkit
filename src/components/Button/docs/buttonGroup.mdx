---
name: ButtonGroup
menu: Input
route: /components/ButtonGroup
---

import { Playground, Props } from 'docz';
import { TimesIcon } from 'icons/TimesIcon';
import { MoonIcon } from 'icons/internal/MoonIcon';
import { SunIcon } from 'icons/internal/SunIcon';
import { Flex } from 'components/Flex';
import { IconText } from 'components/IconText';
import { Rhythm } from 'components/Rhythm';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Button, ButtonGroup, StyledButton } from '../index';

<PageTitle title="ButtonGroup" src="components/Button" />

## Horizontal groups

### Joined group

<Playground>
  <ThemeWrapper>
    <Looper
      list={['pill', 'brick']}
      render={shape => (
        <Rhythm my={2} key={`joinedButtonGroup-${shape}`}>
          <Flex direction="row" justifyContent="space-between" wrap>
            <Looper
              list={['small', 'medium', 'large']}
              render={size => (
                <StateWrapper initialState="first">
                  {({ state, setState }) => (
                    <Rhythm mb={2} mr={2}>
                      <ButtonGroup
                        buttons={[
                          { id: 'first', label: 'First', selected: state === 'first' },
                          { id: 'second', label: 'Second', selected: state === 'second' },
                          { id: 'third', label: 'Third', selected: state === 'third' },
                          { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
                        ]}
                        color="primary"
                        key={`joinedButtonGroup-${shape}-${size}`}
                        onClick={(event, id) => setState(id)}
                        overlap
                        shape={shape}
                        size={size}
                        spacing="joined"
                      />
                    </Rhythm>
                  )}
                </StateWrapper>
              )}
            />
          </Flex>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

### Cozy group

<Playground>
  <ThemeWrapper>
    <Looper
      list={['pill', 'brick']}
      render={shape => (
        <Rhythm my={2} key={`cozyButtonGroup-${shape}`}>
          <Flex direction="row" justifyContent="space-between" wrap>
            <Looper
              list={['small', 'medium', 'large']}
              render={size => (
                <StateWrapper initialState="first">
                  {({ state, setState }) => (
                    <Rhythm mb={2} mr={2}>
                      <ButtonGroup
                        buttons={[
                          { id: 'first', label: 'First', selected: state === 'first' },
                          { id: 'second', label: 'Second', selected: state === 'second' },
                          { id: 'third', label: 'Third', selected: state === 'third' },
                          { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
                        ]}
                        color="primary"
                        key={`cozyButtonGroup-${shape}-${size}`}
                        onClick={(event, id) => setState(id)}
                        overlap
                        spacing="cozy"
                        selectedWeight="outline"
                        shape={shape}
                        size={size}
                        weight="text"
                      />
                    </Rhythm>
                  )}
                </StateWrapper>
              )}
            />
          </Flex>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

### Comfy groups

<Playground>
  <ThemeWrapper>
    <Rhythm my={2} key="comfyButtonGroup">
      <Flex direction="row" justifyContent="space-between" wrap>
        <Looper
          list={['small', 'medium', 'large']}
          render={size => (
            <StateWrapper initialState="first">
              {({ state, setState }) => (
                <Rhythm mb={2} mr={2}>
                  <ButtonGroup
                    buttons={[
                      { id: 'first', label: 'First', selected: state === 'first' },
                      { id: 'second', label: 'Second', selected: state === 'second' },
                      { id: 'third', label: 'Third', selected: state === 'third' },
                      { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
                    ]}
                    color="neutral"
                    key={`comfyButtonGroup-${size}`}
                    onClick={(event, id) => setState(id)}
                    overlap
                    selectedColor="primary"
                    selectedWeight="inline"
                    size={size}
                    spacing="comfy"
                    weight="inline"
                  />
                </Rhythm>
              )}
            </StateWrapper>
          )}
        />
      </Flex>
    </Rhythm>
  </ThemeWrapper>
</Playground>

## Vertical groups

### Joined group

<Playground>
  <ThemeWrapper>
    <Flex direction="row" justifyContent="space-between" wrap>
      <StateWrapper initialState="second">
        {({ state, setState }) => (
          <Rhythm mb={2} mr={2}>
            <ButtonGroup
              shape="brick"
              orientation="vertical"
              spacing="joined"
              overlap
              layout="vertical"
              color="primary"
              onClick={(event, id) => setState(id)}
              buttons={[
                { id: 'first', label: 'First', selected: state === 'first', align: 'left' },
                { id: 'second', label: 'Second', selected: state === 'second', align: 'left' },
                { id: 'third', label: 'Third', selected: state === 'third', align: 'left' },
                { id: 'fourth', label: 'Fourth', selected: state === 'fourth', align: 'left' },
              ]}
              style={{ width: '200px' }}
            />
          </Rhythm>
        )}
      </StateWrapper>
      <StateWrapper initialState="second">
        {({ state, setState }) => (
          <Rhythm mb={2} mr={2}>
            <ButtonGroup
              orientation="vertical"
              spacing="joined"
              overlap
              layout="vertical"
              color="primary"
              onClick={(event, id) => setState(id)}
              buttons={[
                { id: 'first', label: 'First', selected: state === 'first', align: 'center' },
                { id: 'second', label: 'Second', selected: state === 'second', align: 'center' },
                { id: 'third', label: 'Third', selected: state === 'third', align: 'center' },
                { id: 'fourth', label: 'Fourth', selected: state === 'fourth', align: 'center' },
              ]}
              style={{ width: '200px' }}
            />
          </Rhythm>
        )}
      </StateWrapper>
      <StateWrapper initialState="second">
        {({ state, setState }) => (
          <Rhythm mb={2} mr={2}>
            <ButtonGroup
              orientation="vertical"
              spacing="cozy"
              color="primary"
              onClick={(event, id) => setState(id)}
              buttons={[
                { id: 'first', label: 'First', selected: state === 'first', align: 'left' },
                { id: 'second', label: 'Second', selected: state === 'second', align: 'left' },
                { id: 'third', label: 'Third', selected: state === 'third', align: 'left' },
                { id: 'fourth', label: 'Fourth', selected: state === 'fourth', align: 'left' },
              ]}
              style={{ width: '200px' }}
            />
          </Rhythm>
        )}
      </StateWrapper>
      <StateWrapper initialState="second">
        {({ state, setState }) => (
          <Rhythm mb={2} mr={2}>
            <ButtonGroup
              orientation="vertical"
              spacing="cozy"
              align="left"
              color="primary"
              onClick={(event, id) => setState(id)}
              buttons={[
                { id: 'first', label: 'First', selected: state === 'first', weight: 'text' },
                { id: 'second', label: 'Second', selected: state === 'second', weight: 'text' },
                { id: 'third', label: 'Third', selected: state === 'third', weight: 'text' },
                { id: 'fourth', label: 'Fourth', selected: state === 'fourth', weight: 'text' },
              ]}
              style={{ width: '200px' }}
            />
          </Rhythm>
        )}
      </StateWrapper>
    </Flex>

  </ThemeWrapper>
</Playground>

## Full width group

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState="first">
      {({ state, setState }) => (
        <ButtonGroup
          fullWidth
          spacing="joined"
          overlap
          color="primary"
          onClick={(event, id) => setState(id)}
          buttons={[
            { id: 'first', label: 'First', selected: state === 'first' },
            { id: 'second', label: 'Second', selected: state === 'second' },
            { id: 'third', label: 'Third', selected: state === 'third' },
            { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
          ]}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## Variations

### Icon group

<Playground>
  <ThemeWrapper>
    <Flex direction="row" alignItems="center" justifyContent="space-between" style={{ maxWidth: 400 }} wrap>
      <Looper
        list={['horizontal', 'vertical']}
        render={orientation => (
          <React.Fragment>
            <StateWrapper initialState="first">
              {({ state, setState }) => (
                <ButtonGroup
                  color="primary"
                  onClick={(event, id) => setState(id)}
                  orientation={orientation}
                  overlap
                  size="small"
                  spacing="joined"
                  buttons={[
                    {
                      id: 'first',
                      label: (
                        <Rhythm
                          mx={3}
                          ml={orientation === 'horizontal' ? 4 : 3}
                          my={2}
                          mt={orientation === 'vertical' ? 3 : 2}
                        >
                          <TimesIcon scale="xsmall" />
                        </Rhythm>
                      ),
                      selected: state === 'first',
                      noPadding: true,
                    },
                    {
                      id: 'second',
                      label: (
                        <Rhythm mx={3} my={2}>
                          <TimesIcon scale="xsmall" />
                        </Rhythm>
                      ),
                      selected: state === 'second',
                      noPadding: true,
                    },
                    {
                      id: 'third',
                      label: (
                        <Rhythm mx={3} my={2}>
                          <TimesIcon scale="xsmall" />
                        </Rhythm>
                      ),
                      selected: state === 'third',
                      noPadding: true,
                    },
                    {
                      id: 'fourth',
                      label: (
                        <Rhythm
                          mx={3}
                          mr={orientation === 'horizontal' ? 4 : 3}
                          my={2}
                          mb={orientation === 'vertical' ? 3 : 2}
                        >
                          <TimesIcon scale="xsmall" />
                        </Rhythm>
                      ),
                      selected: state === 'fourth',
                      noPadding: true,
                    },
                  ]}
                />
              )}
            </StateWrapper>
            <StateWrapper initialState="first">
              {({ state, setState }) => (
                <ButtonGroup
                  color="neutral"
                  onClick={(event, id) => setState(id)}
                  orientation={orientation}
                  overlap
                  shape="brick"
                  size="small"
                  spacing="joined"
                  buttons={[
                    {
                      id: 'first',
                      label: (
                        <Rhythm mx={2} my={2}>
                          <SunIcon size={14} />
                        </Rhythm>
                      ),
                      selected: state === 'first',
                      noPadding: true,
                    },
                    {
                      id: 'last',
                      label: (
                        <Rhythm mx={2} my={2}>
                          <MoonIcon size={14} />
                        </Rhythm>
                      ),
                      selected: state === 'last',
                      noPadding: true,
                    },
                  ]}
                />
              )}
            </StateWrapper>
          </React.Fragment>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

### Contrast group

<Playground>
  <ThemeWrapper contrast>
    <StateWrapper initialState="third">
      {({ state, setState }) => (
        <ButtonGroup
          contrast
          spacing="joined"
          overlap
          onClick={(event, id) => setState(id)}
          buttons={[
            { id: 'first', label: 'First', selected: state === 'first' },
            { id: 'second', label: 'Second', selected: state === 'second' },
            { id: 'third', label: 'Third', selected: state === 'third' },
            { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
          ]}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Group using links

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState="third">
      {({ state, setState }) => (
        <ButtonGroup
          spacing="joined"
          overlap
          color="primary"
          onClick={(event, id) => setState(id)}
          buttons={[
            { id: 'first', label: 'First', href: '#first', selected: state === 'first' },
            { id: 'second', label: 'Second', href: '#second', selected: state === 'second' },
            { id: 'third', label: 'Third', href: '#third', selected: state === 'third' },
            { id: 'fourth', label: 'Fourth', href: '#fourth', selected: state === 'fourth' },
          ]}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Group with children

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState="third">
      {({ state, setState }) => (
        <ButtonGroup spacing="joined" overlap onClick={(event, id) => setState(id)}>
          <Button
            key="first"
            shape="pill"
            color={state === 'first' ? 'primary' : 'neutral'}
            weight={state === 'first' ? 'filled' : 'outline'}
            data-value="first"
          >
            First
          </Button>
          <Button
            key="second"
            shape="pill"
            color={state === 'second' ? 'primary' : 'neutral'}
            weight={state === 'second' ? 'filled' : 'outline'}
            data-value="second"
          >
            Second
          </Button>
          <Button
            key="third"
            shape="pill"
            color={state === 'third' ? 'primary' : 'neutral'}
            weight={state === 'third' ? 'filled' : 'outline'}
            data-value="third"
          >
            Third
          </Button>
          <Button
            key="fourth"
            shape="pill"
            color={state === 'fourth' ? 'primary' : 'neutral'}
            weight={state === 'fourth' ? 'filled' : 'outline'}
            data-value="fourth"
          >
            Fourth
          </Button>
        </ButtonGroup>
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Group with custom labels

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState="third">
      {({ state, setState }) => (
        <ButtonGroup
          spacing="joined"
          overlap
          onClick={(event, id) => setState(id)}
          buttons={[
            {
              id: 'first',
              label: selected => (
                <Button
                  value="first"
                  weight={selected ? undefined : 'outline'}
                  color={selected ? 'primary' : 'neutral'}
                >
                  First
                </Button>
              ),
              selected: state === 'first',
            },
            {
              id: 'second',
              label: selected => (
                <Button
                  value="second"
                  weight={selected ? undefined : 'outline'}
                  color={selected ? 'primary' : 'neutral'}
                >
                  Second
                </Button>
              ),
              selected: state === 'second',
            },
            {
              id: 'third',
              label: selected => (
                <Button
                  value="third"
                  weight={selected ? undefined : 'outline'}
                  color={selected ? 'primary' : 'neutral'}
                >
                  Third
                </Button>
              ),
              selected: state === 'third',
            },
            {
              id: 'fourth',
              label: selected => (
                <Button
                  value="fourth"
                  weight={selected ? undefined : 'outline'}
                  color={selected ? 'primary' : 'neutral'}
                >
                  Fourth
                </Button>
              ),
              selected: state === 'fourth',
            },
          ]}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={ButtonGroup} />
