---
name: ButtonGroup
menu: Buttons
route: /components/ButtonGroup
---

import { Playground, Props } from 'docz';
import { TimesIcon } from 'icons/TimesIcon';
import { MoonIcon } from 'icons/internal/MoonIcon';
import { SunIcon } from 'icons/internal/SunIcon';
import { Flex } from 'components/Flex';
import { Looper } from 'components/Looper';
import { Rhythm } from 'components/Rhythm';
import { PageTitle } from 'docs/helpers/PageTitle';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Button, ButtonGroup } from '../index';

<PageTitle src="components/Button" title="ButtonGroup" />

## Horizontal groups

### Joined groups

<Playground>
  <ThemeWrapper>
    <Looper
      list={['pill', 'brick']}
      render={shape => (
        <Rhythm key={shape} my={2}>
          <Flex wrap direction="row" justifyContent="space-between">
            <Looper
              list={['small', 'medium', 'large']}
              render={size => (
                <StateWrapper initialState="first" key={`${shape}-${size}`}>
                  {({ state, setState }) => (
                    <Rhythm mb={2} mr={2}>
                      <ButtonGroup
                        buttons={[
                          { id: 'first', label: 'First', selected: state === 'first' },
                          { id: 'second', label: 'Second', selected: state === 'second' },
                          { id: 'third', label: 'Third', selected: state === 'third' },
                          { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
                        ]}
                        color="primary"
                        onClick={(event, id) => setState(id)}
                        shape={shape}
                        size={size}
                        spacing="joined"
                      />
                    </Rhythm>
                  )}
                </StateWrapper>
              )}
            />
          </Flex>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

### Divided groups

<Playground>
  <ThemeWrapper>
    <Looper
      list={['pill', 'brick']}
      render={shape => (
        <Rhythm key={shape} my={2}>
          <Flex wrap direction="row" justifyContent="space-between">
            <Looper
              list={['small', 'medium', 'large']}
              render={size => (
                <StateWrapper initialState="first" key={`${shape}-${size}`}>
                  {({ state, setState }) => (
                    <Rhythm mb={2} mr={2}>
                      <ButtonGroup
                        buttons={[
                          {
                            id: 'first',
                            label: 'First',
                            selected: state === 'first',
                          },
                          {
                            id: 'second',
                            label: 'Second',
                            selected: state === 'second',
                          },
                          {
                            id: 'third',
                            label: 'Third',
                            selected: state === 'third',
                          },
                          {
                            id: 'fourth',
                            label: 'Fourth',
                            selected: state === 'fourth',
                          },
                        ]}
                        color="primary"
                        onClick={(event, id) => setState(id)}
                        selectedStyle={{ opacity: 0.8 }}
                        shape={shape}
                        size={size}
                        spacing="divided"
                        weight="solid"
                      />
                    </Rhythm>
                  )}
                </StateWrapper>
              )}
            />
          </Flex>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

### Cozy groups

<Playground>
  <ThemeWrapper>
    <Looper
      list={['pill', 'brick']}
      render={shape => (
        <Rhythm key={shape} my={2}>
          <Flex wrap direction="row" justifyContent="space-between">
            <Looper
              list={['small', 'medium', 'large']}
              render={size => (
                <StateWrapper initialState="first" key={`${shape}-${size}`}>
                  {({ state, setState }) => (
                    <Rhythm mb={2} mr={2}>
                      <ButtonGroup
                        buttons={[
                          { id: 'first', label: 'First', selected: state === 'first' },
                          { id: 'second', label: 'Second', selected: state === 'second' },
                          { id: 'third', label: 'Third', selected: state === 'third' },
                          { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
                        ]}
                        color="primary"
                        onClick={(event, id) => setState(id)}
                        selectedWeight="outlined"
                        shape={shape}
                        size={size}
                        spacing="cozy"
                        weight="ghost"
                      />
                    </Rhythm>
                  )}
                </StateWrapper>
              )}
            />
          </Flex>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

### Comfy groups

<Playground>
  <ThemeWrapper>
    <Rhythm key="comfyButtonGroup" my={2}>
      <Flex wrap direction="row" justifyContent="space-between">
        <Looper
          list={['small', 'medium', 'large']}
          render={size => (
            <StateWrapper initialState="first" key={size}>
              {({ state, setState }) => (
                <Rhythm mb={2} mr={2}>
                  <ButtonGroup
                    buttons={[
                      { id: 'first', label: 'First', selected: state === 'first' },
                      { id: 'second', label: 'Second', selected: state === 'second' },
                      { id: 'third', label: 'Third', selected: state === 'third' },
                      { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
                    ]}
                    color="neutral"
                    onClick={(event, id) => setState(id)}
                    selectedColor="primary"
                    selectedWeight="inline"
                    size={size}
                    spacing="comfy"
                    weight="inline"
                  />
                </Rhythm>
              )}
            </StateWrapper>
          )}
        />
      </Flex>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Full width group

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState="first">
      {({ state, setState }) => (
        <ButtonGroup
          fullWidth
          buttons={[
            { id: 'first', label: 'First', selected: state === 'first' },
            { id: 'second', label: 'Second', selected: state === 'second' },
            { id: 'third', label: 'Third', selected: state === 'third' },
            { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
          ]}
          color="primary"
          onClick={(event, id) => setState(id)}
          spacing="joined"
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Wrapped groups

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState="first">
      {({ state, setState }) => (
        <ButtonGroup
          wrap
          buttons={[
            { id: 'first', label: 'First', selected: state === 'first' },
            { id: 'second', label: 'Second', selected: state === 'second' },
            { id: 'third', label: 'Third', selected: state === 'third' },
            { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
            { id: 'fifth', label: 'Fifth', selected: state === 'fifth' },
            { id: 'sixth', label: 'Sixth', selected: state === 'sixth' },
            { id: 'seventh', label: 'Seventh', selected: state === 'seventh' },
          ]}
          color="primary"
          onClick={(event, id) => setState(id)}
          selectedWeight="solid"
          shape="pill"
          size="medium"
          spacing="cozy"
          style={{ maxWidth: 400 }}
          weight="outlined"
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## Vertical groups

### Joined groups

<Playground>
  <ThemeWrapper>
    <Flex wrap direction="row" justifyContent="space-between">
      <Looper
        list={['small', 'medium', 'large']}
        render={size => (
          <StateWrapper initialState="first" key={size}>
            {({ state, setState }) => (
              <Rhythm mb={2} mr={2}>
                <ButtonGroup
                  buttons={[
                    { id: 'first', label: 'First', selected: state === 'first', align: 'left' },
                    { id: 'second', label: 'Second', selected: state === 'second', align: 'left' },
                    { id: 'third', label: 'Third', selected: state === 'third', align: 'left' },
                    { id: 'fourth', label: 'Fourth', selected: state === 'fourth', align: 'left' },
                  ]}
                  color="primary"
                  onClick={(event, id) => setState(id)}
                  orientation="vertical"
                  shape="brick"
                  size={size}
                  spacing="joined"
                  style={{ width: '200px' }}
                />
              </Rhythm>
            )}
          </StateWrapper>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

### Divided groups

<Playground>
  <ThemeWrapper>
    <Flex wrap direction="row" justifyContent="space-between">
      <Looper
        list={['small', 'medium', 'large']}
        render={size => (
          <StateWrapper initialState="first" key={size}>
            {({ state, setState }) => (
              <Rhythm mb={2} mr={2}>
                <ButtonGroup
                  buttons={[
                    { id: 'first', label: 'First', selected: state === 'first', align: 'left' },
                    { id: 'second', label: 'Second', selected: state === 'second', align: 'left' },
                    { id: 'third', label: 'Third', selected: state === 'third', align: 'left' },
                    { id: 'fourth', label: 'Fourth', selected: state === 'fourth', align: 'left' },
                  ]}
                  color="primary"
                  onClick={(event, id) => setState(id)}
                  orientation="vertical"
                  shape="brick"
                  size={size}
                  spacing="divided"
                  style={{ width: '200px' }}
                  weight="solid"
                />
              </Rhythm>
            )}
          </StateWrapper>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

### Cozy groups

<Playground>
  <ThemeWrapper>
    <Flex wrap direction="row" justifyContent="space-between">
      <StateWrapper initialState="second">
        {({ state, setState }) => (
          <Rhythm mb={2} mr={2}>
            <ButtonGroup
              buttons={[
                { id: 'first', label: 'First', selected: state === 'first', align: 'left' },
                { id: 'second', label: 'Second', selected: state === 'second', align: 'left' },
                { id: 'third', label: 'Third', selected: state === 'third', align: 'left' },
                { id: 'fourth', label: 'Fourth', selected: state === 'fourth', align: 'left' },
              ]}
              color="primary"
              onClick={(event, id) => setState(id)}
              orientation="vertical"
              spacing="cozy"
              style={{ width: '200px' }}
            />
          </Rhythm>
        )}
      </StateWrapper>
      <StateWrapper initialState="second">
        {({ state, setState }) => (
          <Rhythm mb={2} mr={2}>
            <ButtonGroup
              buttons={[
                { id: 'first', label: 'First', selected: state === 'first', weight: 'text' },
                { id: 'second', label: 'Second', selected: state === 'second', weight: 'text' },
                { id: 'third', label: 'Third', selected: state === 'third', weight: 'text' },
                { id: 'fourth', label: 'Fourth', selected: state === 'fourth', weight: 'text' },
              ]}
              color="primary"
              onClick={(event, id) => setState(id)}
              orientation="vertical"
              spacing="cozy"
              style={{ width: '200px' }}
            />
          </Rhythm>
        )}
      </StateWrapper>
    </Flex>
  </ThemeWrapper>
</Playground>

### Display default

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState="first">
      {({ state, setState }) => (
        <ButtonGroup
          buttons={[
            { id: 'first', label: 'First', selected: state === 'first' },
            { id: 'second', label: 'Second', selected: state === 'second' },
            { id: 'third', label: 'Third', selected: state === 'third' },
            { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
          ]}
          color="primary"
          onClick={(event, id) => setState(id)}
          orientation="vertical"
          shape="brick"
          spacing="joined"
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Display inline

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState="first">
      {({ state, setState }) => (
        <ButtonGroup
          buttons={[
            { id: 'first', label: 'First', selected: state === 'first' },
            { id: 'second', label: 'Second', selected: state === 'second' },
            { id: 'third', label: 'Third', selected: state === 'third' },
            { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
          ]}
          color="primary"
          display="inline"
          onClick={(event, id) => setState(id)}
          orientation="vertical"
          shape="brick"
          spacing="joined"
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Display block

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState="first">
      {({ state, setState }) => (
        <ButtonGroup
          buttons={[
            { id: 'first', label: 'First', selected: state === 'first' },
            { id: 'second', label: 'Second', selected: state === 'second' },
            { id: 'third', label: 'Third', selected: state === 'third' },
            { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
          ]}
          color="neutral"
          display="block"
          onClick={(event, id) => setState(id)}
          orientation="vertical"
          selectedColor="primary"
          selectedWeight="inline"
          spacing="comfy"
          weight="inline"
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Alignment

<Playground>
  <ThemeWrapper>
    <Looper
      list={['left', 'center', 'right']}
      render={align => (
        <StateWrapper initialState="first">
          {({ state, setState }) => (
            <Rhythm key={align} my={2}>
              <ButtonGroup
                align={align}
                buttons={[
                  { id: 'first', label: 'First', selected: state === 'first' },
                  { id: 'second', label: 'Second', selected: state === 'second' },
                  { id: 'third', label: 'Third', selected: state === 'third' },
                  { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
                ]}
                color="primary"
                onClick={(event, id) => setState(id)}
                spacing="joined"
              />
            </Rhythm>
          )}
        </StateWrapper>
      )}
    />
  </ThemeWrapper>
</Playground>

## Variations

### Icon group

<Playground>
  <ThemeWrapper>
    <Flex wrap alignItems="center" direction="row" justifyContent="space-between" style={{ maxWidth: 400 }}>
      <Looper
        list={['horizontal', 'vertical']}
        render={orientation => (
          <React.Fragment key={orientation}>
            <StateWrapper initialState="first">
              {({ state, setState }) => (
                <ButtonGroup
                  buttons={[
                    {
                      id: 'first',
                      label: (
                        <Rhythm
                          ml={orientation === 'horizontal' ? 4 : 3}
                          mt={orientation === 'vertical' ? 3 : 2}
                          mx={3}
                          my={2}
                        >
                          <TimesIcon scale="xsmall" />
                        </Rhythm>
                      ),
                      selected: state === 'first',
                      noPadding: true,
                    },
                    {
                      id: 'second',
                      label: (
                        <Rhythm mx={3} my={2}>
                          <TimesIcon scale="xsmall" />
                        </Rhythm>
                      ),
                      selected: state === 'second',
                      noPadding: true,
                    },
                    {
                      id: 'third',
                      label: (
                        <Rhythm mx={3} my={2}>
                          <TimesIcon scale="xsmall" />
                        </Rhythm>
                      ),
                      selected: state === 'third',
                      noPadding: true,
                    },
                    {
                      id: 'fourth',
                      label: (
                        <Rhythm
                          mb={orientation === 'vertical' ? 3 : 2}
                          mr={orientation === 'horizontal' ? 4 : 3}
                          mx={3}
                          my={2}
                        >
                          <TimesIcon scale="xsmall" />
                        </Rhythm>
                      ),
                      selected: state === 'fourth',
                      noPadding: true,
                    },
                  ]}
                  color="primary"
                  display="inline"
                  onClick={(event, id) => setState(id)}
                  orientation={orientation}
                  size="small"
                  spacing="joined"
                />
              )}
            </StateWrapper>
            <StateWrapper initialState="first">
              {({ state, setState }) => (
                <ButtonGroup
                  buttons={[
                    {
                      id: 'first',
                      label: (
                        <Rhythm mx={2} my={2}>
                          <SunIcon size={14} />
                        </Rhythm>
                      ),
                      selected: state === 'first',
                      noPadding: true,
                    },
                    {
                      id: 'last',
                      label: (
                        <Rhythm mx={2} my={2}>
                          <MoonIcon size={14} />
                        </Rhythm>
                      ),
                      selected: state === 'last',
                      noPadding: true,
                    },
                  ]}
                  color="neutral"
                  onClick={(event, id) => setState(id)}
                  orientation={orientation}
                  shape="brick"
                  size="small"
                  spacing="joined"
                />
              )}
            </StateWrapper>
          </React.Fragment>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

### Contrast group

<Playground>
  <ThemeWrapper contrast>
    <StateWrapper initialState="third">
      {({ state, setState }) => (
        <ButtonGroup
          contrast
          buttons={[
            { id: 'first', label: 'First', selected: state === 'first' },
            { id: 'second', label: 'Second', selected: state === 'second' },
            { id: 'third', label: 'Third', selected: state === 'third' },
            { id: 'fourth', label: 'Fourth', selected: state === 'fourth' },
          ]}
          onClick={(event, id) => setState(id)}
          spacing="joined"
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Group using links

_Note that the links will cause the page to reload._

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState="third">
      {({ state, setState }) => (
        <ButtonGroup
          buttons={[
            { id: 'first', label: 'First', href: '#first', selected: state === 'first' },
            { id: 'second', label: 'Second', href: '#second', selected: state === 'second' },
            { id: 'third', label: 'Third', href: '#third', selected: state === 'third' },
            { id: 'fourth', label: 'Fourth', href: '#fourth', selected: state === 'fourth' },
          ]}
          color="primary"
          onClick={(event, id) => setState(id)}
          spacing="joined"
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Group with children

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState="third">
      {({ state, setState }) => (
        <ButtonGroup onClick={(event, id) => setState(id)} spacing="joined">
          <Button
            color={state === 'first' ? 'primary' : 'neutral'}
            data-value="first"
            key="first"
            shape="pill"
            weight={state === 'first' ? 'solid' : 'outlined'}
          >
            First
          </Button>
          <Button
            color={state === 'second' ? 'primary' : 'neutral'}
            data-value="second"
            key="second"
            shape="pill"
            weight={state === 'second' ? 'solid' : 'outlined'}
          >
            Second
          </Button>
          <Button
            color={state === 'third' ? 'primary' : 'neutral'}
            data-value="third"
            key="third"
            shape="pill"
            weight={state === 'third' ? 'solid' : 'outlined'}
          >
            Third
          </Button>
          <Button
            color={state === 'fourth' ? 'primary' : 'neutral'}
            data-value="fourth"
            key="fourth"
            shape="pill"
            weight={state === 'fourth' ? 'solid' : 'outlined'}
          >
            Fourth
          </Button>
        </ButtonGroup>
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Group with custom labels

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState="third">
      {({ state, setState }) => (
        <ButtonGroup
          buttons={[
            {
              id: 'first',
              label: selected => (
                <Button
                  color={selected ? 'primary' : 'neutral'}
                  value="first"
                  weight={selected ? undefined : 'outlined'}
                >
                  First
                </Button>
              ),
              selected: state === 'first',
            },
            {
              id: 'second',
              label: selected => (
                <Button
                  color={selected ? 'primary' : 'neutral'}
                  value="second"
                  weight={selected ? undefined : 'outlined'}
                >
                  Second
                </Button>
              ),
              selected: state === 'second',
            },
            {
              id: 'third',
              label: selected => (
                <Button
                  color={selected ? 'primary' : 'neutral'}
                  value="third"
                  weight={selected ? undefined : 'outlined'}
                >
                  Third
                </Button>
              ),
              selected: state === 'third',
            },
            {
              id: 'fourth',
              label: selected => (
                <Button
                  color={selected ? 'primary' : 'neutral'}
                  value="fourth"
                  weight={selected ? undefined : 'outlined'}
                >
                  Fourth
                </Button>
              ),
              selected: state === 'fourth',
            },
          ]}
          onClick={(event, id) => setState(id)}
          spacing="joined"
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={ButtonGroup} />
