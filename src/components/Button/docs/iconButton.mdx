---
name: IconButton
menu: Input
route: /components/IconButton
---

import { ColorLooper } from 'docs/helpers/ColorLooper';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Playground, Props } from 'docz';
import { SpinnerIcon } from 'icons/SpinnerIcon';
import { BlobbrIcon } from 'icons/internal/BlobbrIcon';
import { Flex } from 'components/Flex';
import { Rhythm } from 'components/Rhythm';
import { ColoredIconButton, IconButton } from '../index';

<PageTitle title="IconButton" src="components/Button" />

## Basic icon buttons

<Playground>
  <ThemeWrapper>
    <Looper
      list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
      render={color => (
        <Flex direction="row" alignItems="center" wrap key={`iconButton-${color}`}>
          <Looper
            list={[false, true]}
            render={loading => (
              <Flex direction="row" alignItems="center" key={`iconButton-${color}-${loading}`}>
                <Rhythm m={2}>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="medium" />}
                    loading={loading}
                    onClick={() => console.log('medium')}
                    weight="text"
                  >
                    <BlobbrIcon scale="medium" />
                  </IconButton>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="large" />}
                    loading={loading}
                    onClick={() => console.log('large')}
                    weight="text"
                  >
                    <BlobbrIcon scale="large" />
                  </IconButton>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="xlarge" />}
                    loading={loading}
                    onClick={() => console.log('xlarge')}
                    weight="text"
                  >
                    <BlobbrIcon scale="xlarge" />
                  </IconButton>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="medium" />}
                    loading={loading}
                    onClick={() => console.log('round')}
                    shape="round"
                  >
                    <BlobbrIcon scale="medium" />
                  </IconButton>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="medium" />}
                    loading={loading}
                    onClick={() => console.log('square')}
                    shape="square"
                  >
                    <BlobbrIcon scale="medium" />
                  </IconButton>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="medium" />}
                    loading={loading}
                    onClick={() => console.log('round outline')}
                    shape="round"
                    weight="outline"
                  >
                    <BlobbrIcon scale="medium" />
                  </IconButton>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="medium" />}
                    loading={loading}
                    onClick={() => console.log('square outline')}
                    shape="square"
                    weight="outline"
                  >
                    <BlobbrIcon scale="medium" />
                  </IconButton>
                </Rhythm>
              </Flex>
            )}
          />
        </Flex>
      )}
    />
  </ThemeWrapper>
</Playground>

## Variations

### Contrast icon buttons

<Playground>
  <ThemeWrapper contrast>
    <Flex direction="row" alignItems="center">
      <Rhythm m={1}>
        <IconButton contrast>
          <BlobbrIcon scale="medium" />
        </IconButton>
        <IconButton contrast>
          <BlobbrIcon scale="large" />
        </IconButton>
        <IconButton contrast>
          <BlobbrIcon scale="xlarge" />
        </IconButton>
        <IconButton contrast shape="round">
          <BlobbrIcon scale="medium" />
        </IconButton>
        <IconButton contrast shape="square">
          <BlobbrIcon scale="medium" />
        </IconButton>
        <IconButton contrast onClick={() => console.log('round outline')} shape="round" weight="outline">
          <BlobbrIcon scale="medium" />
        </IconButton>
        <IconButton contrast onClick={() => console.log('square outline')} shape="square" weight="outline">
          <BlobbrIcon scale="medium" />
        </IconButton>
        <IconButton contrast loading loader={<SpinnerIcon scale="medium" />}>
          <BlobbrIcon scale="xlarge" />
        </IconButton>
        <IconButton contrast loading shape="square" loader={<SpinnerIcon scale="medium" />}>
          <BlobbrIcon scale="medium" />
        </IconButton>
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

## Colored icon buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Flex direction="column">
        <ColorLooper
          themeId={themeId}
          group="primary"
          render={({ id }) => (
            <Flex direction="row" alignItems="center" key={`coloredIconButton-${id}`}>
              <Rhythm m={2}>
                <ColoredIconButton colorId={id} onClick={() => console.log('medium')} weight="text">
                  <BlobbrIcon scale="medium" />
                </ColoredIconButton>
                <ColoredIconButton colorId={id} onClick={() => console.log('large')} weight="text">
                  <BlobbrIcon scale="large" />
                </ColoredIconButton>
                <ColoredIconButton colorId={id} onClick={() => console.log('xlarge')} weight="text">
                  <BlobbrIcon scale="xlarge" />
                </ColoredIconButton>
                <ColoredIconButton colorId={id} onClick={() => console.log('round')} shape="round">
                  <BlobbrIcon scale="medium" />
                </ColoredIconButton>
                <ColoredIconButton colorId={id} onClick={() => console.log('square')} shape="square">
                  <BlobbrIcon scale="medium" />
                </ColoredIconButton>
                <ColoredIconButton
                  colorId={id}
                  onClick={() => console.log('round outline')}
                  shape="round"
                  weight="outline"
                >
                  <BlobbrIcon scale="medium" />
                </ColoredIconButton>
                <ColoredIconButton
                  colorId={id}
                  onClick={() => console.log('square outline')}
                  shape="square"
                  weight="outline"
                >
                  <BlobbrIcon scale="medium" />
                </ColoredIconButton>
                <ColoredIconButton colorId={id} loading loader={<SpinnerIcon scale="medium" />}>
                  <BlobbrIcon scale="xlarge" />
                </ColoredIconButton>
                <ColoredIconButton colorId={id} loading shape="square" loader={<SpinnerIcon scale="medium" />}>
                  <BlobbrIcon scale="medium" />
                </ColoredIconButton>
              </Rhythm>
            </Flex>
          )}
        />
      </Flex>
    )}
  </ThemeWrapper>
</Playground>

### Link icon buttons

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <Flex direction="row" alignItems="center">
      <Rhythm m={1}>
        <IconButton href="#linked" onClick={() => console.log('default')} color="primary">
          <BlobbrIcon scale="xlarge" />
        </IconButton>
        <IconButton href="#linked" onClick={() => console.log('round')} shape="round" color="primary">
          <BlobbrIcon scale="medium" />
        </IconButton>
        <IconButton href="#linked" onClick={() => console.log('square')} shape="square" color="primary">
          <BlobbrIcon scale="medium" />
        </IconButton>
        <IconButton
          href="#linked"
          onClick={() => console.log('square')}
          disabled
          loading
          shape="square"
          color="primary"
          loader={<SpinnerIcon scale="medium" />}
        >
          <BlobbrIcon scale="medium" />
        </IconButton>
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

### Functional icon buttons

<Playground>
  <ThemeWrapper>
    <IconButton onClick={() => console.log('default')} color="primary">
      {({ scale = ['medium', 'large', 'xlarge', 'xxlarge'][Math.floor(Math.random() * 4)] }) => (
        <BlobbrIcon scale={scale} />
      )}
    </IconButton>
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={IconButton} />
