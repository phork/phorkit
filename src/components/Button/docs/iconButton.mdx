---
name: IconButton
menu: Buttons
route: /components/IconButton
---

import { Playground, Props } from 'docz';
import { SpinnerIcon } from 'icons/SpinnerIcon';
import { BlobbrIcon } from 'icons/internal/BlobbrIcon';
import { Flex } from 'components/Flex';
import { Looper } from 'components/Looper';
import { Rhythm } from 'components/Rhythm';
import { ColorLooper } from 'docs/helpers/ColorLooper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { ColoredIconButton, IconButton, StyledIconButton } from '../index';

<PageTitle src="components/Button" title="IconButton" />

## Basic icon buttons

<Playground>
  <ThemeWrapper>
    <Looper
      list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
      render={color => (
        <Flex wrap alignItems="center" direction="row" key={color}>
          <Looper
            list={[false, true]}
            render={loading => (
              <Flex alignItems="center" direction="row" key={`${color}-${loading}`}>
                <Rhythm m={2}>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon size={20} />}
                    loading={loading}
                    onClick={() => console.log('circle')}
                    shape="circle"
                    weight="ghost"
                  >
                    <BlobbrIcon size={20} />
                  </IconButton>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="medium" />}
                    loading={loading}
                    onClick={() => console.log('circle')}
                    shape="circle"
                  >
                    <BlobbrIcon scale="medium" />
                  </IconButton>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="medium" />}
                    loading={loading}
                    onClick={() => console.log('square')}
                    shape="square"
                  >
                    <BlobbrIcon scale="medium" />
                  </IconButton>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="medium" />}
                    loading={loading}
                    onClick={() => console.log('circle shaded')}
                    shape="circle"
                    weight="shaded"
                  >
                    <BlobbrIcon scale="medium" />
                  </IconButton>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="medium" />}
                    loading={loading}
                    onClick={() => console.log('square shaded')}
                    shape="square"
                    weight="shaded"
                  >
                    <BlobbrIcon scale="medium" />
                  </IconButton>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="medium" />}
                    loading={loading}
                    onClick={() => console.log('circle outline')}
                    shape="circle"
                    weight="outlined"
                  >
                    <BlobbrIcon scale="medium" />
                  </IconButton>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="medium" />}
                    loading={loading}
                    onClick={() => console.log('square outline')}
                    shape="square"
                    weight="outlined"
                  >
                    <BlobbrIcon scale="medium" />
                  </IconButton>
                  <IconButton
                    color={color}
                    loader={<SpinnerIcon scale="xlarge" />}
                    loading={loading}
                    onClick={() => console.log('xlarge')}
                    weight="inline"
                  >
                    <BlobbrIcon scale="xlarge" />
                  </IconButton>
                </Rhythm>
              </Flex>
            )}
          />
        </Flex>
      )}
    />
  </ThemeWrapper>
</Playground>

## Variations

### Contrast icon buttons

<Playground>
  <ThemeWrapper contrast>
    <Flex wrap alignItems="center" direction="row">
      <Looper
        list={[false, true]}
        render={loading => (
          <Flex alignItems="center" direction="row" key={loading ? 'loading' : 'standard'}>
            <Rhythm m={2}>
              <IconButton
                contrast
                loader={<SpinnerIcon size={20} />}
                loading={loading}
                onClick={() => console.log('circle')}
                shape="circle"
                weight="ghost"
              >
                <BlobbrIcon size={20} />
              </IconButton>
              <IconButton
                contrast
                loader={<SpinnerIcon scale="medium" />}
                loading={loading}
                onClick={() => console.log('circle')}
                shape="circle"
              >
                <BlobbrIcon scale="medium" />
              </IconButton>
              <IconButton
                contrast
                loader={<SpinnerIcon scale="medium" />}
                loading={loading}
                onClick={() => console.log('square')}
                shape="square"
              >
                <BlobbrIcon scale="medium" />
              </IconButton>
              <IconButton
                contrast
                loader={<SpinnerIcon scale="medium" />}
                loading={loading}
                onClick={() => console.log('circle shaded')}
                shape="circle"
                weight="shaded"
              >
                <BlobbrIcon scale="medium" />
              </IconButton>
              <IconButton
                contrast
                loader={<SpinnerIcon scale="medium" />}
                loading={loading}
                onClick={() => console.log('square shaded')}
                shape="square"
                weight="shaded"
              >
                <BlobbrIcon scale="medium" />
              </IconButton>
              <IconButton
                contrast
                loader={<SpinnerIcon scale="medium" />}
                loading={loading}
                onClick={() => console.log('circle outline')}
                shape="circle"
                weight="outlined"
              >
                <BlobbrIcon scale="medium" />
              </IconButton>
              <IconButton
                contrast
                loader={<SpinnerIcon scale="medium" />}
                loading={loading}
                onClick={() => console.log('square outline')}
                shape="square"
                weight="outlined"
              >
                <BlobbrIcon scale="medium" />
              </IconButton>
              <IconButton
                contrast
                loader={<SpinnerIcon scale="xlarge" />}
                loading={loading}
                onClick={() => console.log('xlarge')}
                weight="inline"
              >
                <BlobbrIcon scale="xlarge" />
              </IconButton>
            </Rhythm>
          </Flex>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

## Colored icon buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Flex direction="column">
        <ColorLooper
          group="primary"
          render={({ id }) => (
            <Flex wrap alignItems="center" direction="row" key={id}>
              <Looper
                list={[false, true]}
                render={loading => (
                  <Flex alignItems="center" direction="row" key={`${id}-${loading ? 'loading' : 'standard'}`}>
                    <Rhythm m={2}>
                      <ColoredIconButton
                        colorId={id}
                        loader={<SpinnerIcon size={20} />}
                        loading={loading}
                        onClick={() => console.log('circle')}
                        shape="circle"
                        weight="ghost"
                      >
                        <BlobbrIcon size={20} />
                      </ColoredIconButton>
                      <ColoredIconButton
                        colorId={id}
                        loader={<SpinnerIcon scale="medium" />}
                        loading={loading}
                        onClick={() => console.log('circle')}
                        shape="circle"
                      >
                        <BlobbrIcon scale="medium" />
                      </ColoredIconButton>
                      <ColoredIconButton
                        colorId={id}
                        loader={<SpinnerIcon scale="medium" />}
                        loading={loading}
                        onClick={() => console.log('square')}
                        shape="square"
                      >
                        <BlobbrIcon scale="medium" />
                      </ColoredIconButton>
                      <ColoredIconButton
                        colorId={id}
                        loader={<SpinnerIcon scale="medium" />}
                        loading={loading}
                        onClick={() => console.log('circle shaded')}
                        shape="circle"
                        weight="shaded"
                      >
                        <BlobbrIcon scale="medium" />
                      </ColoredIconButton>
                      <ColoredIconButton
                        colorId={id}
                        loader={<SpinnerIcon scale="medium" />}
                        loading={loading}
                        onClick={() => console.log('square shaded')}
                        shape="square"
                        weight="shaded"
                      >
                        <BlobbrIcon scale="medium" />
                      </ColoredIconButton>
                      <ColoredIconButton
                        colorId={id}
                        loader={<SpinnerIcon scale="medium" />}
                        loading={loading}
                        onClick={() => console.log('circle outline')}
                        shape="circle"
                        weight="outlined"
                      >
                        <BlobbrIcon scale="medium" />
                      </ColoredIconButton>
                      <ColoredIconButton
                        colorId={id}
                        loader={<SpinnerIcon scale="medium" />}
                        loading={loading}
                        onClick={() => console.log('square outline')}
                        shape="square"
                        weight="outlined"
                      >
                        <BlobbrIcon scale="medium" />
                      </ColoredIconButton>
                      <ColoredIconButton
                        colorId={id}
                        loader={<SpinnerIcon scale="xlarge" />}
                        loading={loading}
                        onClick={() => console.log('xlarge')}
                        weight="inline"
                      >
                        <BlobbrIcon scale="xlarge" />
                      </ColoredIconButton>
                    </Rhythm>
                  </Flex>
                )}
              />
            </Flex>
          )}
          themeId={themeId}
        />
      </Flex>
    )}
  </ThemeWrapper>
</Playground>

### Styled icon buttons

<Playground>
  {() => {
    const styles = {
      primaryColor: '#556270',
      inverseColor: '#fff',
      hoveredPrimaryColor: '#454f58',
      activePrimaryColor: '#798796',
    };
    return (
      <Flex wrap alignItems="center" direction="row">
        <Looper
          list={[false, true]}
          render={loading => (
            <Flex alignItems="center" direction="row" key={`${loading ? 'loading' : 'standard'}`}>
              <Rhythm m={2}>
                <StyledIconButton
                  loader={<SpinnerIcon size={20} />}
                  loading={loading}
                  onClick={() => console.log('circle')}
                  shape="circle"
                  weight="ghost"
                  {...styles}
                >
                  <BlobbrIcon size={20} />
                </StyledIconButton>
                <StyledIconButton
                  loader={<SpinnerIcon scale="medium" />}
                  loading={loading}
                  onClick={() => console.log('circle')}
                  shape="circle"
                  {...styles}
                >
                  <BlobbrIcon scale="medium" />
                </StyledIconButton>
                <StyledIconButton
                  loader={<SpinnerIcon scale="medium" />}
                  loading={loading}
                  onClick={() => console.log('square')}
                  shape="square"
                  {...styles}
                >
                  <BlobbrIcon scale="medium" />
                </StyledIconButton>
                <StyledIconButton
                  loader={<SpinnerIcon scale="medium" />}
                  loading={loading}
                  onClick={() => console.log('circle shaded')}
                  shape="circle"
                  weight="shaded"
                  {...styles}
                >
                  <BlobbrIcon scale="medium" />
                </StyledIconButton>
                <StyledIconButton
                  loader={<SpinnerIcon scale="medium" />}
                  loading={loading}
                  onClick={() => console.log('square shaded')}
                  shape="square"
                  weight="shaded"
                  {...styles}
                >
                  <BlobbrIcon scale="medium" />
                </StyledIconButton>
                <StyledIconButton
                  loader={<SpinnerIcon scale="medium" />}
                  loading={loading}
                  onClick={() => console.log('circle outline')}
                  shape="circle"
                  weight="outlined"
                  {...styles}
                >
                  <BlobbrIcon scale="medium" />
                </StyledIconButton>
                <StyledIconButton
                  loader={<SpinnerIcon scale="medium" />}
                  loading={loading}
                  onClick={() => console.log('square outline')}
                  shape="square"
                  weight="outlined"
                  {...styles}
                >
                  <BlobbrIcon scale="medium" />
                </StyledIconButton>
                <StyledIconButton
                  loader={<SpinnerIcon scale="xlarge" />}
                  loading={loading}
                  onClick={() => console.log('xlarge')}
                  weight="inline"
                  {...styles}
                >
                  <BlobbrIcon scale="xlarge" />
                </StyledIconButton>
              </Rhythm>
            </Flex>
          )}
        />
      </Flex>
    );
  }}
</Playground>

### Link icon buttons

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <Flex alignItems="center" direction="row">
      <Rhythm m={2}>
        <IconButton color="primary" href="#linked" onClick={() => console.log('circle')} shape="circle" weight="ghost">
          <BlobbrIcon size={20} />
        </IconButton>
        <IconButton color="primary" href="#linked" onClick={() => console.log('circle')} shape="circle">
          <BlobbrIcon scale="medium" />
        </IconButton>
        <IconButton color="primary" href="#linked" onClick={() => console.log('square')} shape="square">
          <BlobbrIcon scale="medium" />
        </IconButton>
        <IconButton
          disabled
          loading
          color="primary"
          href="#linked"
          loader={<SpinnerIcon scale="medium" />}
          onClick={() => console.log('square')}
          shape="square"
        >
          <BlobbrIcon scale="medium" />
        </IconButton>
        <IconButton color="primary" href="#linked" onClick={() => console.log('default')}>
          <BlobbrIcon scale="xlarge" />
        </IconButton>
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

### Functional icon buttons

<Playground>
  <ThemeWrapper>
    <IconButton color="primary" onClick={() => console.log('default')}>
      {({ scale = ['medium', 'large', 'xlarge', '2xlarge'][Math.floor(Math.random() * 4)] }) => (
        <BlobbrIcon scale={scale} />
      )}
    </IconButton>
  </ThemeWrapper>
</Playground>

## \[props\]

### IconButton

<Props of={IconButton} />

### ColoredIconButton

<Props of={ColoredIconButton} />

### StyledIconButton

<Props of={StyledIconButton} />
