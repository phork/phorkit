---
name: IconTextButton
menu: Input
route: /components/IconTextButton
---

import { Playground, Props } from 'docz';
import { ucfirst } from 'utils';
import { SpinnerIcon } from 'icons/SpinnerIcon';
import { TimesIcon } from 'icons/TimesIcon';
import { BlobbrIcon } from 'icons/internal/BlobbrIcon';
import { Flex } from 'components/Flex';
import { Rhythm } from 'components/Rhythm';
import { ColorLooper } from 'docs/helpers/ColorLooper';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { IconTextButton, ColoredIconTextButton, StyledIconTextButton } from '../index';

<PageTitle src="components/Button" title="IconTextButton" />

## Size, shape and weight

<Playground>
  <ThemeWrapper>
    <Looper
      list={['small', 'medium', 'large']}
      render={size => (
        <Rhythm grouped key={size} py={2}>
          <Looper
            list={['pill', 'brick']}
            render={shape => (
              <Rhythm grouped key={`${size}-${shape}`} py={2}>
                <Looper
                  list={['standard', 'loaderReplaceIcon', 'loading']}
                  render={loading => (
                    <Flex wrap direction="row" key={`${size}-${shape}-${loading}`}>
                      <Looper
                        list={['solid', 'shaded', 'outlined', 'ghost']}
                        render={weight => (
                          <Rhythm key={`${size}-${shape}-${loading}-${weight}`} m={2} mb={0}>
                            <IconTextButton
                              color="primary"
                              icon={<BlobbrIcon scale={size} />}
                              loader={<SpinnerIcon scale={size} />}
                              loaderReplaceIcon={loading === 'loaderReplaceIcon'}
                              loading={['loading', 'loaderReplaceIcon'].includes(loading)}
                              onClick={() => console.log('click')}
                              shape={shape}
                              size={size}
                              weight={weight}
                            >
                              {ucfirst(size)} {shape} {weight}
                            </IconTextButton>
                          </Rhythm>
                        )}
                      />
                    </Flex>
                  )}
                />
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

### Inline buttons

<Playground>
  <ThemeWrapper style={{ fontSize: 14 }}>
    <Looper
      list={['standard', 'loaderReplaceIcon', 'loading']}
      render={loading => (
        <Rhythm grouped key={loading} mb={2}>
          <Rhythm m={2}>
            <IconTextButton
              color="primary"
              icon={<BlobbrIcon scale="small" />}
              loader={<SpinnerIcon scale="small" />}
              loaderReplaceIcon={loading === 'loaderReplaceIcon'}
              loading={['loading', 'loaderReplaceIcon'].includes(loading)}
              onClick={() => console.log('small')}
              size="small"
              weight="inline"
            >
              Small inline
            </IconTextButton>
            <IconTextButton
              color="primary"
              icon={<BlobbrIcon scale="medium" />}
              loader={<SpinnerIcon scale="medium" />}
              loading={loading}
              onClick={() => console.log('medium')}
              size="medium"
              weight="inline"
            >
              Medium inline
            </IconTextButton>
            <IconTextButton
              color="primary"
              icon={<BlobbrIcon scale="large" />}
              loader={<SpinnerIcon scale="large" />}
              loaderReplaceIcon={loading === 'loaderReplaceIcon'}
              loading={['loading', 'loaderReplaceIcon'].includes(loading)}
              onClick={() => console.log('large')}
              size="large"
              weight="inline"
            >
              Large inline
            </IconTextButton>
            <IconTextButton
              color="primary"
              icon={<BlobbrIcon scale="large" />}
              loader={<SpinnerIcon scale="large" />}
              loaderReplaceIcon={loading === 'loaderReplaceIcon'}
              loading={['loading', 'loaderReplaceIcon'].includes(loading)}
              onClick={() => console.log('relative')}
              size="relative"
              weight="inline"
            >
              Relative inline
            </IconTextButton>
          </Rhythm>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Reversed

<Playground>
  <ThemeWrapper>
    <Looper
      list={['pill', 'brick']}
      render={shape => (
        <Rhythm grouped key={shape} py={2}>
          <Looper
            list={['standard', 'loaderReplaceIcon', 'loading']}
            render={loading => (
              <Flex wrap direction="row" key={`${shape}-${loading}`}>
                <Looper
                  list={['solid', 'shaded', 'outlined', 'ghost']}
                  render={weight => (
                    <Rhythm key={`${shape}-${loading}-${weight}`} m={2} mb={0}>
                      <IconTextButton
                        reverse
                        color="primary"
                        icon={<TimesIcon scale="xsmall" />}
                        loader={<SpinnerIcon scale="medium" />}
                        loaderReplaceIcon={loading === 'loaderReplaceIcon'}
                        loading={['loading', 'loaderReplaceIcon'].includes(loading)}
                        onClick={() => console.log('click')}
                        shape={shape}
                        size="medium"
                        weight={weight}
                      >
                        Reversed {shape} {weight}
                      </IconTextButton>
                    </Rhythm>
                  )}
                />
              </Flex>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

### Inline buttons

<Playground>
  <ThemeWrapper style={{ fontSize: 14 }}>
    <Looper
      list={['standard', 'loaderReplaceIcon', 'loading']}
      render={loading => (
        <Rhythm grouped key={`${loading}`} mb={2}>
          <Rhythm m={2}>
            <IconTextButton
              reverse
              color="primary"
              icon={<TimesIcon scale="xsmall" />}
              loader={<SpinnerIcon scale="small" />}
              loaderReplaceIcon={loading === 'loaderReplaceIcon'}
              loading={['loading', 'loaderReplaceIcon'].includes(loading)}
              onClick={() => console.log('small')}
              size="small"
              weight="inline"
            >
              Small inline
            </IconTextButton>
            <IconTextButton
              reverse
              color="primary"
              icon={<TimesIcon scale="small" />}
              loader={<SpinnerIcon scale="medium" />}
              loaderReplaceIcon={loading === 'loaderReplaceIcon'}
              loading={['loading', 'loaderReplaceIcon'].includes(loading)}
              onClick={() => console.log('medium')}
              size="medium"
              weight="inline"
            >
              Medium inline
            </IconTextButton>
            <IconTextButton
              reverse
              color="primary"
              icon={<TimesIcon scale="small" />}
              loader={<SpinnerIcon scale="medium" />}
              loaderReplaceIcon={loading === 'loaderReplaceIcon'}
              loading={['loading', 'loaderReplaceIcon'].includes(loading)}
              onClick={() => console.log('large')}
              size="large"
              weight="inline"
            >
              Large inline
            </IconTextButton>
            <IconTextButton
              reverse
              color="primary"
              icon={<TimesIcon scale="small" />}
              loader={<SpinnerIcon scale="large" />}
              loaderReplaceIcon={loading === 'loaderReplaceIcon'}
              loading={['loading', 'loaderReplaceIcon'].includes(loading)}
              onClick={() => console.log('relative')}
              size="relative"
              weight="inline"
            >
              Relative inline
            </IconTextButton>
          </Rhythm>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Button colors

<Playground>
  <ThemeWrapper>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <Rhythm grouped key={weight} m={2}>
          <Looper
            list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
            render={color => (
              <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                <IconTextButton
                  color={color}
                  icon={<BlobbrIcon scale="medium" />}
                  onClick={() => console.log(color)}
                  weight={weight}
                >
                  {ucfirst(color)}
                </IconTextButton>
              </Rhythm>
            )}
          />
          <br />
          <Looper
            list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
            render={color => (
              <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                <IconTextButton
                  loading
                  color={color}
                  icon={<BlobbrIcon scale="medium" />}
                  loader={<SpinnerIcon scale="medium" />}
                  onClick={() => console.log(color)}
                  weight={weight}
                >
                  {ucfirst(color)}
                </IconTextButton>
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

<div style="height: 50px" />

<Playground>
  <ThemeWrapper>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <Rhythm grouped key={weight} m={2}>
          <Looper
            list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
            render={color => (
              <Rhythm key={`buttonColor-${weight}-${color}`} mx={2} my={1}>
                <IconTextButton
                  color={color}
                  icon={<BlobbrIcon scale="medium" />}
                  onClick={() => console.log(color)}
                  shape="brick"
                  weight={weight}
                >
                  {ucfirst(color)}
                </IconTextButton>
              </Rhythm>
            )}
          />
          <br />
          <Looper
            list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
            render={color => (
              <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                <IconTextButton
                  loading
                  color={color}
                  icon={<BlobbrIcon scale="medium" />}
                  loader={<SpinnerIcon scale="medium" />}
                  onClick={() => console.log(color)}
                  shape="brick"
                  weight={weight}
                >
                  {ucfirst(color)}
                </IconTextButton>
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

<div style={{ height: 50 }} />

<Playground>
  <ThemeWrapper>
    <Looper
      list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
      render={color => (
        <Rhythm key={color} m={2}>
          <IconTextButton
            color={color}
            icon={<BlobbrIcon scale="medium" />}
            onClick={() => console.log(color)}
            shape="brick"
            weight="inline"
          >
            {ucfirst(color)}
          </IconTextButton>
        </Rhythm>
      )}
    />
    <br />
    <Looper
      list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
      render={color => (
        <Rhythm key={color} m={2}>
          <IconTextButton
            loading
            color={color}
            icon={<BlobbrIcon scale="medium" />}
            loader={<SpinnerIcon scale="medium" />}
            onClick={() => console.log(color)}
            shape="brick"
            weight="inline"
          >
            {ucfirst(color)}
          </IconTextButton>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Variations

### Contrast buttons

<Playground>
  <ThemeWrapper contrast>
    <Rhythm m={2}>
      <IconTextButton contrast icon={<BlobbrIcon scale="medium" />} weight="solid">
        Pill solid
      </IconTextButton>
      <IconTextButton contrast icon={<BlobbrIcon scale="medium" />} weight="shaded">
        Pill shaded
      </IconTextButton>
      <IconTextButton contrast icon={<BlobbrIcon scale="medium" />} weight="outlined">
        Pill outlined
      </IconTextButton>
      <IconTextButton contrast icon={<BlobbrIcon scale="medium" />} weight="ghost">
        Pill ghost
      </IconTextButton>
      <IconTextButton contrast icon={<BlobbrIcon scale="medium" />} weight="inline">
        Pill inline
      </IconTextButton>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <IconTextButton contrast loading icon={<BlobbrIcon scale="medium" />} loader={<SpinnerIcon scale="medium" />}>
        Pill solid
      </IconTextButton>
      <IconTextButton
        contrast
        loading
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        weight="shaded"
      >
        Pill shaded
      </IconTextButton>
      <IconTextButton
        contrast
        loading
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        weight="outlined"
      >
        Pill outlined
      </IconTextButton>
      <IconTextButton
        contrast
        loading
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        weight="ghost"
      >
        Pill ghost
      </IconTextButton>
      <IconTextButton
        contrast
        loading
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        weight="inline"
      >
        Pill inline
      </IconTextButton>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Colored buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Flex wrap direction="column">
        <ColorLooper
          group="primary"
          render={({ id }) => (
            <Rhythm key={id} my={2}>
              <Flex direction="column">
                <Flex wrap durection="row">
                  <Rhythm mx={2} my={1}>
                    <ColoredIconTextButton colorId={id} icon={<BlobbrIcon scale="medium" />} weight="solid">
                      {`Color ${id}`}
                    </ColoredIconTextButton>
                    <ColoredIconTextButton colorId={id} icon={<BlobbrIcon scale="medium" />} weight="shaded">
                      {`Color ${id}`}
                    </ColoredIconTextButton>
                    <ColoredIconTextButton colorId={id} icon={<BlobbrIcon scale="medium" />} weight="outlined">
                      {`Color ${id}`}
                    </ColoredIconTextButton>
                    <ColoredIconTextButton colorId={id} icon={<BlobbrIcon scale="medium" />} weight="ghost">
                      {`Color ${id}`}
                    </ColoredIconTextButton>
                    <ColoredIconTextButton colorId={id} icon={<BlobbrIcon scale="medium" />} weight="inline">
                      {`Color ${id}`}
                    </ColoredIconTextButton>
                  </Rhythm>
                </Flex>
                <Flex wrap durection="row">
                  <Rhythm mx={2} my={1}>
                    <ColoredIconTextButton
                      loading
                      colorId={id}
                      icon={<BlobbrIcon scale="medium" />}
                      loader={<SpinnerIcon scale="medium" />}
                      weight="solid"
                    >
                      {`Color ${id}`}
                    </ColoredIconTextButton>
                    <ColoredIconTextButton
                      loading
                      colorId={id}
                      icon={<BlobbrIcon scale="medium" />}
                      loader={<SpinnerIcon scale="medium" />}
                      weight="shaded"
                    >
                      {`Color ${id}`}
                    </ColoredIconTextButton>
                    <ColoredIconTextButton
                      loading
                      colorId={id}
                      icon={<BlobbrIcon scale="medium" />}
                      loader={<SpinnerIcon scale="medium" />}
                      weight="outlined"
                    >
                      {`Color ${id}`}
                    </ColoredIconTextButton>
                    <ColoredIconTextButton
                      loading
                      colorId={id}
                      icon={<BlobbrIcon scale="medium" />}
                      loader={<SpinnerIcon scale="medium" />}
                      weight="ghost"
                    >
                      {`Color ${id}`}
                    </ColoredIconTextButton>
                    <ColoredIconTextButton
                      loading
                      colorId={id}
                      icon={<BlobbrIcon scale="medium" />}
                      loader={<SpinnerIcon scale="medium" />}
                      weight="inline"
                    >
                      {`Color ${id}`}
                    </ColoredIconTextButton>
                  </Rhythm>
                </Flex>
              </Flex>
            </Rhythm>
          )}
          themeId={themeId}
        />
      </Flex>
    )}
  </ThemeWrapper>
</Playground>

### Styled buttons

<Playground>
  {() => {
    const styles = {
      primaryColor: '#556270',
      inverseColor: '#fff',
      hoveredPrimaryColor: '#454f58',
      activePrimaryColor: '#798796',
    };
    return (
      <React.Fragment>
        <Rhythm m={2}>
          <StyledIconTextButton icon={<BlobbrIcon scale="medium" />} {...styles}>
            Styled solid
          </StyledIconTextButton>
          <StyledIconTextButton icon={<BlobbrIcon scale="medium" />} weight="shaded" {...styles}>
            Styled shaded
          </StyledIconTextButton>
          <StyledIconTextButton icon={<BlobbrIcon scale="medium" />} weight="outlined" {...styles}>
            Styled outlined
          </StyledIconTextButton>
          <StyledIconTextButton icon={<BlobbrIcon scale="medium" />} weight="ghost" {...styles}>
            Styled ghost
          </StyledIconTextButton>
          <StyledIconTextButton icon={<BlobbrIcon scale="medium" />} weight="inline" {...styles}>
            Styled inline
          </StyledIconTextButton>
        </Rhythm>
        <br />
        <Rhythm m={2}>
          <StyledIconTextButton
            loading
            icon={<BlobbrIcon scale="medium" />}
            loader={<SpinnerIcon scale="medium" />}
            {...styles}
          >
            Styled solid
          </StyledIconTextButton>
          <StyledIconTextButton
            loading
            icon={<BlobbrIcon scale="medium" />}
            loader={<SpinnerIcon scale="medium" />}
            weight="shaded"
            {...styles}
          >
            Styled shaded
          </StyledIconTextButton>
          <StyledIconTextButton
            loading
            icon={<BlobbrIcon scale="medium" />}
            loader={<SpinnerIcon scale="medium" />}
            weight="outlined"
            {...styles}
          >
            Styled outlined
          </StyledIconTextButton>
          <StyledIconTextButton
            loading
            icon={<BlobbrIcon scale="medium" />}
            loader={<SpinnerIcon scale="medium" />}
            weight="ghost"
            {...styles}
          >
            Styled ghost
          </StyledIconTextButton>
          <StyledIconTextButton
            loading
            icon={<BlobbrIcon scale="medium" />}
            loader={<SpinnerIcon scale="medium" />}
            weight="inline"
            {...styles}
          >
            Styled inline
          </StyledIconTextButton>
        </Rhythm>
      </React.Fragment>
    );
  }}
</Playground>

### Linked buttons

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <IconTextButton
        color="primary"
        href="#linked"
        icon={<BlobbrIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
      >
        Linked solid
      </IconTextButton>
      <IconTextButton
        color="primary"
        href="#linked"
        icon={<BlobbrIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
        weight="shaded"
      >
        Linked shaded
      </IconTextButton>
      <IconTextButton
        color="primary"
        href="#linked"
        icon={<BlobbrIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
        weight="outlined"
      >
        Linked outlined
      </IconTextButton>
      <IconTextButton
        color="primary"
        href="#linked"
        icon={<BlobbrIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
        weight="ghost"
      >
        Linked ghost
      </IconTextButton>
      <IconTextButton
        color="primary"
        href="#linked"
        icon={<BlobbrIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
        weight="inline"
      >
        Linked inline
      </IconTextButton>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <IconTextButton
        loading
        color="primary"
        href="#linked"
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
      >
        Linked solid
      </IconTextButton>
      <IconTextButton
        loading
        color="primary"
        href="#linked"
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
        weight="shaded"
      >
        Linked shaded
      </IconTextButton>
      <IconTextButton
        loading
        color="primary"
        href="#linked"
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
        weight="outlined"
      >
        Linked outlined
      </IconTextButton>
      <IconTextButton
        loading
        color="primary"
        href="#linked"
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
        weight="ghost"
      >
        Linked ghost
      </IconTextButton>
      <IconTextButton
        loading
        color="primary"
        href="#linked"
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
        weight="inline"
      >
        Linked inline
      </IconTextButton>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Non-button buttons

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <IconTextButton
        as="div"
        color="primary"
        icon={<BlobbrIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
      >
        Acts like a button
      </IconTextButton>
      <IconTextButton
        as="div"
        color="primary"
        icon={<BlobbrIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
        weight="shaded"
      >
        Acts like a button
      </IconTextButton>
      <IconTextButton
        as="div"
        color="primary"
        icon={<BlobbrIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
        weight="outlined"
      >
        Acts like a button
      </IconTextButton>
      <IconTextButton
        as="div"
        color="primary"
        icon={<BlobbrIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
        weight="ghost"
      >
        Acts like a button
      </IconTextButton>
      <IconTextButton
        as="div"
        color="primary"
        icon={<BlobbrIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
        weight="inline"
      >
        Acts like a button
      </IconTextButton>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <IconTextButton
        loading
        as="div"
        color="primary"
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
      >
        Acts like a button
      </IconTextButton>
      <IconTextButton
        loading
        as="div"
        color="primary"
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
        weight="shaded"
      >
        Acts like a button
      </IconTextButton>
      <IconTextButton
        loading
        as="div"
        color="primary"
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
        weight="outlined"
      >
        Acts like a button
      </IconTextButton>
      <IconTextButton
        loading
        as="div"
        color="primary"
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
        weight="ghost"
      >
        Acts like a button
      </IconTextButton>
      <IconTextButton
        loading
        as="div"
        color="primary"
        icon={<BlobbrIcon scale="medium" />}
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
        weight="inline"
      >
        Acts like a button
      </IconTextButton>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Sized buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Looper
        list={['pill', 'brick']}
        render={shape => (
          <div key={shape}>
            <Rhythm m={2}>
              <IconTextButton
                fullWidth
                color="primary"
                icon={<BlobbrIcon scale="medium" />}
                shape={shape}
                themeId={themeId}
              >
                Full width
              </IconTextButton>
              <IconTextButton
                color="primary"
                icon={<BlobbrIcon scale="medium" />}
                shape={shape}
                themeId={themeId}
                width={200}
              >
                Fixed width
              </IconTextButton>
            </Rhythm>
          </div>
        )}
      />
    )}
  </ThemeWrapper>
</Playground>

### Aligned buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Rhythm m={2}>
        <StyledIconTextButton
          align="left"
          color="primary"
          icon={<BlobbrIcon scale="medium" />}
          themeId={themeId}
          width={200}
        >
          Align left
        </StyledIconTextButton>
        <StyledIconTextButton color="primary" icon={<BlobbrIcon scale="medium" />} themeId={themeId} width={200}>
          Align center
        </StyledIconTextButton>
        <StyledIconTextButton
          align="right"
          color="primary"
          icon={<BlobbrIcon scale="medium" />}
          themeId={themeId}
          width={200}
        >
          Align right
        </StyledIconTextButton>
      </Rhythm>
    )}
  </ThemeWrapper>
</Playground>

### Unstyled button

<Playground>
  <ThemeWrapper>
    <IconTextButton unstyled icon={<BlobbrIcon scale="medium" />} onClick={() => console.log('unstyled')}>
      Unstyled
    </IconTextButton>
  </ThemeWrapper>
</Playground>

## \[props\]

### IconTextButton

<Props of={IconTextButton} />

### ColoredIconTextButton

<Props of={ColoredIconTextButton} />

### StyledIconTextButton

<Props of={StyledIconTextButton} />
