---
name: Button
menu: Input
route: /components/Button
---

import { ColorLooper } from 'docs/helpers/ColorLooper';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Playground, Props } from 'docz';
import { ucfirst } from 'utils';
import { SpinnerIcon } from 'icons/SpinnerIcon';
import { TimesIcon } from 'icons/TimesIcon';
import { BlobbrIcon } from 'icons/internal/BlobbrIcon';
import { Flex } from 'components/Flex';
import { Hoverable } from 'components/Hoverable';
import { Rhythm } from 'components/Rhythm';
import { Button, ColoredButton, ColoredIconTextButton, StyledButton, IconTextButton } from '../index';

<PageTitle title="Button" src="components/Button" />

## Button sizes

<Playground>
  <ThemeWrapper>
    <Looper
      list={['small', 'medium', 'large']}
      render={size => (
        <Rhythm py={1} grouped key={`buttonSize-${size}`}>
          <Rhythm m={2}>
            <Button color="primary" shape="pill" weight="filled" onClick={() => console.log('click')} size={size}>
              Primary
            </Button>
            <Button color="primary" shape="pill" weight="outline" onClick={() => console.log('click')} size={size}>
              Primary
            </Button>
            <Button
              color="primary"
              shape="pill"
              weight="outline"
              disabled
              loading
              loader={<SpinnerIcon scale={size} />}
              onClick={() => console.log('click')}
              size={size}
            >
              Primary
            </Button>
            <Button color="primary" shape="brick" weight="filled" onClick={() => console.log('click')} size={size}>
              Primary
            </Button>
            <Button color="primary" shape="brick" weight="outline" onClick={() => console.log('click')} size={size}>
              Primary
            </Button>
            <Button
              color="primary"
              shape="brick"
              weight="outline"
              disabled
              loading
              loader={<SpinnerIcon scale={size} />}
              onClick={() => console.log('click')}
              size={size}
            >
              Primary
            </Button>
            <Button color="primary" weight="text" onClick={() => console.log('click')} size={size}>
              Primary
            </Button>
            <Button
              color="primary"
              weight="text"
              loading
              disabled
              loader={<SpinnerIcon scale={size} />}
              onClick={() => console.log('click')}
              size={size}
            >
              Primary
            </Button>
          </Rhythm>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

<div style="height: 50px" />

<Playground>
  <ThemeWrapper>
    <Looper
      list={['small', 'medium', 'large']}
      render={size => (
        <Rhythm py={1} grouped key={`iconButtonSize-${size}`}>
          <Rhythm m={2}>
            <IconTextButton
              color="primary"
              icon={<BlobbrIcon scale={size} />}
              shape="pill"
              size={size}
              text="Primary"
            />
            <IconTextButton
              color="primary"
              icon={<BlobbrIcon scale={size} />}
              shape="pill"
              size={size}
              text="Primary"
              weight="outline"
            />
            <IconTextButton
              color="primary"
              disabled
              icon={<BlobbrIcon scale={size} />}
              loader={<SpinnerIcon scale={size} />}
              loading
              shape="pill"
              size={size}
              text="Primary"
              weight="outline"
            />
            <IconTextButton
              color="primary"
              icon={<BlobbrIcon scale={size} />}
              shape="brick"
              size={size}
              text="Primary"
            />
            <IconTextButton
              color="primary"
              disabled
              icon={<BlobbrIcon scale={size} />}
              loader={<SpinnerIcon scale={size} />}
              loading
              shape="brick"
              size={size}
              text="Primary"
              weight="outline"
            />
            <IconTextButton
              color="primary"
              icon={<BlobbrIcon scale={size} />}
              shape="brick"
              size={size}
              text="Primary"
              weight="outline"
            />
            <IconTextButton
              color="primary"
              icon={<BlobbrIcon scale={size} />}
              size={size}
              text="Primary"
              weight="text"
            />
            <IconTextButton
              color="primary"
              disabled
              icon={<BlobbrIcon scale={size} />}
              loader={<SpinnerIcon scale={size} />}
              loading
              size={size}
              text="Primary"
              weight="text"
            />
          </Rhythm>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Pill buttons

<Playground>
  <ThemeWrapper>
    <Looper
      list={['filled', 'outline']}
      render={weight => (
        <div key={weight || 'undefined'}>
          <Rhythm m={2}>
            <Button color="primary" weight={weight} onClick={() => console.log('primary')}>
              Primary
            </Button>
            <Button color="success" weight={weight} onClick={() => console.log('success')}>
              Success
            </Button>
            <Button color="warning" weight={weight} onClick={() => console.log('warning')}>
              Warning
            </Button>
            <Button color="danger" weight={weight} onClick={() => console.log('danger')}>
              Danger
            </Button>
            <Button color="neutral" weight={weight} onClick={() => console.log('neutral')}>
              Neutral
            </Button>
            <Button color="black" weight={weight} onClick={() => console.log('black')}>
              Black
            </Button>
            <Button color="white" weight={weight} onClick={() => console.log('white')}>
              White
            </Button>
          </Rhythm>
          <br />
          <Rhythm m={2}>
            <Button
              color="primary"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('primary')}
              weight={weight}
            >
              Primary
            </Button>
            <Button
              color="success"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('success')}
              weight={weight}
            >
              Success
            </Button>
            <Button
              color="warning"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('warning')}
              weight={weight}
            >
              Warning
            </Button>
            <Button
              color="danger"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('danger')}
              weight={weight}
            >
              Danger
            </Button>
            <Button
              color="neutral"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('neutral')}
              weight={weight}
            >
              Neutral
            </Button>
            <Button
              color="black"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('black')}
              weight={weight}
            >
              Black
            </Button>
            <Button
              color="white"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('white')}
              weight={weight}
            >
              White
            </Button>
          </Rhythm>
        </div>
      )}
    />
  </ThemeWrapper>
</Playground>

## Brick buttons

<Playground>
  <ThemeWrapper>
    <Looper
      list={[undefined, 'outline']}
      render={weight => (
        <div key={weight || 'undefined'}>
          <Rhythm m={2}>
            <Button color="primary" shape="brick" weight={weight} onClick={() => console.log('primary')}>
              Primary
            </Button>
            <Button color="success" shape="brick" weight={weight} onClick={() => console.log('success')}>
              Success
            </Button>
            <Button color="warning" shape="brick" weight={weight} onClick={() => console.log('warning')}>
              Warning
            </Button>
            <Button color="danger" shape="brick" weight={weight} onClick={() => console.log('danger')}>
              Danger
            </Button>
            <Button color="neutral" shape="brick" weight={weight} onClick={() => console.log('neutral')}>
              Neutral
            </Button>
            <Button color="black" shape="brick" weight={weight} onClick={() => console.log('black')}>
              Black
            </Button>
            <Button color="white" shape="brick" weight={weight} onClick={() => console.log('white')}>
              White
            </Button>
          </Rhythm>
          <br />
          <Rhythm m={2}>
            <Button
              color="primary"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('primary')}
              shape="brick"
              weight={weight}
            >
              Primary
            </Button>
            <Button
              color="success"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('success')}
              shape="brick"
              weight={weight}
            >
              Success
            </Button>
            <Button
              color="warning"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('warning')}
              shape="brick"
              weight={weight}
            >
              Warning
            </Button>
            <Button
              color="danger"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('danger')}
              shape="brick"
              weight={weight}
            >
              Danger
            </Button>
            <Button
              color="neutral"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('neutral')}
              shape="brick"
              weight={weight}
            >
              Neutral
            </Button>
            <Button
              color="black"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('black')}
              shape="brick"
              weight={weight}
            >
              Black
            </Button>
            <Button
              color="white"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('white')}
              shape="brick"
              weight={weight}
            >
              White
            </Button>
          </Rhythm>
        </div>
      )}
    />
  </ThemeWrapper>
</Playground>

## Text buttons

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <Button weight="text" color="primary" onClick={() => console.log('primary')}>
        Primary
      </Button>
      <Button weight="text" color="success" onClick={() => console.log('success')}>
        Success
      </Button>
      <Button weight="text" color="warning" onClick={() => console.log('warning')}>
        Warning
      </Button>
      <Button weight="text" color="danger" onClick={() => console.log('danger')}>
        Danger
      </Button>
      <Button weight="text" color="neutral" onClick={() => console.log('neutral')}>
        Neutral
      </Button>
      <Button weight="text" color="black" onClick={() => console.log('black')}>
        Black
      </Button>
      <Button weight="text" color="white" onClick={() => console.log('white')}>
        White
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('primary')}
        weight="text"
      >
        Primary
      </Button>
      <Button
        color="success"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('success')}
        weight="text"
      >
        Success
      </Button>
      <Button
        color="warning"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('warning')}
        weight="text"
      >
        Warning
      </Button>
      <Button
        color="danger"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('danger')}
        weight="text"
      >
        Danger
      </Button>
      <Button
        color="neutral"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('neutral')}
        weight="text"
      >
        Neutral
      </Button>
      <Button
        color="black"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('black')}
        weight="text"
      >
        Black
      </Button>
      <Button
        color="white"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('white')}
        weight="text"
      >
        White
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

## Variations

### IconText buttons

<Playground>
  <ThemeWrapper>
    <Looper
      list={[undefined, 'outline']}
      render={weight => (
        <Rhythm mb={2} key={`iconText-${weight}`} grouped>
          <Looper
            list={['pill', 'brick']}
            render={shape => (
              <div key={`iconText-${weight}-${shape}`}>
                <Looper
                  list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
                  render={color => (
                    <Rhythm m={2} key={`iconText-${weight}-${shape}-${color}`}>
                      <IconTextButton
                        color={color}
                        icon={<BlobbrIcon scale="medium" />}
                        shape={shape}
                        text={ucfirst(color)}
                        weight={weight}
                      />
                    </Rhythm>
                  )}
                />
                <br />
              </div>
            )}
          />
        </Rhythm>
      )}
    />
    <Rhythm mt={4} grouped>
      <Looper
        list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
        render={color => (
          <Rhythm mx={4} key={`text-${color}`}>
            <IconTextButton color={color} icon={<BlobbrIcon scale="medium" />} text={ucfirst(color)} weight="text" />
          </Rhythm>
        )}
      />
    </Rhythm>
  </ThemeWrapper>
</Playground>

<div style="height: 50px" />

<Playground>
  <ThemeWrapper>
    <Looper
      list={[undefined, 'outline']}
      render={weight => (
        <Rhythm mb={2} key={`iconText-${weight}`} grouped>
          <Looper
            list={['pill', 'brick']}
            render={shape => (
              <div key={`iconText-${weight}-${shape}`}>
                <Looper
                  list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
                  render={color => (
                    <Rhythm m={2} key={`iconTextReverse-${weight}-${shape}-${color}`}>
                      <IconTextButton
                        color={color}
                        icon={<TimesIcon scale="xsmall" />}
                        reverse
                        shape={shape}
                        text={ucfirst(color)}
                        weight={weight}
                      />
                    </Rhythm>
                  )}
                />
                <br />
              </div>
            )}
          />
        </Rhythm>
      )}
    />
    <Rhythm mt={4} grouped>
      <Looper
        list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
        render={color => (
          <Rhythm mx={4} key={`text-${color}`}>
            <IconTextButton
              color={color}
              icon={<TimesIcon scale="xsmall" />}
              reverse
              text={ucfirst(color)}
              weight="text"
            />
          </Rhythm>
        )}
      />
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Neutral and primary buttons

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <Button color="neutralAndPrimary">Contrast</Button>
      <Button color="neutralAndPrimary" weight="outline">
        Contrast
      </Button>
      <Button color="neutralAndPrimary" weight="text">
        Contrast
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Contrast buttons

<Playground>
  <ThemeWrapper contrast>
    <Rhythm m={2}>
      <Button contrast>Contrast</Button>
      <Button contrast weight="outline">
        Contrast
      </Button>
      <Button contrast weight="text">
        Contrast
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button contrast loading loader={<SpinnerIcon scale="medium" />}>
        Contrast
      </Button>
      <Button contrast weight="outline" loading loader={<SpinnerIcon scale="medium" />}>
        Contrast
      </Button>
      <Button contrast weight="text" loading loader={<SpinnerIcon scale="medium" />}>
        Contrast
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

## Colored buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Flex direction="column">
        <ColorLooper
          themeId={themeId}
          group="primary"
          render={({ id }) => (
            <Flex direction="row" key={`coloredButton-${id}`}>
              <Rhythm key={`colorButton-${id}`} mb={1} mr={4}>
                <ColoredButton shape="pill" weight="filled" colorId={id}>
                  {`Color button ${id}`}
                </ColoredButton>
                <ColoredButton shape="brick" weight="filled" colorId={id}>
                  {`Color button ${id}`}
                </ColoredButton>
                <ColoredIconTextButton
                  colorId={id}
                  icon={<BlobbrIcon scale="medium" />}
                  shape="brick"
                  text={`Color button ${id}`}
                  weight="filled"
                />
                <ColoredButton shape="pill" weight="outline" colorId={id}>
                  {`Color button ${id}`}
                </ColoredButton>
                <ColoredButton shape="brick" weight="outline" colorId={id}>
                  {`Color button ${id}`}
                </ColoredButton>
                <ColoredIconTextButton
                  colorId={id}
                  icon={<BlobbrIcon scale="medium" />}
                  shape="brick"
                  text={`Color button ${id}`}
                  weight="outline"
                />
              </Rhythm>
              <br />
            </Flex>
          )}
        />
      </Flex>
    )}
  </ThemeWrapper>
</Playground>

### Linked buttons

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <Button href="#linked" onClick={() => console.log('linked clicked')} color="primary">
        Linked
      </Button>
      <Button href="#linked" onClick={() => console.log('linked clicked')} weight="outline" color="primary">
        Linked
      </Button>
      <Button href="#linked" onClick={() => console.log('linked clicked')} weight="text" color="primary">
        Linked
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('linked clicked')}
      >
        Linked
      </Button>
      <Button
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('linked clicked')}
        weight="outline"
      >
        Linked
      </Button>
      <Button
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('linked clicked')}
        weight="text"
      >
        Linked
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Non-button buttons

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <Button as="div" onClick={() => console.log('div clicked')} color="primary">
        Acts like a button
      </Button>
      <Button as="div" onClick={() => console.log('div clicked')} weight="outline" color="primary">
        Acts like a button
      </Button>
      <Button as="div" onClick={() => console.log('div clicked')} weight="text" color="primary">
        Acts like a button
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('div clicked')}
      >
        Acts like a button
      </Button>
      <Button
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('div clicked')}
        weight="outline"
      >
        Acts like a button
      </Button>
      <Button
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('div clicked')}
        weight="text"
      >
        Acts like a button
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Sized buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Looper
        list={['pill', 'brick']}
        render={shape => (
          <div key={shape}>
            <Rhythm m={2}>
              <StyledButton shape={shape} themeId={themeId} color="primary" fullWidth>
                Full width
              </StyledButton>
              <StyledButton shape={shape} themeId={themeId} color="primary" width={400}>
                Fixed width
              </StyledButton>
            </Rhythm>
          </div>
        )}
      />
    )}
  </ThemeWrapper>
</Playground>

### Aligned buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Rhythm m={2}>
        <StyledButton themeId={themeId} color="primary" width={200} align="left">
          Align left
        </StyledButton>
        <StyledButton themeId={themeId} color="primary" width={200}>
          Align center
        </StyledButton>
        <StyledButton themeId={themeId} color="primary" width={200} align="right">
          Align right
        </StyledButton>
      </Rhythm>
    )}
  </ThemeWrapper>
</Playground>

### Unstyled button

<Playground>
  <ThemeWrapper>
    <Button unstyled onClick={() => console.log('unstyled')}>
      Unstyled
    </Button>
  </ThemeWrapper>
</Playground>

## \[props\]

### Button

<Props of={Button} />

### ColoredButton

<Props of={ColoredButton} />

### IconTextButton

<Props of={IconTextButton} />

### ColoredIconTextButton

<Props of={ColoredIconTextButton} />
