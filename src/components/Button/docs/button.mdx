---
name: Button
menu: Input
route: /components/Button
---

import { Playground, Props } from 'docz';
import { ucfirst } from 'utils';
import { SpinnerIcon } from 'icons/SpinnerIcon';
import { TimesIcon } from 'icons/TimesIcon';
import { BlobbrIcon } from 'icons/internal/BlobbrIcon';
import { Flex } from 'components/Flex';
import { Rhythm } from 'components/Rhythm';
import { ColorLooper } from 'docs/helpers/ColorLooper';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Button, ColoredButton, StyledButton } from '../index';

<PageTitle title="Button" src="components/Button" />

## Size, shape and weight

<Playground>
  <ThemeWrapper>
    <Looper
      list={['small', 'medium', 'large']}
      render={size => (
        <Rhythm py={2} grouped key={`buttonSize-${size}`}>
          <Looper
            list={['pill', 'brick']}
            render={shape => (
              <Rhythm py={2} grouped key={`buttonSize-${size}-${shape}`}>
                <Looper
                  list={[false, true]}
                  render={loading => (
                    <Flex direction="row" key={`buttonSize-${size}-${shape}-${loading ? 'loading' : 'standard'}`} wrap>
                      <Looper
                        list={['solid', 'shaded', 'outlined', 'ghost']}
                        render={weight => (
                          <Rhythm
                            m={2}
                            mb={0}
                            key={`buttonSize-${size}-${shape}-${weight}-${loading ? 'loading' : 'standard'}`}
                          >
                            <Button
                              color="primary"
                              loader={<SpinnerIcon scale={size} />}
                              loading={loading}
                              onClick={() => console.log('click')}
                              shape={shape}
                              size={size}
                              weight={weight}
                            >
                              {ucfirst(size)} {shape} {weight}
                            </Button>
                          </Rhythm>
                        )}
                      />
                    </Flex>
                  )}
                />
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

### Inline buttons

<Playground>
  <ThemeWrapper style={{ fontSize: 14 }}>
    <Looper
      list={[false, true]}
      render={loading => (
        <Rhythm mb={2} key={`inlineButton-${loading ? 'loading' : 'standard'}`} grouped>
          <Rhythm m={2}>
            <Button
              color="primary"
              size="small"
              weight="inline"
              loader={<SpinnerIcon scale="small" />}
              loading={loading}
              onClick={() => console.log('small')}
            >
              Small inline
            </Button>
            <Button
              color="primary"
              size="medium"
              weight="inline"
              loader={<SpinnerIcon scale="medium" />}
              loading={loading}
              onClick={() => console.log('medium')}
            >
              Medium inline
            </Button>
            <Button
              color="primary"
              size="large"
              weight="inline"
              loader={<SpinnerIcon scale="large" />}
              loading={loading}
              onClick={() => console.log('large')}
            >
              Large inline
            </Button>
            <Button
              color="primary"
              size="relative"
              weight="inline"
              loader={<SpinnerIcon scale="large" />}
              loading={loading}
              onClick={() => console.log('relative')}
            >
              Relative inline
            </Button>
          </Rhythm>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Button colors

<Playground>
  <ThemeWrapper>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <Rhythm m={2} key={weight || 'undefined'} grouped>
          <Looper
            list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
            render={color => (
              <Rhythm mx={2} my={1} key={`buttonColor-${weight}-${color}`}>
                <Button color={color} weight={weight} onClick={() => console.log(color)}>
                  {ucfirst(color)}
                </Button>
              </Rhythm>
            )}
          />
          <br />
          <Looper
            list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
            render={color => (
              <Rhythm mx={2} my={1} key={`buttonColorLoading-${weight}-${color}`}>
                <Button
                  color={color}
                  disabled
                  loader={<SpinnerIcon scale="medium" />}
                  loading
                  onClick={() => console.log(color)}
                  weight={weight}
                >
                  {ucfirst(color)}
                </Button>
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

<div style="height: 50px" />

<Playground>
  <ThemeWrapper>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <Rhythm m={2} key={weight || 'undefined'} grouped>
          <Looper
            list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
            render={color => (
              <Rhythm mx={2} my={1} key={`buttonColor-${weight}-${color}`}>
                <Button color={color} weight={weight} onClick={() => console.log(color)} shape="brick">
                  {ucfirst(color)}
                </Button>
              </Rhythm>
            )}
          />
          <br />
          <Looper
            list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
            render={color => (
              <Rhythm mx={2} my={1} key={`buttonColorLoading-${weight}-${color}`}>
                <Button
                  color={color}
                  disabled
                  loader={<SpinnerIcon scale="medium" />}
                  loading
                  onClick={() => console.log(color)}
                  shape="brick"
                  weight={weight}
                >
                  {ucfirst(color)}
                </Button>
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

<div style={{ height: 50 }} />

<Playground>
  <ThemeWrapper>
    <Looper
      list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
      render={color => (
        <Rhythm m={2} key={`buttonColor-inline-${color}`}>
          <Button color={color} weight="inline" onClick={() => console.log(color)} shape="brick">
            {ucfirst(color)}
          </Button>
        </Rhythm>
      )}
    />
    <br />
    <Looper
      list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
      render={color => (
        <Rhythm m={2} key={`buttonColorLoading-inline-${color}`}>
          <Button
            color={color}
            disabled
            loader={<SpinnerIcon scale="medium" />}
            loading
            onClick={() => console.log(color)}
            shape="brick"
            weight="inline"
          >
            {ucfirst(color)}
          </Button>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Variations

### Contrast buttons

<Playground>
  <ThemeWrapper contrast>
    <Rhythm m={2}>
      <Button contrast weight="solid">
        Pill solid
      </Button>
      <Button contrast weight="shaded">
        Pill shaded
      </Button>
      <Button contrast weight="outlined">
        Pill outlined
      </Button>
      <Button contrast weight="ghost">
        Pill ghost
      </Button>
      <Button contrast weight="inline">
        Pill inline
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button contrast loading loader={<SpinnerIcon scale="medium" />}>
        Pill solid
      </Button>
      <Button contrast weight="shaded" loading loader={<SpinnerIcon scale="medium" />}>
        Pill shaded
      </Button>
      <Button contrast weight="outlined" loading loader={<SpinnerIcon scale="medium" />}>
        Pill outlined
      </Button>
      <Button contrast weight="ghost" loading loader={<SpinnerIcon scale="medium" />}>
        Pill ghost
      </Button>
      <Button contrast weight="inline" loading loader={<SpinnerIcon scale="medium" />}>
        Pill inline
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Colored buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Flex direction="column" wrap>
        <ColorLooper
          themeId={themeId}
          group="primary"
          render={({ id }) => (
            <Rhythm my={2} key={`coloredButton-${id}`}>
              <Flex direction="column">
                <Flex durection="row" wrap>
                  <Rhythm mx={2} my={1}>
                    <ColoredButton colorId={id} weight="solid">
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton colorId={id} weight="shaded">
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton colorId={id} weight="outlined">
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton colorId={id} weight="ghost">
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton colorId={id} weight="inline">
                      {`Color ${id}`}
                    </ColoredButton>
                  </Rhythm>
                </Flex>
                <Flex durection="row" wrap>
                  <Rhythm mx={2} my={1}>
                    <ColoredButton colorId={id} weight="solid" loading loader={<SpinnerIcon scale="medium" />}>
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton colorId={id} weight="shaded" loading loader={<SpinnerIcon scale="medium" />}>
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton colorId={id} weight="outlined" loading loader={<SpinnerIcon scale="medium" />}>
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton colorId={id} weight="ghost" loading loader={<SpinnerIcon scale="medium" />}>
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton colorId={id} weight="inline" loading loader={<SpinnerIcon scale="medium" />}>
                      {`Color ${id}`}
                    </ColoredButton>
                  </Rhythm>
                </Flex>
              </Flex>
            </Rhythm>
          )}
        />
      </Flex>
    )}
  </ThemeWrapper>
</Playground>

### Styled buttons

<Playground>
  {() => {
    const styles = {
      primaryColor: '#556270',
      inverseColor: '#fff',
      hoveredPrimaryColor: '#454f58',
      activePrimaryColor: '#798796',
    };
    return (
      <React.Fragment>
        <Rhythm m={2}>
          <StyledButton {...styles}>Styled solid</StyledButton>
          <StyledButton weight="shaded" {...styles}>
            Styled shaded
          </StyledButton>
          <StyledButton weight="outlined" {...styles}>
            Styled outlined
          </StyledButton>
          <StyledButton weight="ghost" {...styles}>
            Styled ghost
          </StyledButton>
          <StyledButton weight="inline" {...styles}>
            Styled inline
          </StyledButton>
        </Rhythm>
        <br />
        <Rhythm m={2}>
          <StyledButton loading loader={<SpinnerIcon scale="medium" />} {...styles}>
            Styled solid
          </StyledButton>
          <StyledButton weight="shaded" loading loader={<SpinnerIcon scale="medium" />} {...styles}>
            Styled shaded
          </StyledButton>
          <StyledButton weight="outlined" loading loader={<SpinnerIcon scale="medium" />} {...styles}>
            Styled outlined
          </StyledButton>
          <StyledButton weight="ghost" loading loader={<SpinnerIcon scale="medium" />} {...styles}>
            Styled ghost
          </StyledButton>
          <StyledButton weight="inline" loading loader={<SpinnerIcon scale="medium" />} {...styles}>
            Styled inline
          </StyledButton>
        </Rhythm>
      </React.Fragment>
    );
  }}
</Playground>

### Linked buttons

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <Button href="#linked" onClick={() => console.log('linked clicked')} color="primary">
        Linked solid
      </Button>
      <Button href="#linked" onClick={() => console.log('linked clicked')} weight="shaded" color="primary">
        Linked shaded
      </Button>
      <Button href="#linked" onClick={() => console.log('linked clicked')} weight="outlined" color="primary">
        Linked outlined
      </Button>
      <Button href="#linked" onClick={() => console.log('linked clicked')} weight="ghost" color="primary">
        Linked ghost
      </Button>
      <Button href="#linked" onClick={() => console.log('linked clicked')} weight="inline" color="primary">
        Linked inline
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('linked clicked')}
      >
        Linked solid
      </Button>
      <Button
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('linked clicked')}
        weight="shaded"
      >
        Linked shaded
      </Button>
      <Button
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('linked clicked')}
        weight="outlined"
      >
        Linked outlined
      </Button>
      <Button
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('linked clicked')}
        weight="ghost"
      >
        Linked ghost
      </Button>
      <Button
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('linked clicked')}
        weight="inline"
      >
        Linked inline
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Non-button buttons

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <Button as="div" onClick={() => console.log('div clicked')} color="primary">
        Acts like a button
      </Button>
      <Button as="div" onClick={() => console.log('div clicked')} weight="shaded" color="primary">
        Acts like a button
      </Button>
      <Button as="div" onClick={() => console.log('div clicked')} weight="outlined" color="primary">
        Acts like a button
      </Button>
      <Button as="div" onClick={() => console.log('div clicked')} weight="ghost" color="primary">
        Acts like a button
      </Button>
      <Button as="div" onClick={() => console.log('div clicked')} weight="inline" color="primary">
        Acts like a button
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('div clicked')}
      >
        Acts like a button
      </Button>
      <Button
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('div clicked')}
        weight="shaded"
      >
        Acts like a button
      </Button>
      <Button
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('div clicked')}
        weight="outlined"
      >
        Acts like a button
      </Button>
      <Button
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('div clicked')}
        weight="ghost"
      >
        Acts like a button
      </Button>
      <Button
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('div clicked')}
        weight="inline"
      >
        Acts like a button
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Sized buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Looper
        list={['pill', 'brick']}
        render={shape => (
          <div key={shape}>
            <Rhythm m={2}>
              <Button shape={shape} themeId={themeId} color="primary" fullWidth>
                Full width
              </Button>
              <Button shape={shape} themeId={themeId} color="primary" width={200}>
                Fixed width
              </Button>
            </Rhythm>
          </div>
        )}
      />
    )}
  </ThemeWrapper>
</Playground>

### Aligned buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Rhythm m={2}>
        <StyledButton themeId={themeId} color="primary" width={200} align="left">
          Align left
        </StyledButton>
        <StyledButton themeId={themeId} color="primary" width={200}>
          Align center
        </StyledButton>
        <StyledButton themeId={themeId} color="primary" width={200} align="right">
          Align right
        </StyledButton>
      </Rhythm>
    )}
  </ThemeWrapper>
</Playground>

### Unstyled button

<Playground>
  <ThemeWrapper>
    <Button unstyled onClick={() => console.log('unstyled')}>
      Unstyled
    </Button>
  </ThemeWrapper>
</Playground>

## \[props\]

### Button

<Props of={Button} />

### ColoredButton

<Props of={ColoredButton} />

### StyledButton

<Props of={StyledButton} />
