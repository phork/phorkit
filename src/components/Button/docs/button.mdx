---
name: Button
menu: Input
route: /components/Button
---

import { Playground, Props } from 'docz';
import { ucfirst } from 'utils';
import { SpinnerIcon } from 'icons/SpinnerIcon';
import { TimesIcon } from 'icons/TimesIcon';
import { BlobbrIcon } from 'icons/internal/BlobbrIcon';
import { Flex } from 'components/Flex';
import { Hoverable } from 'components/Hoverable';
import { Rhythm } from 'components/Rhythm';
import { ColorLooper } from 'docs/helpers/ColorLooper';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Button, ColoredButton, ColoredIconTextButton, StyledButton, IconTextButton } from '../index';

<PageTitle title="Button" src="components/Button" />

## Size, shape and weight

<Playground>
  <ThemeWrapper>
    <Looper
      list={['small', 'medium', 'large']}
      render={size => (
        <Rhythm py={1} grouped key={`buttonSize-${size}`}>
          <Looper
            list={[false, true]}
            render={loading => (
              <Rhythm py={1} grouped key={`buttonSize-${size}-${loading ? 'loading' : 'standard'}`}>
                <Looper
                  list={['pill', 'brick']}
                  render={shape => (
                    <Rhythm m={2} mb={0} key={`buttonSize-${size}-${shape}-${loading ? 'loading' : 'standard'}`}>
                      <Button
                        color="primary"
                        loader={<SpinnerIcon scale={size} />}
                        loading={loading}
                        onClick={() => console.log('click')}
                        shape={shape}
                        size={size}
                        weight="solid"
                      >
                        {ucfirst(size)} {shape} solid
                      </Button>
                      <Button
                        color="primary"
                        loader={<SpinnerIcon scale={size} />}
                        loading={loading}
                        onClick={() => console.log('click')}
                        shape={shape}
                        size={size}
                        weight="shaded"
                      >
                        {ucfirst(size)} {shape} shaded
                      </Button>
                      <Button
                        color="primary"
                        loader={<SpinnerIcon scale={size} />}
                        loading={loading}
                        onClick={() => console.log('click')}
                        shape={shape}
                        size={size}
                        weight="outlined"
                      >
                        {ucfirst(size)} {shape} outlined
                      </Button>
                      <Button
                        color="primary"
                        loader={<SpinnerIcon scale={size} />}
                        loading={loading}
                        shape={shape}
                        weight="ghost"
                        onClick={() => console.log('click')}
                        size={size}
                      >
                        {ucfirst(size)} {shape} ghost
                      </Button>
                      <br />
                    </Rhythm>
                  )}
                />
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

<div style="height: 50px" />

<Playground>
  <ThemeWrapper>
    <Looper
      list={['small', 'medium', 'large']}
      render={size => (
        <Rhythm py={1} grouped key={`iconButtonSize-${size}`}>
          <Looper
            list={[false, true]}
            render={loading => (
              <Rhythm py={1} grouped key={`iconButtonSize-${size}-${loading ? 'loading' : 'standard'}`}>
                <Looper
                  list={['pill', 'brick']}
                  render={shape => (
                    <Rhythm m={2} mb={0} key={`iconButtonSize-${size}-${shape}-${loading ? 'loading' : 'standard'}`}>
                      <IconTextButton
                        color="primary"
                        icon={<BlobbrIcon scale={size} />}
                        loader={<SpinnerIcon scale={size} />}
                        loading={loading}
                        onClick={() => console.log('click')}
                        shape={shape}
                        size={size}
                        text={`${ucfirst(size)} ${shape} solid`}
                        weight="solid"
                      />
                      <IconTextButton
                        color="primary"
                        icon={<BlobbrIcon scale={size} />}
                        loader={<SpinnerIcon scale={size} />}
                        loading={loading}
                        onClick={() => console.log('click')}
                        shape={shape}
                        size={size}
                        text={`${ucfirst(size)} ${shape} shaded`}
                        weight="shaded"
                      />
                      <IconTextButton
                        color="primary"
                        icon={<BlobbrIcon scale={size} />}
                        loader={<SpinnerIcon scale={size} />}
                        loading={loading}
                        onClick={() => console.log('click')}
                        shape={shape}
                        size={size}
                        text={`${ucfirst(size)} ${shape} outlined`}
                        weight="outlined"
                      />
                      <IconTextButton
                        color="primary"
                        icon={<BlobbrIcon scale={size} />}
                        loader={<SpinnerIcon scale={size} />}
                        loading={loading}
                        shape={shape}
                        text={`${ucfirst(size)} ${shape} ghost`}
                        weight="ghost"
                        onClick={() => console.log('click')}
                        size={size}
                      />
                      <br />
                    </Rhythm>
                  )}
                />
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

<div style="height: 50px" />

<Playground>
  <ThemeWrapper>
    <Looper
      list={[false, true]}
      render={loading => (
        <Rhythm mb={2} key={`inlineButton-${loading ? 'loading' : 'standard'}`} grouped>
          <Rhythm m={2}>
            <Button
              color="primary"
              size="small"
              weight="inline"
              loader={<SpinnerIcon scale="small" />}
              loading={loading}
              onClick={() => console.log('primary')}
            >
              Small inline
            </Button>
            <Button
              color="primary"
              size="medium"
              weight="inline"
              loader={<SpinnerIcon scale="medium" />}
              loading={loading}
              onClick={() => console.log('primary')}
            >
              Medium inline
            </Button>
            <Button
              color="primary"
              size="large"
              weight="inline"
              loader={<SpinnerIcon scale="large" />}
              loading={loading}
              onClick={() => console.log('primary')}
            >
              Large inline
            </Button>
          </Rhythm>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Button colors

<Playground>
  <ThemeWrapper>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <div key={weight || 'undefined'}>
          <Rhythm m={2}>
            <Button color="primary" weight={weight} onClick={() => console.log('primary')}>
              Primary
            </Button>
            <Button color="success" weight={weight} onClick={() => console.log('success')}>
              Success
            </Button>
            <Button color="warning" weight={weight} onClick={() => console.log('warning')}>
              Warning
            </Button>
            <Button color="danger" weight={weight} onClick={() => console.log('danger')}>
              Danger
            </Button>
            <Button color="neutral" weight={weight} onClick={() => console.log('neutral')}>
              Neutral
            </Button>
            <Button color="black" weight={weight} onClick={() => console.log('black')}>
              Black
            </Button>
            <Button color="white" weight={weight} onClick={() => console.log('white')}>
              White
            </Button>
          </Rhythm>
          <br />
          <Rhythm m={2}>
            <Button
              color="primary"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('primary')}
              weight={weight}
            >
              Primary
            </Button>
            <Button
              color="success"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('success')}
              weight={weight}
            >
              Success
            </Button>
            <Button
              color="warning"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('warning')}
              weight={weight}
            >
              Warning
            </Button>
            <Button
              color="danger"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('danger')}
              weight={weight}
            >
              Danger
            </Button>
            <Button
              color="neutral"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('neutral')}
              weight={weight}
            >
              Neutral
            </Button>
            <Button
              color="black"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('black')}
              weight={weight}
            >
              Black
            </Button>
            <Button
              color="white"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('white')}
              weight={weight}
            >
              White
            </Button>
          </Rhythm>
        </div>
      )}
    />
  </ThemeWrapper>
</Playground>

<div style="height: 50px" />

<Playground>
  <ThemeWrapper>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <div key={weight || 'undefined'}>
          <Rhythm m={2}>
            <Button color="primary" shape="brick" weight={weight} onClick={() => console.log('primary')}>
              Primary
            </Button>
            <Button color="success" shape="brick" weight={weight} onClick={() => console.log('success')}>
              Success
            </Button>
            <Button color="warning" shape="brick" weight={weight} onClick={() => console.log('warning')}>
              Warning
            </Button>
            <Button color="danger" shape="brick" weight={weight} onClick={() => console.log('danger')}>
              Danger
            </Button>
            <Button color="neutral" shape="brick" weight={weight} onClick={() => console.log('neutral')}>
              Neutral
            </Button>
            <Button color="black" shape="brick" weight={weight} onClick={() => console.log('black')}>
              Black
            </Button>
            <Button color="white" shape="brick" weight={weight} onClick={() => console.log('white')}>
              White
            </Button>
          </Rhythm>
          <br />
          <Rhythm m={2}>
            <Button
              color="primary"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('primary')}
              shape="brick"
              weight={weight}
            >
              Primary
            </Button>
            <Button
              color="success"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('success')}
              shape="brick"
              weight={weight}
            >
              Success
            </Button>
            <Button
              color="warning"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('warning')}
              shape="brick"
              weight={weight}
            >
              Warning
            </Button>
            <Button
              color="danger"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('danger')}
              shape="brick"
              weight={weight}
            >
              Danger
            </Button>
            <Button
              color="neutral"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('neutral')}
              shape="brick"
              weight={weight}
            >
              Neutral
            </Button>
            <Button
              color="black"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('black')}
              shape="brick"
              weight={weight}
            >
              Black
            </Button>
            <Button
              color="white"
              disabled
              loader={<SpinnerIcon scale="medium" />}
              loading
              onClick={() => console.log('white')}
              shape="brick"
              weight={weight}
            >
              White
            </Button>
          </Rhythm>
        </div>
      )}
    />
  </ThemeWrapper>
</Playground>

<div style="height: 50px" />

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <Button weight="inline" color="primary" onClick={() => console.log('primary')}>
        Primary
      </Button>
      <Button weight="inline" color="success" onClick={() => console.log('success')}>
        Success
      </Button>
      <Button weight="inline" color="warning" onClick={() => console.log('warning')}>
        Warning
      </Button>
      <Button weight="inline" color="danger" onClick={() => console.log('danger')}>
        Danger
      </Button>
      <Button weight="inline" color="neutral" onClick={() => console.log('neutral')}>
        Neutral
      </Button>
      <Button weight="inline" color="black" onClick={() => console.log('black')}>
        Black
      </Button>
      <Button weight="inline" color="white" onClick={() => console.log('white')}>
        White
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('primary')}
        weight="inline"
      >
        Primary
      </Button>
      <Button
        color="success"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('success')}
        weight="inline"
      >
        Success
      </Button>
      <Button
        color="warning"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('warning')}
        weight="inline"
      >
        Warning
      </Button>
      <Button
        color="danger"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('danger')}
        weight="inline"
      >
        Danger
      </Button>
      <Button
        color="neutral"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('neutral')}
        weight="inline"
      >
        Neutral
      </Button>
      <Button
        color="black"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('black')}
        weight="inline"
      >
        Black
      </Button>
      <Button
        color="white"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('white')}
        weight="inline"
      >
        White
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

## IconText buttons

<Playground>
  <ThemeWrapper>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <Rhythm mb={2} key={`iconText-${weight}`} grouped>
          <Looper
            list={['pill', 'brick']}
            render={shape => (
              <div key={`iconText-${weight}-${shape}`}>
                <Looper
                  list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
                  render={color => (
                    <Rhythm m={2} key={`iconText-${weight}-${shape}-${color}`}>
                      <IconTextButton
                        color={color}
                        icon={<BlobbrIcon scale="medium" />}
                        shape={shape}
                        text={ucfirst(color)}
                        weight={weight}
                      />
                    </Rhythm>
                  )}
                />
                <br />
              </div>
            )}
          />
        </Rhythm>
      )}
    />
    <Rhythm mt={4} grouped>
      <Looper
        list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
        render={color => (
          <Rhythm mx={4} key={`inline-${color}`}>
            <IconTextButton color={color} icon={<BlobbrIcon scale="medium" />} text={ucfirst(color)} weight="inline" />
          </Rhythm>
        )}
      />
    </Rhythm>
  </ThemeWrapper>
</Playground>

<div style="height: 50px" />

<Playground>
  <ThemeWrapper>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <Rhythm mb={2} key={`iconText-${weight}`} grouped>
          <Looper
            list={['pill', 'brick']}
            render={shape => (
              <div key={`iconText-${weight}-${shape}`}>
                <Looper
                  list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
                  render={color => (
                    <Rhythm m={2} key={`iconTextReverse-${weight}-${shape}-${color}`}>
                      <IconTextButton
                        color={color}
                        icon={<TimesIcon scale="xsmall" />}
                        reverse
                        shape={shape}
                        text={ucfirst(color)}
                        weight={weight}
                      />
                    </Rhythm>
                  )}
                />
                <br />
              </div>
            )}
          />
        </Rhythm>
      )}
    />
    <Rhythm mt={4} grouped>
      <Looper
        list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
        render={color => (
          <Rhythm mx={4} key={`inline-${color}`}>
            <IconTextButton
              color={color}
              icon={<TimesIcon scale="xsmall" />}
              reverse
              text={ucfirst(color)}
              weight="inline"
            />
          </Rhythm>
        )}
      />
    </Rhythm>
  </ThemeWrapper>
</Playground>

## Variations

### Neutral and primary buttons

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <Button color="neutralAndPrimary" weight="solid">
        Pill solid
      </Button>
      <Button color="neutralAndPrimary" weight="shaded">
        Pill shaded
      </Button>
      <Button color="neutralAndPrimary" weight="outlined">
        Pill outlined
      </Button>
      <Button color="neutralAndPrimary" weight="ghost">
        Pill ghost
      </Button>
      <Button color="neutralAndPrimary" weight="inline">
        Inline
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Contrast buttons

<Playground>
  <ThemeWrapper contrast>
    <Rhythm m={2}>
      <Button contrast weight="solid">
        Pill solid
      </Button>
      <Button contrast weight="shaded">
        Pill shaded
      </Button>
      <Button contrast weight="outlined">
        Pill outlined
      </Button>
      <Button contrast weight="ghost">
        Pill ghost
      </Button>
      <Button contrast weight="inline">
        Inline
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button contrast loading loader={<SpinnerIcon scale="medium" />}>
        Pill solid
      </Button>
      <Button contrast weight="shaded" loading loader={<SpinnerIcon scale="medium" />}>
        Pill shaded
      </Button>
      <Button contrast weight="outlined" loading loader={<SpinnerIcon scale="medium" />}>
        Pill outlined
      </Button>
      <Button contrast weight="ghost" loading loader={<SpinnerIcon scale="medium" />}>
        Pill ghost
      </Button>
      <Button contrast weight="inline" loading loader={<SpinnerIcon scale="medium" />}>
        Inline
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

## Colored buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Flex direction="column" wrap>
        <ColorLooper
          themeId={themeId}
          group="primary"
          render={({ id }) => (
            <Flex direction="row" key={`coloredButton-${id}`} wrap>
              <Rhythm key={`coloredButtonSolid-${id}`} m={2}>
                <ColoredIconTextButton
                  colorId={id}
                  icon={<BlobbrIcon scale="medium" />}
                  shape="pill"
                  text={`Color ${id}`}
                  weight="solid"
                />
                <ColoredButton shape="brick" weight="solid" colorId={id}>
                  {`Color ${id}`}
                </ColoredButton>
              </Rhythm>
              <Rhythm key={`coloredButtonShaded-${id}`} m={2}>
                <ColoredIconTextButton
                  colorId={id}
                  icon={<BlobbrIcon scale="medium" />}
                  shape="pill"
                  text={`Color ${id}`}
                  weight="shaded"
                />
                <ColoredButton shape="brick" weight="shaded" colorId={id}>
                  {`Color ${id}`}
                </ColoredButton>
              </Rhythm>
              <Rhythm key={`coloredButtonOutlined-${id}`} m={2}>
                <ColoredIconTextButton
                  colorId={id}
                  icon={<BlobbrIcon scale="medium" />}
                  shape="pill"
                  text={`Color ${id}`}
                  weight="outlined"
                />
                <ColoredButton shape="brick" weight="outlined" colorId={id}>
                  {`Color ${id}`}
                </ColoredButton>
              </Rhythm>
              <Rhythm key={`coloredButtonText-${id}`} m={2}>
                <ColoredIconTextButton
                  colorId={id}
                  icon={<BlobbrIcon scale="medium" />}
                  shape="pill"
                  text={`Color ${id}`}
                  weight="ghost"
                />
                <ColoredButton shape="brick" weight="ghost" colorId={id}>
                  {`Color ${id}`}
                </ColoredButton>
                <ColoredButton weight="inline" colorId={id}>
                  {`Color ${id}`}
                </ColoredButton>
              </Rhythm>
              <div style={{ width: '100%', height: '4px' }} />
            </Flex>
          )}
        />
      </Flex>
    )}
  </ThemeWrapper>
</Playground>

### Styled buttons

<Playground>
  {() => {
    const styles = {
      primaryColor: '#556270',
      inverseColor: '#fff',
      hoveredPrimaryColor: '#454f58',
      activePrimaryColor: '#798796',
    };
    return (
      <React.Fragment>
        <Rhythm m={2}>
          <StyledButton {...styles}>Styled</StyledButton>
          <StyledButton weight="outlined" {...styles}>
            Styled
          </StyledButton>
          <StyledButton weight="shaded" {...styles}>
            Styled
          </StyledButton>
          <StyledButton weight="ghost" {...styles}>
            Styled
          </StyledButton>
          <StyledButton weight="inline" {...styles}>
            Styled
          </StyledButton>
        </Rhythm>
        <br />
        <Rhythm m={2}>
          <StyledButton loading loader={<SpinnerIcon scale="medium" />} {...styles}>
            Styled
          </StyledButton>
          <StyledButton weight="outlined" loading loader={<SpinnerIcon scale="medium" />} {...styles}>
            Styled
          </StyledButton>
          <StyledButton weight="shaded" loading loader={<SpinnerIcon scale="medium" />} {...styles}>
            Styled
          </StyledButton>
          <StyledButton weight="ghost" loading loader={<SpinnerIcon scale="medium" />} {...styles}>
            Styled
          </StyledButton>
          <StyledButton weight="inline" loading loader={<SpinnerIcon scale="medium" />} {...styles}>
            Styled
          </StyledButton>
        </Rhythm>
      </React.Fragment>
    );
  }}
</Playground>

### Linked buttons

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <Button href="#linked" onClick={() => console.log('linked clicked')} color="primary">
        Linked
      </Button>
      <Button href="#linked" onClick={() => console.log('linked clicked')} weight="shaded" color="primary">
        Linked
      </Button>
      <Button href="#linked" onClick={() => console.log('linked clicked')} weight="outlined" color="primary">
        Linked
      </Button>
      <Button href="#linked" onClick={() => console.log('linked clicked')} weight="ghost" color="primary">
        Linked
      </Button>
      <Button href="#linked" onClick={() => console.log('linked clicked')} weight="inline" color="primary">
        Linked
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('linked clicked')}
      >
        Linked
      </Button>
      <Button
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('linked clicked')}
        weight="shaded"
      >
        Linked
      </Button>
      <Button
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('linked clicked')}
        weight="outlined"
      >
        Linked
      </Button>
      <Button
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('linked clicked')}
        weight="ghost"
      >
        Linked
      </Button>
      <Button
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('linked clicked')}
        weight="inline"
      >
        Linked
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Non-button buttons

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <Button as="div" onClick={() => console.log('div clicked')} color="primary">
        Acts like a button
      </Button>
      <Button as="div" onClick={() => console.log('div clicked')} weight="shaded" color="primary">
        Acts like a button
      </Button>
      <Button as="div" onClick={() => console.log('div clicked')} weight="outlined" color="primary">
        Acts like a button
      </Button>
      <Button as="div" onClick={() => console.log('div clicked')} weight="ghost" color="primary">
        Acts like a button
      </Button>
      <Button as="div" onClick={() => console.log('div clicked')} weight="inline" color="primary">
        Acts like a button
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('div clicked')}
      >
        Acts like a button
      </Button>
      <Button
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('div clicked')}
        weight="shaded"
      >
        Acts like a button
      </Button>
      <Button
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('div clicked')}
        weight="outlined"
      >
        Acts like a button
      </Button>
      <Button
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('div clicked')}
        weight="ghost"
      >
        Acts like a button
      </Button>
      <Button
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        loading
        onClick={() => console.log('div clicked')}
        weight="inline"
      >
        Acts like a button
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Sized buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Looper
        list={['pill', 'brick']}
        render={shape => (
          <div key={shape}>
            <Rhythm m={2}>
              <Button shape={shape} themeId={themeId} color="primary" fullWidth>
                Full width
              </Button>
              <Button shape={shape} themeId={themeId} color="primary" width={200}>
                Fixed width
              </Button>
            </Rhythm>
          </div>
        )}
      />
    )}
  </ThemeWrapper>
</Playground>

### Aligned buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Rhythm m={2}>
        <StyledButton themeId={themeId} color="primary" width={200} align="left">
          Align left
        </StyledButton>
        <StyledButton themeId={themeId} color="primary" width={200}>
          Align center
        </StyledButton>
        <StyledButton themeId={themeId} color="primary" width={200} align="right">
          Align right
        </StyledButton>
      </Rhythm>
    )}
  </ThemeWrapper>
</Playground>

### Unstyled button

<Playground>
  <ThemeWrapper>
    <Button unstyled onClick={() => console.log('unstyled')}>
      Unstyled
    </Button>
  </ThemeWrapper>
</Playground>

## \[props\]

### Button

<Props of={Button} />

### ColoredButton

<Props of={ColoredButton} />

### StyledButton

<Props of={StyledButton} />

### IconTextButton

<Props of={IconTextButton} />

### ColoredIconTextButton

<Props of={ColoredIconTextButton} />
