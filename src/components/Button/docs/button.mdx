---
name: Button
menu: Buttons
route: /components/Button
---

import { Playground, Props } from 'docz';
import { ucfirst } from 'utils';
import { SpinnerIcon } from 'icons/SpinnerIcon';
import { Flex } from 'components/Flex';
import { Looper } from 'components/Looper';
import { Rhythm } from 'components/Rhythm';
import { ColorLooper } from 'docs/helpers/ColorLooper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Button, ColoredButton, StyledButton } from '../index';

<PageTitle src="components/Button" title="Button" />

## Size, shape and weight

<Playground>
  <ThemeWrapper>
    <Looper
      list={['small', 'medium', 'large']}
      render={size => (
        <Rhythm grouped key={size} py={2}>
          <Looper
            list={['pill', 'brick']}
            render={shape => (
              <Rhythm grouped key={`${size}-${shape}`} py={2}>
                <Looper
                  list={[false, true]}
                  render={loading => (
                    <Flex wrap direction="row" key={`${size}-${shape}-${loading ? 'loading' : 'standard'}`}>
                      <Looper
                        list={['solid', 'shaded', 'outlined', 'ghost']}
                        render={weight => (
                          <Rhythm key={`${size}-${shape}-${loading ? 'loading' : 'standard'}-${weight}`} m={2} mb={0}>
                            <Button
                              color="primary"
                              loader={<SpinnerIcon scale={size} />}
                              loading={loading}
                              onClick={() => console.log('click')}
                              shape={shape}
                              size={size}
                              weight={weight}
                            >
                              {ucfirst(size)} {shape} {weight}
                            </Button>
                          </Rhythm>
                        )}
                      />
                    </Flex>
                  )}
                />
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

### Inline buttons

<Playground>
  <ThemeWrapper style={{ fontSize: 14 }}>
    <Looper
      list={[false, true]}
      render={loading => (
        <Rhythm grouped key={`${loading ? 'loading' : 'standard'}`} mb={2}>
          <Rhythm m={2}>
            <Button
              color="primary"
              loader={<SpinnerIcon scale="small" />}
              loading={loading}
              onClick={() => console.log('small')}
              size="small"
              weight="inline"
            >
              Small inline
            </Button>
            <Button
              color="primary"
              loader={<SpinnerIcon scale="medium" />}
              loading={loading}
              onClick={() => console.log('medium')}
              size="medium"
              weight="inline"
            >
              Medium inline
            </Button>
            <Button
              color="primary"
              loader={<SpinnerIcon scale="large" />}
              loading={loading}
              onClick={() => console.log('large')}
              size="large"
              weight="inline"
            >
              Large inline
            </Button>
            <Button
              color="primary"
              loader={<SpinnerIcon scale="large" />}
              loading={loading}
              onClick={() => console.log('relative')}
              size="relative"
              weight="inline"
            >
              Relative inline
            </Button>
          </Rhythm>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Button colors

<Playground>
  <ThemeWrapper>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <Rhythm grouped key={weight || 'undefined'} m={2}>
          <Looper
            list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
            render={color => (
              <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                <Button color={color} onClick={() => console.log(color)} weight={weight}>
                  {ucfirst(color)}
                </Button>
              </Rhythm>
            )}
          />
          <br />
          <Looper
            list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
            render={color => (
              <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                <Button
                  disabled
                  loading
                  color={color}
                  loader={<SpinnerIcon scale="medium" />}
                  onClick={() => console.log(color)}
                  weight={weight}
                >
                  {ucfirst(color)}
                </Button>
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

<div style="height: 50px" />

<Playground>
  <ThemeWrapper>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <Rhythm grouped key={weight} m={2}>
          <Looper
            list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
            render={color => (
              <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                <Button color={color} onClick={() => console.log(color)} shape="brick" weight={weight}>
                  {ucfirst(color)}
                </Button>
              </Rhythm>
            )}
          />
          <br />
          <Looper
            list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
            render={color => (
              <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                <Button
                  disabled
                  loading
                  color={color}
                  loader={<SpinnerIcon scale="medium" />}
                  onClick={() => console.log(color)}
                  shape="brick"
                  weight={weight}
                >
                  {ucfirst(color)}
                </Button>
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

<div style={{ height: '50px' }} />

<Playground>
  <ThemeWrapper>
    <Looper
      list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
      render={color => (
        <Rhythm key={color} m={2}>
          <Button color={color} onClick={() => console.log(color)} shape="brick" weight="inline">
            {ucfirst(color)}
          </Button>
        </Rhythm>
      )}
    />
    <br />
    <Looper
      list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
      render={color => (
        <Rhythm key={color} m={2}>
          <Button
            disabled
            loading
            color={color}
            loader={<SpinnerIcon scale="medium" />}
            onClick={() => console.log(color)}
            shape="brick"
            weight="inline"
          >
            {ucfirst(color)}
          </Button>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Variations

### Contrast buttons

<Playground>
  <ThemeWrapper contrast>
    <Rhythm m={2}>
      <Button contrast weight="solid">
        Pill solid
      </Button>
      <Button contrast weight="shaded">
        Pill shaded
      </Button>
      <Button contrast weight="outlined">
        Pill outlined
      </Button>
      <Button contrast weight="ghost">
        Pill ghost
      </Button>
      <Button contrast weight="inline">
        Pill inline
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button contrast loading loader={<SpinnerIcon scale="medium" />}>
        Pill solid
      </Button>
      <Button contrast loading loader={<SpinnerIcon scale="medium" />} weight="shaded">
        Pill shaded
      </Button>
      <Button contrast loading loader={<SpinnerIcon scale="medium" />} weight="outlined">
        Pill outlined
      </Button>
      <Button contrast loading loader={<SpinnerIcon scale="medium" />} weight="ghost">
        Pill ghost
      </Button>
      <Button contrast loading loader={<SpinnerIcon scale="medium" />} weight="inline">
        Pill inline
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Colored buttons

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Flex wrap direction="column">
        <ColorLooper
          group="primary"
          render={({ id }) => (
            <Rhythm key={id} my={2}>
              <Flex direction="column">
                <Flex wrap durection="row">
                  <Rhythm mx={2} my={1}>
                    <ColoredButton colorId={id} weight="solid">
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton colorId={id} weight="shaded">
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton colorId={id} weight="outlined">
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton colorId={id} weight="ghost">
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton colorId={id} weight="inline">
                      {`Color ${id}`}
                    </ColoredButton>
                  </Rhythm>
                </Flex>
                <Flex wrap durection="row">
                  <Rhythm mx={2} my={1}>
                    <ColoredButton loading colorId={id} loader={<SpinnerIcon scale="medium" />} weight="solid">
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton loading colorId={id} loader={<SpinnerIcon scale="medium" />} weight="shaded">
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton loading colorId={id} loader={<SpinnerIcon scale="medium" />} weight="outlined">
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton loading colorId={id} loader={<SpinnerIcon scale="medium" />} weight="ghost">
                      {`Color ${id}`}
                    </ColoredButton>
                    <ColoredButton loading colorId={id} loader={<SpinnerIcon scale="medium" />} weight="inline">
                      {`Color ${id}`}
                    </ColoredButton>
                  </Rhythm>
                </Flex>
              </Flex>
            </Rhythm>
          )}
          themeId={themeId}
        />
      </Flex>
    )}
  </ThemeWrapper>
</Playground>

### Styled buttons

<Playground>
  {() => {
    const styles = {
      primaryColor: '#556270',
      inverseColor: '#fff',
      hoveredPrimaryColor: '#454f58',
      activePrimaryColor: '#798796',
    };
    return (
      <React.Fragment>
        <Rhythm m={2}>
          <StyledButton {...styles}>Styled solid</StyledButton>
          <StyledButton weight="shaded" {...styles}>
            Styled shaded
          </StyledButton>
          <StyledButton weight="outlined" {...styles}>
            Styled outlined
          </StyledButton>
          <StyledButton weight="ghost" {...styles}>
            Styled ghost
          </StyledButton>
          <StyledButton weight="inline" {...styles}>
            Styled inline
          </StyledButton>
        </Rhythm>
        <br />
        <Rhythm m={2}>
          <StyledButton loading loader={<SpinnerIcon scale="medium" />} {...styles}>
            Styled solid
          </StyledButton>
          <StyledButton loading loader={<SpinnerIcon scale="medium" />} weight="shaded" {...styles}>
            Styled shaded
          </StyledButton>
          <StyledButton loading loader={<SpinnerIcon scale="medium" />} weight="outlined" {...styles}>
            Styled outlined
          </StyledButton>
          <StyledButton loading loader={<SpinnerIcon scale="medium" />} weight="ghost" {...styles}>
            Styled ghost
          </StyledButton>
          <StyledButton loading loader={<SpinnerIcon scale="medium" />} weight="inline" {...styles}>
            Styled inline
          </StyledButton>
        </Rhythm>
      </React.Fragment>
    );
  }}
</Playground>

### Linked buttons

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <Button color="primary" href="#linked" onClick={() => console.log('linked clicked')}>
        Linked solid
      </Button>
      <Button color="primary" href="#linked" onClick={() => console.log('linked clicked')} weight="shaded">
        Linked shaded
      </Button>
      <Button color="primary" href="#linked" onClick={() => console.log('linked clicked')} weight="outlined">
        Linked outlined
      </Button>
      <Button color="primary" href="#linked" onClick={() => console.log('linked clicked')} weight="ghost">
        Linked ghost
      </Button>
      <Button color="primary" href="#linked" onClick={() => console.log('linked clicked')} weight="inline">
        Linked inline
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button
        loading
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
      >
        Linked solid
      </Button>
      <Button
        loading
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
        weight="shaded"
      >
        Linked shaded
      </Button>
      <Button
        loading
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
        weight="outlined"
      >
        Linked outlined
      </Button>
      <Button
        loading
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
        weight="ghost"
      >
        Linked ghost
      </Button>
      <Button
        loading
        color="primary"
        href="#linked"
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('linked clicked')}
        weight="inline"
      >
        Linked inline
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Non-button buttons

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <Button as="div" color="primary" onClick={() => console.log('div clicked')}>
        Acts like a button
      </Button>
      <Button as="div" color="primary" onClick={() => console.log('div clicked')} weight="shaded">
        Acts like a button
      </Button>
      <Button as="div" color="primary" onClick={() => console.log('div clicked')} weight="outlined">
        Acts like a button
      </Button>
      <Button as="div" color="primary" onClick={() => console.log('div clicked')} weight="ghost">
        Acts like a button
      </Button>
      <Button as="div" color="primary" onClick={() => console.log('div clicked')} weight="inline">
        Acts like a button
      </Button>
    </Rhythm>
    <br />
    <Rhythm m={2}>
      <Button
        loading
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
      >
        Acts like a button
      </Button>
      <Button
        loading
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
        weight="shaded"
      >
        Acts like a button
      </Button>
      <Button
        loading
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
        weight="outlined"
      >
        Acts like a button
      </Button>
      <Button
        loading
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
        weight="ghost"
      >
        Acts like a button
      </Button>
      <Button
        loading
        as="div"
        color="primary"
        loader={<SpinnerIcon scale="medium" />}
        onClick={() => console.log('div clicked')}
        weight="inline"
      >
        Acts like a button
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Sized buttons

<Playground>
  <ThemeWrapper>
    <Looper
      list={['pill', 'brick']}
      render={shape => (
        <div key={shape}>
          <Rhythm m={2}>
            <Button fullWidth color="primary" shape={shape}>
              Full width
            </Button>
            <Button color="primary" shape={shape} width={200}>
              Fixed width
            </Button>
          </Rhythm>
        </div>
      )}
    />
  </ThemeWrapper>
</Playground>

### Aligned buttons

<Playground>
  <ThemeWrapper>
    <Rhythm m={2}>
      <Button align="left" color="primary" style={{ width: 200 }}>
        Align left
      </Button>
      <Button color="primary" style={{ width: 200 }}>
        Align center
      </Button>
      <Button align="right" color="primary" style={{ width: 200 }}>
        Align right
      </Button>
    </Rhythm>
  </ThemeWrapper>
</Playground>

### Unstyled button

<Playground>
  <ThemeWrapper>
    <Button unstyled onClick={() => console.log('unstyled')}>
      Unstyled
    </Button>
  </ThemeWrapper>
</Playground>

## \[props\]

### Button

<Props of={Button} />

### ColoredButton

<Props of={ColoredButton} />

### StyledButton

<Props of={StyledButton} />
