import { action } from '@storybook/addon-actions';
import { ArgsTable, Description, Primary, Stories, Subtitle, PRIMARY_STORY } from '@storybook/addon-docs';
import { SpinnerIcon } from 'icons/SpinnerIcon';
import { CheckIcon } from 'icons/internal/CheckIcon';
import { Flex } from 'components/Flex';
import { Looper } from 'components/Looper';
import { Rhythm } from 'components/Rhythm';
import { Canvas } from 'stories/helpers/Canvas';
import { PageTitle } from 'stories/helpers/PageTitle';
import { IconButton } from '../IconButton';

<PageTitle src="components/Button/IconButton" title="IconButton" />
<Subtitle />
<Description />

#### Colors

[Primary](#color--primary) | [Success](#color--success) | [Warning](#color--warning) | [Danger](#color--danger) | [Neutral](#color--neutral) | [Black](#color--black) | [White](#color--white)

#### Shapes

[Circle](#shape--circle) | [Square](#shape--square)

#### Weights

[Solid](#weight--solid) | [Shaded](#weight--shaded) | [Outlined](#weight--outlined) | [Ghost](#weight--ghost) | [Inline](#weight--inline)

#### Sizes

[Small](#size--small) | [Medium](#size--medium) | [Large](#size--large) | [XLarge](#size--xlarge) | [2XLarge](#size--xxlarge) | [3XLarge](#size--xxxlarge) | [4XLarge](#size--xxxxlarge) | [5XLarge](#size--xxxxxlarge)

<Primary />
<ArgsTable story={PRIMARY_STORY} />
<Stories />

### Contrast

<Canvas contrast>
  <IconButton contrast shape="circle" weight="solid">
    <CheckIcon scale="medium" />
  </IconButton>
</Canvas>

## Examples

<Canvas>
  <Rhythm grouped m={-2}>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <Rhythm grouped key={weight || 'undefined'} m={2}>
          <Flex direction="row">
            <Looper
              list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
              render={color => (
                <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                  <IconButton color={color} onClick={() => action(color)} weight={weight}>
                    <CheckIcon scale="medium" />
                  </IconButton>
                </Rhythm>
              )}
            />
          </Flex>
          <Flex direction="row">
            <Looper
              list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
              render={color => (
                <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                  <IconButton
                    disabled
                    loading
                    color={color}
                    loader={<SpinnerIcon scale="medium" />}
                    onClick={() => action(color)}
                    weight={weight}
                  >
                    <CheckIcon scale="medium" />
                  </IconButton>
                </Rhythm>
              )}
            />
          </Flex>
        </Rhythm>
      )}
    />
  </Rhythm>
</Canvas>

<Canvas>
  <Rhythm grouped m={-2}>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <Rhythm grouped key={weight || 'undefined'} m={2}>
          <Flex direction="row">
            <Looper
              list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
              render={color => (
                <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                  <IconButton color={color} onClick={() => action(color)} shape="square" weight={weight}>
                    <CheckIcon scale="medium" />
                  </IconButton>
                </Rhythm>
              )}
            />
          </Flex>
          <Flex direction="row">
            <Looper
              list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
              render={color => (
                <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                  <IconButton
                    disabled
                    loading
                    color={color}
                    loader={<SpinnerIcon scale="medium" />}
                    onClick={() => action(color)}
                    shape="square"
                    weight={weight}
                  >
                    <CheckIcon scale="medium" />
                  </IconButton>
                </Rhythm>
              )}
            />
          </Flex>
        </Rhythm>
      )}
    />
  </Rhythm>
</Canvas>

<Canvas>
  <Rhythm grouped m={-2}>
    <Flex direction="row">
      <Looper
        list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
        render={color => (
          <Rhythm key={color} m={2}>
            <IconButton color={color} onClick={() => action(color)} shape="square" weight="inline">
              <CheckIcon scale="xlarge" />
            </IconButton>
          </Rhythm>
        )}
      />
    </Flex>
    <Flex direction="row">
      <Looper
        list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
        render={color => (
          <Rhythm key={color} m={2}>
            <IconButton
              disabled
              loading
              color={color}
              loader={<SpinnerIcon scale="xlarge" />}
              onClick={() => action(color)}
              shape="square"
              weight="inline"
            >
              <CheckIcon scale="xlarge" />
            </IconButton>
          </Rhythm>
        )}
      />
    </Flex>
  </Rhythm>
</Canvas>

<Canvas contrast>
  <Rhythm grouped m={-2}>
    <Flex alignItems="center" direction="row">
      <Looper
        list={['solid', 'shaded', 'outlined', 'ghost', 'inline']}
        render={weight => (
          <Rhythm key={weight} m={2}>
            <IconButton contrast weight={weight}>
              <CheckIcon scale="medium" />
            </IconButton>
          </Rhythm>
        )}
      />
    </Flex>
    <Flex alignItems="center" direction="row">
      <Looper
        list={['solid', 'shaded', 'outlined', 'ghost', 'inline']}
        render={weight => (
          <Rhythm key={weight} m={2}>
            <IconButton contrast loading key={weight} loader={<SpinnerIcon scale="medium" />} weight={weight}>
              <CheckIcon scale="xlarge" />
            </IconButton>
          </Rhythm>
        )}
      />
    </Flex>
  </Rhythm>
</Canvas>
