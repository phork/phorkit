import { action } from '@storybook/addon-actions';
import { ArgsTable, Description, Primary, Stories, Subtitle, PRIMARY_STORY } from '@storybook/addon-docs';
import { ucfirst } from 'utils';
import { SpinnerIcon } from 'icons/SpinnerIcon';
import { CheckIcon } from 'icons/internal/CheckIcon';
import { Flex } from 'components/Flex';
import { Looper } from 'components/Looper';
import { Rhythm } from 'components/Rhythm';
import { Canvas } from 'stories/helpers/Canvas';
import { PageTitle } from 'stories/helpers/PageTitle';
import { IconTextButton } from '../IconTextButton';

<PageTitle src="components/Button/IconTextButton" title="IconTextButton" />
<Subtitle />
<Description />

#### Colors

[Primary](#color--primary) | [Success](#color--success) | [Warning](#color--warning) | [Danger](#color--danger) | [Neutral](#color--neutral) | [Black](#color--black) | [White](#color--white)

#### Shapes

[Pill](#shape--pill) | [Brick](#shape--brick)

#### Weights

[Solid](#weight--solid) | [Shaded](#weight--shaded) | [Outlined](#weight--outlined) | [Ghost](#weight--ghost) | [Inline](#weight--inline)

#### Sizes

[Small](#size--small) | [Medium](#size--medium) | [Large](#size--large)

<Primary />
<ArgsTable story={PRIMARY_STORY} />
<Stories />

### Contrast

<Canvas contrast>
  <IconTextButton contrast icon={<CheckIcon scale="medium" />}>
    Click me
  </IconTextButton>
</Canvas>

## Examples

<Canvas>
  <Rhythm grouped m={-2}>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <Rhythm grouped key={weight || 'undefined'} m={2}>
          <Flex direction="row">
            <Looper
              list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
              render={color => (
                <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                  <IconTextButton
                    color={color}
                    icon={<CheckIcon scale="medium" />}
                    onClick={() => action(color)}
                    weight={weight}
                  >
                    {ucfirst(color)}
                  </IconTextButton>
                </Rhythm>
              )}
            />
          </Flex>
          <Flex direction="row">
            <Looper
              list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
              render={color => (
                <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                  <IconTextButton
                    loaderReplaceIcon
                    loading
                    color={color}
                    icon={<CheckIcon scale="medium" />}
                    loader={<SpinnerIcon scale="medium" />}
                    onClick={() => action(color)}
                    weight={weight}
                  >
                    {ucfirst(color)}
                  </IconTextButton>
                </Rhythm>
              )}
            />
          </Flex>
          <Flex direction="row">
            <Looper
              list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
              render={color => (
                <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                  <IconTextButton
                    loading
                    color={color}
                    icon={<CheckIcon scale="medium" />}
                    loader={<SpinnerIcon scale="medium" />}
                    onClick={() => action(color)}
                    weight={weight}
                  >
                    {ucfirst(color)}
                  </IconTextButton>
                </Rhythm>
              )}
            />
          </Flex>
        </Rhythm>
      )}
    />
  </Rhythm>
</Canvas>

<Canvas>
  <Rhythm grouped m={-2}>
    <Looper
      list={['solid', 'shaded', 'outlined', 'ghost']}
      render={weight => (
        <Rhythm grouped key={weight || 'undefined'} m={2}>
          <Flex direction="row">
            <Looper
              list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
              render={color => (
                <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                  <IconTextButton
                    color={color}
                    icon={<CheckIcon scale="medium" />}
                    onClick={() => action(color)}
                    shape="brick"
                    weight={weight}
                  >
                    {ucfirst(color)}
                  </IconTextButton>
                </Rhythm>
              )}
            />
          </Flex>
          <Flex direction="row">
            <Looper
              list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
              render={color => (
                <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                  <IconTextButton
                    loaderReplaceIcon
                    loading
                    color={color}
                    icon={<CheckIcon scale="medium" />}
                    loader={<SpinnerIcon scale="medium" />}
                    onClick={() => action(color)}
                    shape="brick"
                    weight={weight}
                  >
                    {ucfirst(color)}
                  </IconTextButton>
                </Rhythm>
              )}
            />
          </Flex>
          <Flex direction="row">
            <Looper
              list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
              render={color => (
                <Rhythm key={`${weight}-${color}`} mx={2} my={1}>
                  <IconTextButton
                    loading
                    color={color}
                    icon={<CheckIcon scale="medium" />}
                    loader={<SpinnerIcon scale="medium" />}
                    onClick={() => action(color)}
                    shape="brick"
                    weight={weight}
                  >
                    {ucfirst(color)}
                  </IconTextButton>
                </Rhythm>
              )}
            />
          </Flex>
        </Rhythm>
      )}
    />
  </Rhythm>
</Canvas>

<Canvas>
  <Rhythm grouped m={-2}>
    <Flex direction="row">
      <Looper
        list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
        render={color => (
          <Rhythm key={color} m={2}>
            <IconTextButton
              color={color}
              icon={<CheckIcon scale="medium" />}
              onClick={() => action(color)}
              shape="brick"
              weight="inline"
            >
              {ucfirst(color)}
            </IconTextButton>
          </Rhythm>
        )}
      />
    </Flex>
    <Flex direction="row">
      <Looper
        list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
        render={color => (
          <Rhythm key={color} m={2}>
            <IconTextButton
              loaderReplaceIcon
              loading
              color={color}
              icon={<CheckIcon scale="medium" />}
              loader={<SpinnerIcon scale="medium" />}
              onClick={() => action(color)}
              shape="brick"
              weight="inline"
            >
              {ucfirst(color)}
            </IconTextButton>
          </Rhythm>
        )}
      />
    </Flex>
    <Flex direction="row">
      <Looper
        list={['primary', 'success', 'warning', 'danger', 'neutral', 'black', 'white']}
        render={color => (
          <Rhythm key={color} m={2}>
            <IconTextButton
              loading
              color={color}
              icon={<CheckIcon scale="medium" />}
              loader={<SpinnerIcon scale="medium" />}
              onClick={() => action(color)}
              shape="brick"
              weight="inline"
            >
              {ucfirst(color)}
            </IconTextButton>
          </Rhythm>
        )}
      />
    </Flex>
  </Rhythm>
</Canvas>

<Canvas contrast>
  <Rhythm grouped m={-2}>
    <Flex alignItems="center" direction="row">
      <Looper
        list={['solid', 'shaded', 'outlined', 'ghost', 'inline']}
        render={weight => (
          <Rhythm key={weight} m={2}>
            <IconTextButton contrast icon={<CheckIcon scale="medium" />} weight={weight}>
              {ucfirst(weight)}
            </IconTextButton>
          </Rhythm>
        )}
      />
    </Flex>
    <Flex alignItems="center" direction="row">
      <Looper
        list={['solid', 'shaded', 'outlined', 'ghost', 'inline']}
        render={weight => (
          <Rhythm key={weight} m={2}>
            <IconTextButton
              contrast
              loaderReplaceIcon
              loading
              icon={<CheckIcon scale="medium" />}
              loader={<SpinnerIcon scale="medium" />}
              weight={weight}
            >
              {ucfirst(weight)}
            </IconTextButton>
          </Rhythm>
        )}
      />
    </Flex>
    <Flex alignItems="center" direction="row">
      <Looper
        list={['solid', 'shaded', 'outlined', 'ghost', 'inline']}
        render={weight => (
          <Rhythm key={weight} m={2}>
            <IconTextButton
              contrast
              loading
              icon={<CheckIcon scale="medium" />}
              key={weight}
              loader={<SpinnerIcon scale="medium" />}
              weight={weight}
            >
              {ucfirst(weight)}
            </IconTextButton>
          </Rhythm>
        )}
      />
    </Flex>
  </Rhythm>
</Canvas>
