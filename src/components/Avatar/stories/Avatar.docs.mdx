import { action } from '@storybook/addon-actions';
import { ArgsTable, Description, Primary, Stories, Subtitle, PRIMARY_STORY } from '@storybook/addon-docs';
import { Flex } from 'components/Flex';
import { Looper } from 'components/Looper';
import { Rhythm } from 'components/Rhythm';
import { Canvas } from 'stories/helpers/Canvas';
import { PageTitle } from 'stories/helpers/PageTitle';
import { ThemeWrapper } from 'stories/helpers/ThemeWrapper';
import { Avatar } from '../Avatar';

<PageTitle src="components/Avatar" title="Avatar" />
<Subtitle />
<Description />
<Primary />
<ArgsTable story={PRIMARY_STORY} />
<Stories />

### Contrast

<Canvas contrast>
  <ThemeWrapper>
    <Avatar contrast initials="EC" />
  </ThemeWrapper>
</Canvas>

## Examples

<Canvas>
  <ThemeWrapper>
    <Flex direction="column">
      <div>
        <h3>With image</h3>
        <Flex wrap alignItems="center" direction="row">
          <Rhythm m={2}>
            <Looper
              list={['xsmall', 'small', 'medium', 'large', 'xlarge', '2xlarge', '3xlarge']}
              render={size => (
                <Rhythm key={size} m={2}>
                  <Avatar actionable color="primary" imgSrc="/images/avatar.jpg" initials="EC" size={size} />
                </Rhythm>
              )}
            />
          </Rhythm>
        </Flex>
      </div>
      <div>
        <h3>Primary color</h3>
        <Flex wrap alignItems="center" direction="row">
          <Rhythm m={2}>
            <Looper
              list={['xsmall', 'small', 'medium', 'large', 'xlarge', '2xlarge', '3xlarge']}
              render={size => (
                <Rhythm key={size} m={2}>
                  <Avatar actionable color="primary" initials="EC" size={size} />
                </Rhythm>
              )}
            />
          </Rhythm>
        </Flex>
      </div>
      <div>
        <h3>Neutral color</h3>
        <Flex wrap alignItems="center" direction="row">
          <Rhythm m={2}>
            <Looper
              list={['xsmall', 'small', 'medium', 'large', 'xlarge', '2xlarge', '3xlarge']}
              render={size => (
                <Rhythm key={size} m={2}>
                  <Avatar actionable color="neutral" initials="EC" size={size} />
                </Rhythm>
              )}
            />
          </Rhythm>
        </Flex>
      </div>
    </Flex>
  </ThemeWrapper>
</Canvas>

<Canvas contrast>
  <ThemeWrapper>
    <Flex wrap alignItems="center" direction="row">
      <Rhythm m={2}>
        <Looper
          list={['xsmall', 'small', 'medium', 'large', 'xlarge', '2xlarge', '3xlarge']}
          render={size => (
            <Rhythm key={size} m={2}>
              <Avatar actionable contrast initials="EC" size={size} />
            </Rhythm>
          )}
        />
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Canvas>
