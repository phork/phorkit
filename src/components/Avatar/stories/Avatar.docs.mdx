import { ArgsTable, Description, Primary, Stories, Subtitle, PRIMARY_STORY } from '@storybook/addon-docs';
import { Flex } from 'components/Flex';
import { Looper } from 'components/Looper';
import { Rhythm } from 'components/Rhythm';
import { Canvas } from 'stories/helpers/Canvas';
import { PageTitle } from 'stories/helpers/PageTitle';
import { Avatar } from '../Avatar';

<PageTitle src="components/Avatar" title="Avatar" />
<Subtitle />
<Description />
<Primary />
<ArgsTable story={PRIMARY_STORY} />
<Stories />

### Contrast

<Canvas contrast>
  <Avatar contrast initials="P" />
</Canvas>

## Examples

<Canvas>
  <Flex wrap alignItems="center" direction="row">
    <Rhythm m={2}>
      <Looper
        list={['xsmall', 'small', 'medium', 'large', 'xlarge', '2xlarge', '3xlarge', '4xlarge', '5xlarge']}
        render={size => (
          <Rhythm key={size} m={2}>
            <Avatar actionable color="primary" imgSrc="/images/avatar.png" initials="P" size={size} title={size} />
          </Rhythm>
        )}
      />
    </Rhythm>
  </Flex>
</Canvas>

<Canvas>
  <Flex wrap alignItems="center" direction="row">
    <Rhythm m={2}>
      <Looper
        list={['xsmall', 'small', 'medium', 'large', 'xlarge', '2xlarge', '3xlarge', '4xlarge', '5xlarge']}
        render={size => (
          <Rhythm key={size} m={2}>
            <Avatar actionable color="primary" imgSrc="/images/avatar.jpg" initials="P" size={size} title={size} />
          </Rhythm>
        )}
      />
    </Rhythm>
  </Flex>
</Canvas>

<Canvas>
  <Flex wrap alignItems="center" direction="row">
    <Rhythm m={2}>
      <Looper
        list={['xsmall', 'small', 'medium', 'large', 'xlarge', '2xlarge', '3xlarge', '4xlarge', '5xlarge']}
        render={size => (
          <Rhythm key={size} m={2}>
            <Avatar actionable color="primary" initials="P" size={size} title={size} />
          </Rhythm>
        )}
      />
    </Rhythm>
  </Flex>
</Canvas>

<Canvas>
  <Flex wrap alignItems="center" direction="row">
    <Rhythm m={2}>
      <Looper
        list={['xsmall', 'small', 'medium', 'large', 'xlarge', '2xlarge', '3xlarge', '4xlarge', '5xlarge']}
        render={size => (
          <Rhythm key={size} m={2}>
            <Avatar actionable color="neutral" initials="P" size={size} title={size} />
          </Rhythm>
        )}
      />
    </Rhythm>
  </Flex>
</Canvas>

<Canvas contrast>
  <Flex wrap alignItems="center" direction="row">
    <Rhythm m={2}>
      <Looper
        list={['xsmall', 'small', 'medium', 'large', 'xlarge', '2xlarge', '3xlarge', '4xlarge', '5xlarge']}
        render={size => (
          <Rhythm key={size} m={2}>
            <Avatar actionable contrast initials="P" size={size} title={size} />
          </Rhythm>
        )}
      />
    </Rhythm>
  </Flex>
</Canvas>
