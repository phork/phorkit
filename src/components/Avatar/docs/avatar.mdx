---
name: Avatar
menu: Display
route: /components/Avatar
---

import { Playground, Props } from 'docz';
import { Flex } from 'components/Flex';
import { Rhythm } from 'components/Rhythm';
import { ColorLooper } from 'docs/helpers/ColorLooper';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Avatar, ColoredAvatar, StyledAvatar } from '../index';

<PageTitle title="Avatar" src="components/Avatar" />

## Image

<Playground>
  <ThemeWrapper>
    <Flex direction="row" alignItems="center" wrap>
      <Looper
        list={['xsmall', 'small', 'medium', 'large', 'xlarge', 'xxlarge', 'xxxlarge']}
        render={size => (
          <Rhythm m={2}>
            <Avatar key={`image-${size}`} imgSrc="/public/images/avatar.jpg" initials="EC" size={size} />
          </Rhythm>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

## Actionable image

<Playground>
  <ThemeWrapper>
    <Flex direction="row" alignItems="center" wrap>
      <Looper
        list={['xsmall', 'small', 'medium', 'large', 'xlarge', 'xxlarge', 'xxxlarge']}
        render={size => (
          <Rhythm m={2}>
            <Avatar
              key={`actionableImage-${size}`}
              actionable
              imgSrc="/public/images/avatar.jpg"
              initials="EC"
              size={size}
            />
          </Rhythm>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

## Initials

<Playground>
  <ThemeWrapper>
    <Flex direction="column">
      <div>
        <h3>Primary color</h3>
        <Flex direction="row" alignItems="center" wrap>
          <Rhythm m={2}>
            <Looper
              list={['xsmall', 'small', 'medium', 'large', 'xlarge', 'xxlarge', 'xxxlarge']}
              render={size => (
                <Rhythm m={2}>
                  <Avatar key={`primaryColorInitials-${size}`} color="primary" initials="EC" size={size} />
                </Rhythm>
              )}
            />
          </Rhythm>
        </Flex>
      </div>
      <div>
        <h3>Neutral color</h3>
        <Flex direction="row" alignItems="center" wrap>
          <Rhythm m={2}>
            <Looper
              list={['xsmall', 'small', 'medium', 'large', 'xlarge', 'xxlarge', 'xxxlarge']}
              render={size => (
                <Rhythm m={2}>
                  <Avatar key={`neutralColorInitials-${size}`} color="neutral" initials="EC" size={size} />
                </Rhythm>
              )}
            />
          </Rhythm>
        </Flex>
      </div>
    </Flex>
  </ThemeWrapper>
</Playground>

## Actionable initials

<Playground>
  <ThemeWrapper>
    <Flex direction="column">
      <div>
        <h3>Primary color</h3>
        <Flex direction="row" alignItems="center" wrap>
          <Looper
            list={['xsmall', 'small', 'medium', 'large', 'xlarge', 'xxlarge', 'xxxlarge']}
            render={size => (
              <Rhythm m={2}>
                <Avatar
                  key={`primaryColorActionableInitials-${size}`}
                  actionable
                  color="primary"
                  initials="EC"
                  size={size}
                />
              </Rhythm>
            )}
          />
        </Flex>
      </div>
      <div>
        <h3>Neutral color</h3>
        <Flex direction="row" alignItems="center" wrap>
          <Looper
            list={['xsmall', 'small', 'medium', 'large', 'xlarge', 'xxlarge', 'xxxlarge']}
            render={size => (
              <Rhythm m={2}>
                <Avatar
                  key={`neutralColorActionableInitials-${size}`}
                  actionable
                  color="neutral"
                  initials="EC"
                  size={size}
                />
              </Rhythm>
            )}
          />
        </Flex>
      </div>
    </Flex>
  </ThemeWrapper>
</Playground>

## Variations

### Contrast initials

<Playground>
  <ThemeWrapper contrast>
    <Flex direction="row" alignItems="center" wrap>
      <Looper
        list={['xsmall', 'small', 'medium', 'large', 'xlarge', 'xxlarge', 'xxxlarge']}
        render={size => (
          <Rhythm m={2}>
            <Avatar key={`contrastActionableInitials-${size}`} actionable contrast initials="EC" size={size} />
          </Rhythm>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

### Colored initials

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Rhythm my={2}>
        <Flex direction="column">
          <ColorLooper
            themeId={themeId}
            group="primary"
            render={({ id }) => (
              <Flex direction="row" alignItems="center" key={`coloredInitials-${id}`} wrap>
                <Looper
                  list={['xsmall', 'small', 'medium', 'large', 'xlarge', 'xxlarge', 'xxxlarge']}
                  render={size => (
                    <Rhythm m={2}>
                      <ColoredAvatar
                        key={`coloredInitials-${id}-${size}`}
                        actionable
                        colorId={id}
                        initials="EC"
                        size={size}
                      />
                    </Rhythm>
                  )}
                />
              </Flex>
            )}
          />
        </Flex>
      </Rhythm>
    )}
  </ThemeWrapper>
</Playground>

### Styled initials

<Playground>
  <ThemeWrapper>
    <Flex direction="row" alignItems="center" wrap>
      <Looper
        list={['xsmall', 'small', 'medium', 'large', 'xlarge', 'xxlarge', 'xxxlarge']}
        render={size => (
          <Rhythm m={2}>
            <StyledAvatar
              key={`styledInitials-${size}`}
              actionable
              backgroundColor="#556270"
              textColor="#fff"
              initials="EC"
              size={size}
            />
          </Rhythm>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

### Custom size

<Playground>
  <ThemeWrapper>
    <StyledAvatar
      actionable
      color="primary"
      initials="EC"
      size="custom"
      style={{ '--avatar-size': '72px', '--avatar-font-size': '24px' }}
    />
  </ThemeWrapper>
</Playground>

## \[props\]

### Avatar

<Props of={Avatar} />

### ColoredAvatar

<Props of={ColoredAvatar} />

### StyledAvatar

<Props of={StyledAvatar} />
