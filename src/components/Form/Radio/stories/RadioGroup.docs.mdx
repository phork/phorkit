import { action } from '@storybook/addon-actions';
import { ArgsTable, Description, Primary, Stories, Subtitle, PRIMARY_STORY } from '@storybook/addon-docs';
import { Rhythm } from 'components/Rhythm';
import { Looper } from 'docs/helpers/Looper';
import { Canvas } from 'stories/helpers/Canvas';
import { PageTitle } from 'stories/helpers/PageTitle';
import { ThemeWrapper } from 'stories/helpers/ThemeWrapper';
import { FormComponentDemo } from '../../stories/helpers/FormComponentDemo';
import { NotifiedRadioGroup } from '../NotifiedRadioGroup';
import { RadioGroup } from '../RadioGroup';
import { radios } from './helpers/options';

<PageTitle src="components/Form/Radio" title="RadioGroup" />
<Subtitle />
<Description />
<Primary />
<ArgsTable story={PRIMARY_STORY} />
<Stories />

### Contrast

<Canvas contrast>
  <ThemeWrapper>
    <FormComponentDemo initialValue="one" property="value" type="radio">
      <RadioGroup contrast legend="Super fantastic label" radios={radios} value="one" />
    </FormComponentDemo>
  </ThemeWrapper>
</Canvas>

## Notifications

<Canvas>
  <Looper
    list={['success', 'danger', 'warning', 'primary', 'neutral']}
    render={level => (
      <Rhythm grouped mb={4}>
        <FormComponentDemo initialValue="one" property="value" type="radio">
          <NotifiedRadioGroup
            layout="inline"
            legend="Super fantastic legend"
            level={level}
            notification={`This is an example ${level} notification.`}
            onChange={action('changed')}
            radios={radios}
            value="one"
          />
        </FormComponentDemo>
      </Rhythm>
    )}
  />
</Canvas>
