---
name: Radio
menu: Forms
route: /components/FormRadio
---

import { Playground, Props } from 'docz';
import { Flex } from 'components/Flex';
import { Rhythm } from 'components/Rhythm';
import { Typography } from 'components/Typography';
import { FormComponentDemo } from '../../docs/helpers/FormComponentDemo';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Radio, RadioGroup, NotifiedRadioGroup } from '../index';
import { radioGroup, logValue } from './helpers/utils';

<PageTitle title="Radio" src="components/Form/Radio" />

## Basic radio button

<Playground>
  <ThemeWrapper>
    <Flex direction="row" wrap>
      <Rhythm m={2}>
        <Flex direction="column">
          <Rhythm my={2}>
            <Radio name="basic" onChange={logValue} checked />
            <Radio name="basic" onChange={logValue} />
            <Radio name="basic" onChange={logValue} checked>
              Primary checked
            </Radio>
            <Radio name="basic" onChange={logValue}>
              Primary unchecked
            </Radio>
            <Radio name="basic" onChange={logValue} disabled checked>
              Primary checked disabled
            </Radio>
            <Radio name="basic" onChange={logValue} disabled>
              Primary unchecked disabled
            </Radio>
          </Rhythm>
        </Flex>
        <Flex direction="column">
          <Rhythm my={2}>
            <Radio name="basic" onChange={logValue} checked variant="secondary" />
            <Radio name="basic" onChange={logValue} variant="secondary" />
            <Radio name="basic" onChange={logValue} checked variant="secondary">
              Secondary checked
            </Radio>
            <Radio name="basic" onChange={logValue} variant="secondary">
              Secondary unchecked
            </Radio>
            <Radio name="basic" onChange={logValue} disabled checked variant="secondary">
              Secondary checked disabled
            </Radio>
            <Radio name="basic" onChange={logValue} disabled variant="secondary">
              Secondary unchecked disabled
            </Radio>
          </Rhythm>
        </Flex>
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

## Full width labeled radio button

<Playground>
  <ThemeWrapper>
    <Flex direction="column" style={{ width: 240 }}>
      <Rhythm my={2}>
        <Radio name="labeled-radio" onChange={logValue} full checked>
          Super fantastic label
        </Radio>
        <Radio name="labeled-radio" onChange={logValue} full reverse>
          Super fantastic label
        </Radio>
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

## Variations

### Danger radio button

<Playground>
  <ThemeWrapper>
    <Flex direction="row" wrap>
      <Rhythm m={2}>
        <Flex direction="column">
          <Rhythm my={2}>
            <Radio name="danger" onChange={logValue} checked validity="danger">
              Primary checked
            </Radio>
            <Radio name="danger" onChange={logValue} validity="danger">
              Primary unchecked
            </Radio>
            <Radio name="danger" onChange={logValue} disabled checked validity="danger">
              Primary checked disabled
            </Radio>
            <Radio name="danger" onChange={logValue} disabled validity="danger">
              Primary unchecked disabled
            </Radio>
          </Rhythm>
        </Flex>
        <Flex direction="column">
          <Rhythm my={2}>
            <Radio name="danger" onChange={logValue} checked validity="danger" variant="secondary">
              Secondary checked
            </Radio>
            <Radio name="danger" onChange={logValue} validity="danger" variant="secondary">
              Secondary unchecked
            </Radio>
            <Radio name="danger" onChange={logValue} disabled checked validity="danger" variant="secondary">
              Secondary checked disabled
            </Radio>
            <Radio name="danger" onChange={logValue} disabled validity="danger" variant="secondary">
              Secondary unchecked disabled
            </Radio>
          </Rhythm>
        </Flex>
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

### Contrast radio button

<Playground>
  <ThemeWrapper contrast>
    <Flex direction="row" wrap>
      <Rhythm m={2}>
        <Flex direction="column">
          <Rhythm my={2}>
            <Radio name="contrast" onChange={logValue} checked contrast>
              Primary checked
            </Radio>
            <Radio name="contrast" onChange={logValue} contrast>
              Primary unchecked
            </Radio>
            <Radio name="contrast" onChange={logValue} disabled checked contrast>
              Primary checked disabled
            </Radio>
            <Radio name="contrast" onChange={logValue} disabled contrast>
              Primary unchecked disabled
            </Radio>
          </Rhythm>
        </Flex>
        <Flex direction="column">
          <Rhythm my={2}>
            <Radio name="contrast" onChange={logValue} checked contrast variant="secondary">
              Secondary checked
            </Radio>
            <Radio name="contrast" onChange={logValue} contrast variant="secondary">
              Secondary unchecked
            </Radio>
            <Radio name="contrast" onChange={logValue} disabled checked contrast variant="secondary">
              Secondary checked disabled
            </Radio>
            <Radio name="contrast" onChange={logValue} disabled contrast variant="secondary">
              Secondary unchecked disabled
            </Radio>
          </Rhythm>
        </Flex>
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

# <br />

# RadioGroup

## Example `radios` values

```
[{
  id: "radio-group-item1",
  value: "one",
  label: 'Super fantastic label one',
}, {
  id: "radio-group-item2",
  value: "two",
  label: 'Super fantastic label two',
}, {
  id: "radio-group-item3",
  value: "three",
  label: 'Super fantastic label three',
}]
```

## Stacked group

<Playground>
  <FormComponentDemo type="radio" property="value" initialValue="one">
    <RadioGroup
      layout="stacked"
      legend="Super fantastic label says what?"
      name="stacked-radio-group"
      onChange={logValue}
      radios={radioGroup}
    />
  </FormComponentDemo>
</Playground>

## Inline group

<Playground>
  <FormComponentDemo type="radio" property="value" initialValue="one">
    <RadioGroup
      layout="inline"
      legend="Super fantastic label says what?"
      name="inline-radio-group"
      onChange={logValue}
      radios={radioGroup}
    />
  </FormComponentDemo>
</Playground>

## Inline secondary group

<Playground>
  <FormComponentDemo type="radio" property="value" initialValue="one">
    <RadioGroup
      layout="inline"
      legend="Super fantastic label says what?"
      name="inline-secondary-radio-group"
      onChange={logValue}
      radios={radioGroup}
      variant="secondary"
    />
  </FormComponentDemo>
</Playground>

## Custom label

<Playground>
  <FormComponentDemo type="radio" property="value" initialValue="one">
    <RadioGroup
      layout="inline"
      legend={
        <Typography as="legend" variants="medium-caps" style={{ marginBottom: '8px' }}>
          Super fantastic legend says what?
        </Typography>
      }
      name="custom-radio-group"
      onChange={logValue}
      radios={radioGroup}
    />
  </FormComponentDemo>
</Playground>

## Notifications

<Playground>
  <Looper
    list={['success', 'danger', 'warning', 'primary', 'neutral']}
    render={level => (
      <FormComponentDemo type="radio" property="value" initialValue="one">
        <NotifiedRadioGroup
          layout="inline"
          legend="Super fantastic label says what?"
          name={`notified-radio-group-${level}`}
          onChange={logValue}
          radios={radioGroup}
          level={level}
          notification={`This is an example ${level} notification.`}
        />
      </FormComponentDemo>
    )}
  />
</Playground>

## \[props\]

### Radio

<Props of={Radio} />

### RadioGroup

<Props of={RadioGroup} />
