---
name: Radio
menu: Forms
route: /components/FormRadio
---

import { Playground, Props } from 'docz';
import { Flex } from 'components/Flex';
import { Looper } from 'components/Looper';
import { Rhythm } from 'components/Rhythm';
import { Typography } from 'components/Typography';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { FormComponentDemo } from '../../docs/helpers/FormComponentDemo';
import { Radio, RadioGroup, NotifiedRadioGroup } from '../index';
import { sizes } from './helpers/sizes';
import { radioGroup, logValue } from './helpers/utils';

<PageTitle src="components/Form/Radio" title="Radio" />

## Basic radio button

<Playground>
  <ThemeWrapper>
    <Flex wrap direction="row">
      <Rhythm m={2}>
        <Flex direction="column">
          <Rhythm my={2}>
            <Radio checked name="basic" onChange={logValue} />
            <Radio name="basic" onChange={logValue} />
            <Radio checked name="basic" onChange={logValue}>
              Primary checked
            </Radio>
            <Radio name="basic" onChange={logValue}>
              Primary unchecked
            </Radio>
            <Radio checked disabled name="basic" onChange={logValue}>
              Primary checked disabled
            </Radio>
            <Radio disabled name="basic" onChange={logValue}>
              Primary unchecked disabled
            </Radio>
          </Rhythm>
        </Flex>
        <Flex direction="column">
          <Rhythm my={2}>
            <Radio checked name="basic" onChange={logValue} variant="secondary" />
            <Radio name="basic" onChange={logValue} variant="secondary" />
            <Radio checked name="basic" onChange={logValue} variant="secondary">
              Secondary checked
            </Radio>
            <Radio name="basic" onChange={logValue} variant="secondary">
              Secondary unchecked
            </Radio>
            <Radio checked disabled name="basic" onChange={logValue} variant="secondary">
              Secondary checked disabled
            </Radio>
            <Radio disabled name="basic" onChange={logValue} variant="secondary">
              Secondary unchecked disabled
            </Radio>
          </Rhythm>
        </Flex>
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

## Full width labeled radio button

<Playground>
  <ThemeWrapper>
    <Flex direction="column" style={{ width: 240 }}>
      <Rhythm my={2}>
        <Radio checked full name="labeled-radio" onChange={logValue}>
          Super fantastic label
        </Radio>
        <Radio full reverse name="labeled-radio" onChange={logValue}>
          Super fantastic label
        </Radio>
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

## Variations

### Danger radio button

<Playground>
  <ThemeWrapper>
    <Flex wrap direction="row">
      <Rhythm m={2}>
        <Flex direction="column">
          <Rhythm my={2}>
            <Radio checked name="danger" onChange={logValue} validity="danger">
              Primary checked
            </Radio>
            <Radio name="danger" onChange={logValue} validity="danger">
              Primary unchecked
            </Radio>
            <Radio checked disabled name="danger" onChange={logValue} validity="danger">
              Primary checked disabled
            </Radio>
            <Radio disabled name="danger" onChange={logValue} validity="danger">
              Primary unchecked disabled
            </Radio>
          </Rhythm>
        </Flex>
        <Flex direction="column">
          <Rhythm my={2}>
            <Radio checked name="danger" onChange={logValue} validity="danger" variant="secondary">
              Secondary checked
            </Radio>
            <Radio name="danger" onChange={logValue} validity="danger" variant="secondary">
              Secondary unchecked
            </Radio>
            <Radio checked disabled name="danger" onChange={logValue} validity="danger" variant="secondary">
              Secondary checked disabled
            </Radio>
            <Radio disabled name="danger" onChange={logValue} validity="danger" variant="secondary">
              Secondary unchecked disabled
            </Radio>
          </Rhythm>
        </Flex>
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

### Contrast radio button

<Playground>
  <ThemeWrapper contrast>
    <Flex wrap direction="row">
      <Rhythm m={2}>
        <Flex direction="column">
          <Rhythm my={2}>
            <Radio checked contrast name="contrast" onChange={logValue}>
              Primary checked
            </Radio>
            <Radio contrast name="contrast" onChange={logValue}>
              Primary unchecked
            </Radio>
            <Radio checked contrast disabled name="contrast" onChange={logValue}>
              Primary checked disabled
            </Radio>
            <Radio contrast disabled name="contrast" onChange={logValue}>
              Primary unchecked disabled
            </Radio>
          </Rhythm>
        </Flex>
        <Flex direction="column">
          <Rhythm my={2}>
            <Radio checked contrast name="contrast" onChange={logValue} variant="secondary">
              Secondary checked
            </Radio>
            <Radio contrast name="contrast" onChange={logValue} variant="secondary">
              Secondary unchecked
            </Radio>
            <Radio checked contrast disabled name="contrast" onChange={logValue} variant="secondary">
              Secondary checked disabled
            </Radio>
            <Radio contrast disabled name="contrast" onChange={logValue} variant="secondary">
              Secondary unchecked disabled
            </Radio>
          </Rhythm>
        </Flex>
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

# <br />

# RadioGroup

## Example `radios` values

```
[{
  id: "radio-group-item1",
  value: "one",
  label: 'Super fantastic label one',
}, {
  id: "radio-group-item2",
  value: "two",
  label: 'Super fantastic label two',
}, {
  id: "radio-group-item3",
  value: "three",
  label: 'Super fantastic label three',
}]
```

## Stacked group

<Playground>
  <FormComponentDemo initialValue="one" property="value" type="radio">
    <RadioGroup
      layout="stacked"
      legend="Super fantastic label"
      name="stacked-radio-group"
      onChange={logValue}
      radios={radioGroup}
    />
  </FormComponentDemo>
</Playground>

## Inline group

<Playground>
  <FormComponentDemo initialValue="one" property="value" type="radio">
    <RadioGroup
      layout="inline"
      legend="Super fantastic label"
      name="inline-radio-group"
      onChange={logValue}
      radios={radioGroup}
    />
  </FormComponentDemo>
</Playground>

## Inline secondary group

<Playground>
  <FormComponentDemo initialValue="one" property="value" type="radio">
    <RadioGroup
      layout="inline"
      legend="Super fantastic label"
      name="inline-secondary-radio-group"
      onChange={logValue}
      radios={radioGroup}
      variant="secondary"
    />
  </FormComponentDemo>
</Playground>

## Custom group legend

<Playground>
  <FormComponentDemo initialValue="one" property="value" type="radio">
    <RadioGroup
      layout="inline"
      legend={
        <Typography as="legend" style={{ marginBottom: '8px' }} variants="medium-caps">
          Super fantastic legend
        </Typography>
      }
      name="custom-radio-group"
      onChange={logValue}
      radios={radioGroup}
    />
  </FormComponentDemo>
</Playground>

## No group legend

<Playground>
  <FormComponentDemo initialValue="one" property="value" type="radio">
    <RadioGroup layout="stacked" name="unlabeled-radio-group" onChange={logValue} radios={radioGroup} />
  </FormComponentDemo>
</Playground>

## Notifications

<Playground>
  <Looper
    list={['danger', 'warning', 'success', 'primary', 'neutral']}
    render={level => (
      <FormComponentDemo initialValue="one" property="value" type="radio">
        <NotifiedRadioGroup
          layout="inline"
          legend="Super fantastic label"
          level={level}
          name={`notified-radio-group-${level}`}
          notification={`This is an example ${level} notification.`}
          onChange={logValue}
          radios={radioGroup}
        />
      </FormComponentDemo>
    )}
  />
</Playground>

## Sizes

<Playground>
  <ThemeWrapper>
    <Looper
      list={sizes}
      render={size => (
        <Rhythm grouped py={3}>
          <Radio checked onChange={logValue} size={size}>
            Lorem ipsum dolor sit amet
          </Radio>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## \[props\]

### Radio

<Props of={Radio} />

### RadioGroup

<Props of={RadioGroup} />
