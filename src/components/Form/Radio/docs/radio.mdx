---
name: Radio
menu: Forms
route: /components/FormRadio
---

import { Playground, Props } from 'docz';
import { Typography } from 'components/Typography';
import { FormComponentDemo } from '../../docs/helpers/FormComponentDemo';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { Radio, RadioGroup, NotifiedRadioGroup } from '../index';
import { radioGroup, logValue } from './helpers/utils';

<PageTitle title="Radio" src="components/Form/Radio" />

## Basic radio button

<Playground>
  <FormComponentDemo property="checked" initialValue={false}>
    <Radio name="basic-radio" id="demo-basic-radio" value="basic" onChange={logValue} />
  </FormComponentDemo>
</Playground>

## Labeled radio button

<Playground>
  <FormComponentDemo property="checked" initialValue={true}>
    <Radio name="labeled-radio" id="demo-labeled-radio" value="labeled" onChange={logValue}>
      Super fantastic label
    </Radio>
  </FormComponentDemo>
  <FormComponentDemo property="checked" initialValue={true}>
    <Radio name="labeled-reverse-radio" id="demo-labeled-reverse-radio" onChange={logValue} reverse>
      Super fantastic label
    </Radio>
  </FormComponentDemo>
</Playground>

## Full width labeled radio button

<Playground>
  <FormComponentDemo property="checked" initialValue={true} style={{ width: 240 }}>
    <Radio name="labeled-radio" id="demo-labeled-radio" value="labeled" onChange={logValue} full>
      Super fantastic label
    </Radio>
  </FormComponentDemo>
  <FormComponentDemo property="checked" initialValue={true} style={{ width: 240 }}>
    <Radio name="labeled-reverse-radio" id="demo-labeled-reverse-radio" onChange={logValue} full reverse>
      Super fantastic label
    </Radio>
  </FormComponentDemo>
</Playground>

## Disabled radio button

<Playground>
  <Radio checked disabled name="disabled-radio" id="demo-disabled-checked-radio" value="disabled">
    Super fantastic label
  </Radio>
  <br />
  <br />
  <Radio disabled name="disabled-radio" id="demo-disabled-unchecked-radio" value="disabled">
    Super fantastic label
  </Radio>
</Playground>

## Contrast radio button

<Playground>
  <FormComponentDemo property="checked" initialValue={false} contrast>
    <Radio contrast name="contrast-radio" id="contrast-basic-radio" onChange={logValue}>
      Super fantastic label
    </Radio>
  </FormComponentDemo>
</Playground>

# <br />

# RadioGroup

## Example `radios` values

```
[{
  id: "radio-group-item1",
  value: "one",
  label: 'Super fantastic label one',
}, {
  id: "radio-group-item2",
  value: "two",
  label: 'Super fantastic label two',
}, {
  id: "radio-group-item3",
  value: "three",
  label: 'Super fantastic label three',
}]
```

## Stacked group

<Playground>
  <FormComponentDemo property="value" initialValue="one">
    <RadioGroup
      layout="stacked"
      legend="Super fantastic label says what?"
      name="stacked-radio-group"
      onChange={logValue}
      radios={radioGroup}
    />
  </FormComponentDemo>
</Playground>

## Inline group

<Playground>
  <FormComponentDemo property="value" initialValue="one">
    <RadioGroup
      layout="inline"
      legend="Super fantastic label says what?"
      name="inline-radio-group"
      onChange={logValue}
      radios={radioGroup}
    />
  </FormComponentDemo>
</Playground>

## Custom label

<Playground>
  <FormComponentDemo property="value" initialValue="one">
    <RadioGroup
      layout="inline"
      legend={
        <Typography as="legend" variants="medium-caps" style={{ marginBottom: '8px' }}>
          Super fantastic legend says what?
        </Typography>
      }
      name="custom-radio-group"
      onChange={logValue}
      radios={radioGroup}
    />
  </FormComponentDemo>
</Playground>

## Notifications

<Playground>
  <Looper
    list={['success', 'danger', 'warning', 'primary', 'neutral']}
    render={level => (
      <FormComponentDemo property="value" initialValue="one">
        <NotifiedRadioGroup
          layout="inline"
          legend="Super fantastic label says what?"
          name={`notified-radio-group-${level}`}
          onChange={logValue}
          radios={radioGroup}
          level={level}
          notification={`This is an example ${level} notification.`}
        />
      </FormComponentDemo>
    )}
  />
</Playground>

## \[props\]

### Radio

<Props of={Radio} />

### RadioGroup

<Props of={RadioGroup} />
