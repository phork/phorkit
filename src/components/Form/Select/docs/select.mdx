---
name: Select
menu: Forms
route: /components/FormSelect
---

import { Playground, Props } from 'docz';
import { SearchIcon } from 'icons/SearchIcon';
import { TimesIcon } from 'icons/TimesIcon';
import { Looper } from 'components/Looper';
import { Rhythm } from 'components/Rhythm';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { FormComponentDemo } from '../../docs/helpers/FormComponentDemo';
import { Select, NotifiedSelect } from '../index';
import { options } from './helpers/options';
import { variants, sizes } from './helpers/variants';

<PageTitle src="components/Form/Select" title="Select" />

## Basic select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="yellow" property="value" type="select">
            <Select onChange={(event, value) => console.log(value)} options={options} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Multiple select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue={['yellow', 'blue']} property="values" type="select">
            <Select
              multiple
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="yellow" property="value" type="select">
            <Select
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Transitional labeled select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped property="value" type="select">
            <Select
              transitional
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Transitional labeled select with icons

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped property="value" type="select">
            <Select
              transitional
              iconAfter={<TimesIcon size={8} />}
              iconBefore={SearchIcon}
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Select with placeholder

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped property="value" type="select">
            <Select
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              placeholder={{ label: 'Choose a color' }}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Select with separate list and view placeholder

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped property="value" type="select">
            <Select
              transitional
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              placeholder={{ optionLabel: '(None)', label: 'Choose a color' }}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Select with list placeholder only

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped property="value" type="select">
            <Select
              transitional
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              placeholder={{ optionLabel: '(None)', label: undefined }}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Select with disabled placeholder

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped property="value" type="select">
            <Select
              transitional
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              placeholder={{ label: 'Choose a color', disabled: true }}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Disabled select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="yellow" property="value" type="select">
            <Select
              disabled
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Read only select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="yellow" property="value" type="select">
            <Select
              readOnly
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Read only multiple select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue={['yellow', 'blue']} property="values" type="select">
            <Select
              multiple
              readOnly
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Danger select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="yellow" property="value" type="select">
            <Select
              transitional
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              validity="danger"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Warning select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="yellow" property="value" type="select">
            <Select
              transitional
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              validity="warning"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Success select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="yellow" property="value" type="select">
            <Select
              transitional
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              validity="success"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Contrast select

<Playground>
  <ThemeWrapper contrast>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo contrast unwrapped initialValue="yellow" property="value" type="select">
            <Select
              contrast
              id="demo-contrast-select"
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Notifications

<Playground>
  <ThemeWrapper>
    <Looper
      list={['danger', 'warning', 'success', 'primary', 'neutral']}
      render={level => (
        <Looper
          list={variants}
          render={variant => (
            <Rhythm py={3}>
              <FormComponentDemo unwrapped initialValue="yellow" property="value" type="select">
                <NotifiedSelect
                  label="Super fantastic label"
                  level={level}
                  notification={`This is an example ${level} notification.`}
                  onChange={(event, value) => console.log(value)}
                  options={options}
                  variant={variant}
                />
              </FormComponentDemo>
            </Rhythm>
          )}
        />
      )}
    />
  </ThemeWrapper>
</Playground>

## Sizes

<Playground>
  <ThemeWrapper>
    <Looper
      list={sizes}
      render={size => (
        <Rhythm grouped my={3}>
          <Looper
            list={variants}
            render={variant => (
              <Rhythm key={variant} py={3}>
                <FormComponentDemo unwrapped property="value" type="select">
                  <Select
                    transitional
                    iconBefore={SearchIcon}
                    label={`Size ${size}`}
                    onChange={(event, value) => console.log(value)}
                    options={options}
                    size={size}
                    variant={variant}
                  />
                </FormComponentDemo>
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={Select} />
