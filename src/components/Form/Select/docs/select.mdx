---
name: Select
menu: Forms
route: /components/FormSelect
---

import { Playground, Props } from 'docz';
import { SearchIcon } from 'icons/SearchIcon';
import { TimesIcon } from 'icons/TimesIcon';
import { TagIcon } from 'icons/internal/TagIcon';
import { Rhythm } from 'components/Rhythm';
import { FormComponentDemo } from '../../docs/helpers/FormComponentDemo';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Select, NotifiedSelect } from '../index';
import { options } from './helpers/options';
import { variants, sizes } from './helpers/variants';

<PageTitle title="Select" src="components/Form/Select" />

## Basic select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" initialValue="yellow" unwrapped>
            <Select onChange={(event, value) => console.log(value)} options={options} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Multiple select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="values" initialValue={['yellow', 'blue']} unwrapped>
            <Select
              label="Super fantastic label"
              multiple
              onChange={(event, value) => console.log(value)}
              options={options}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" initialValue="yellow" unwrapped>
            <Select
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Transitional labeled select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" unwrapped>
            <Select
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              transitional
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Transitional labeled select with icons

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" unwrapped>
            <Select
              iconBefore={SearchIcon}
              iconAfter={<TimesIcon size={8} />}
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              transitional
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Select with placeholder

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" unwrapped>
            <Select
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              placeholder={{ label: 'Choose a color' }}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Select with separate list and view placeholder

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" unwrapped>
            <Select
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              placeholder={{ optionLabel: '(None)', label: 'Choose a color' }}
              transitional
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Select with list placeholder only

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" unwrapped>
            <Select
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              placeholder={{ optionLabel: '(None)', label: undefined }}
              transitional
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Select with disabled placeholder

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" unwrapped>
            <Select
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              placeholder={{ label: 'Choose a color', disabled: true }}
              transitional
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Disabled select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" initialValue="yellow" unwrapped>
            <Select
              disabled
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Read only select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" initialValue="yellow" unwrapped>
            <Select
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              readOnly
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Read only multiple select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="values" initialValue={['yellow', 'blue']} unwrapped>
            <Select
              label="Super fantastic label"
              multiple
              onChange={(event, value) => console.log(value)}
              options={options}
              readOnly
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Danger select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" initialValue="yellow" unwrapped>
            <Select
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              transitional
              validity="danger"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Warning select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" initialValue="yellow" unwrapped>
            <Select
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              transitional
              validity="warning"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Success select

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" initialValue="yellow" unwrapped>
            <Select
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              transitional
              validity="success"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Contrast select

<Playground>
  <ThemeWrapper contrast>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="select" property="value" initialValue="yellow" contrast unwrapped>
            <Select
              contrast
              id="demo-contrast-select"
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              options={options}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Notifications

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Looper
          list={['success', 'danger', 'warning', 'primary', 'neutral']}
          render={level => (
            <Rhythm py={3}>
              <FormComponentDemo type="select" property="value" initialValue="yellow" unwrapped>
                <NotifiedSelect
                  label="Super fantastic label"
                  onChange={(event, value) => console.log(value)}
                  options={options}
                  variant={variant}
                  level={level}
                  notification={`This is an example ${level} notification.`}
                />
              </FormComponentDemo>
            </Rhythm>
          )}
        />
      )}
    />
  </ThemeWrapper>
</Playground>

## Sizes

<Playground>
  <ThemeWrapper>
    <Looper
      list={sizes}
      render={size => (
        <Rhythm my={3} grouped>
          <Looper
            list={variants}
            render={variant => (
              <Rhythm py={3}>
                <FormComponentDemo type="select" property="value" key={variant} unwrapped>
                  <Select
                    iconBefore={SearchIcon}
                    label={`Size ${size}`}
                    onChange={(event, value) => console.log(value)}
                    options={options}
                    size={size}
                    transitional
                    variant={variant}
                  />
                </FormComponentDemo>
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={Select} />
