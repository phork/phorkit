import { action } from '@storybook/addon-actions';
import { ArgsTable, Description, Primary, Stories, Subtitle, PRIMARY_STORY } from '@storybook/addon-docs';
import { Rhythm } from 'components/Rhythm';
import { Looper } from 'docs/helpers/Looper';
import { Canvas } from 'stories/helpers/Canvas';
import { PageTitle } from 'stories/helpers/PageTitle';
import { ThemeWrapper } from 'stories/helpers/ThemeWrapper';
import { CheckboxGroup } from '../CheckboxGroup';
import { NotifiedCheckboxGroup } from '../NotifiedCheckboxGroup';

export const checkboxGroup = [
  {
    id: 'checkbox-group-item1',
    name: 'checkbox-group-item1',
    value: 'one',
    label: 'Super fantastic label one',
  },
  {
    id: 'checkbox-group-item2',
    name: 'checkbox-group-item2',
    value: 'two',
    label: 'Super fantastic label two',
  },
  {
    id: 'checkbox-group-item3',
    name: 'checkbox-group-item3',
    value: 'three',
    label: 'Super fantastic label three',
  },
];

<PageTitle src="components/CheckboxGroup" title="CheckboxGroup" />
<Subtitle />
<Description />
<Primary />
<ArgsTable story={PRIMARY_STORY} />
<Stories />

### Contrast

<Canvas contrast>
  <ThemeWrapper>
    <CheckboxGroup contrast checkboxes={checkboxGroup} legend="Super fantastic label" values={['one']} />
  </ThemeWrapper>
</Canvas>

## Notifications

<Canvas>
  <Looper
    list={['success', 'danger', 'warning', 'primary', 'neutral']}
    render={level => (
      <Rhythm grouped mb={4} style={{ maxWidth: 400 }}>
        <NotifiedCheckboxGroup
          checkboxes={checkboxGroup}
          layout="inline"
          legend="Super fantastic legend"
          level={level}
          notification={`This is an example ${level} notification.`}
          values={['one']}
        />
      </Rhythm>
    )}
  />
</Canvas>
