import { action } from '@storybook/addon-actions';
import { ArgsTable, Description, Primary, Stories, Subtitle, PRIMARY_STORY } from '@storybook/addon-docs';
import { Looper } from 'components/Looper';
import { Rhythm } from 'components/Rhythm';
import { Canvas } from 'stories/helpers/Canvas';
import { PageTitle } from 'stories/helpers/PageTitle';
import { FormComponentDemo } from '../../stories/helpers/FormComponentDemo';
import { CheckboxGroup } from '../CheckboxGroup';
import { NotifiedCheckboxGroup } from '../NotifiedCheckboxGroup';
import { checkboxes } from './helpers/options';

<PageTitle src="components/Form/Checkbox" title="CheckboxGroup" />
<Subtitle />
<Description />
<Primary />
<ArgsTable story={PRIMARY_STORY} />
<Stories />

### Contrast

<Canvas contrast>
  <FormComponentDemo initialValue={['one']} property="values" type="checkbox">
    <CheckboxGroup contrast checkboxes={checkboxes} legend="Super fantastic label" values={['one']} />
  </FormComponentDemo>
</Canvas>

## Notifications

<Canvas>
  <Looper
    list={['danger', 'warning', 'success', 'primary', 'neutral']}
    render={level => (
      <Rhythm grouped mb={4}>
        <FormComponentDemo initialValue={['one']} property="values" type="checkbox">
          <NotifiedCheckboxGroup
            checkboxes={checkboxes}
            layout="inline"
            legend="Super fantastic legend"
            level={level}
            notification={`This is an example ${level} notification.`}
            onChange={action('changed')}
            values={['one']}
          />
        </FormComponentDemo>
      </Rhythm>
    )}
  />
</Canvas>
