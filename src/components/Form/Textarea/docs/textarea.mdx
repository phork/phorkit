---
name: Textarea
menu: Forms
route: /components/FormTextarea
---

import { Playground, Props } from 'docz';
import { Rhythm } from 'components/Rhythm';
import { FormComponentDemo } from '../../docs/helpers/FormComponentDemo';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Textarea, NotifiedTextarea } from '../index';
import { variants } from './helpers/variants';

<PageTitle title="Textarea" src="components/Form/Textarea" />

## Basic textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" unwrapped>
            <Textarea variant={variant} onChange={(event, value) => console.log(value)} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Textarea with rows and cols

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" unwrapped>
            <Textarea variant={variant} rows={2} cols={12} onChange={(event, value) => console.log(value)} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Textarea with max length

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" unwrapped>
            <Textarea variant={variant} maxLength={12} onChange={(event, value) => console.log(value)} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" unwrapped>
            <Textarea variant={variant} label="Super fantastic label" onChange={(event, value) => console.log(value)} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Transitional labeled textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" unwrapped>
            <Textarea
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              transitional
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled textarea with placeholder

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" unwrapped>
            <Textarea
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              placeholder="Hello world"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Disabled textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" unwrapped>
            <Textarea
              disabled
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Read only textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" unwrapped>
            <Textarea
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              readOnly
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Danger textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" unwrapped>
            <Textarea
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              transitional
              validity="danger"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Warning textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" unwrapped>
            <Textarea
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              transitional
              validity="warning"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Success textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" unwrapped>
            <Textarea
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              transitional
              validity="success"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Contrast textarea

<Playground>
  <ThemeWrapper contrast>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" contrast unwrapped>
            <Textarea
              contrast
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              transitional
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Notifications

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Looper
          list={['success', 'danger', 'warning', 'primary', 'neutral']}
          render={level => (
            <Rhythm py={3}>
              <FormComponentDemo property="value" initialValue="Hello world" unwrapped>
                <NotifiedTextarea
                  level={level}
                  notification={`This is an example ${level} notification.`}
                  onChange={(event, value) => console.log(value)}
                  variant={variant}
                />
              </FormComponentDemo>
            </Rhythm>
          )}
        />
      )}
    />
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={Textarea} />
