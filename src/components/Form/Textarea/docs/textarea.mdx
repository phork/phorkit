---
name: Textarea
menu: Forms
route: /components/FormTextarea
---

import { Playground, Props } from 'docz';
import { Looper } from 'components/Looper';
import { Rhythm } from 'components/Rhythm';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { FormComponentDemo } from '../../docs/helpers/FormComponentDemo';
import { Textarea, NotifiedTextarea } from '../index';
import { variants, sizes } from './helpers/variants';

<PageTitle src="components/Form/Textarea" title="Textarea" />

## Basic textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="textarea">
            <Textarea onChange={(event, value) => console.log(value)} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Textarea with rows and cols

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="textarea">
            <Textarea cols={12} onChange={(event, value) => console.log(value)} rows={2} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Textarea with max length

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="textarea">
            <Textarea maxLength={12} onChange={(event, value) => console.log(value)} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="textarea">
            <Textarea label="Super fantastic label" onChange={(event, value) => console.log(value)} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Transitional labeled textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped property="value" type="textarea">
            <Textarea
              transitional
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled textarea with placeholder

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped property="value" type="textarea">
            <Textarea
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              placeholder="Hello world"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Disabled textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="textarea">
            <Textarea
              disabled
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Read only textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="textarea">
            <Textarea
              readOnly
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Danger textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="textarea">
            <Textarea
              transitional
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              validity="danger"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Warning textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="textarea">
            <Textarea
              transitional
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              validity="warning"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Success textarea

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="textarea">
            <Textarea
              transitional
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              validity="success"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Contrast textarea

<Playground>
  <ThemeWrapper contrast>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo contrast unwrapped initialValue="Hello world" property="value" type="textarea">
            <Textarea
              contrast
              transitional
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Notifications

<Playground>
  <ThemeWrapper>
    <Looper
      list={['danger', 'warning', 'success', 'primary', 'neutral']}
      render={level => (
        <Looper
          list={variants}
          render={variant => (
            <Rhythm py={3}>
              <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="textarea">
                <NotifiedTextarea
                  level={level}
                  notification={`This is an example ${level} notification.`}
                  onChange={(event, value) => console.log(value)}
                  variant={variant}
                />
              </FormComponentDemo>
            </Rhythm>
          )}
        />
      )}
    />
  </ThemeWrapper>
</Playground>

## Sizes

<Playground>
  <ThemeWrapper>
    <Looper
      list={sizes}
      render={size => (
        <Rhythm grouped my={3}>
          <Looper
            list={variants}
            render={variant => (
              <Rhythm key={variant} py={3}>
                <FormComponentDemo unwrapped property="value" type="textarea">
                  <Textarea
                    transitional
                    label={`Size ${size}`}
                    onChange={(event, value) => console.log(value)}
                    size={size}
                    variant={variant}
                  />
                </FormComponentDemo>
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={Textarea} />
