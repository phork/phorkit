---
name: Password
menu: Forms
route: /components/FormPassword
---

import { Playground, Props } from 'docz';
import { Password, NotifiedPassword } from '../index';
import { FormComponentDemo } from '../../docs/helpers/FormComponentDemo';
import { Looper } from 'docs/helpers/Looper';
import { variants } from './helpers/variants.js';
import { CheckIcon } from 'icons/internal/CheckIcon';
import { HelpIcon } from 'icons/internal/HelpIcon';
import { PageTitle } from 'docs/helpers/PageTitle';

<PageTitle title="Password" src="components/Form/Textbox" />

## Basic password

<Playground>
  <Looper
    list={variants}
    render={variant => (
      <FormComponentDemo property="value" initialValue="Hello world">
        <Password onChange={(event, value) => console.log(value)} variant={variant} />
      </FormComponentDemo>
    )}
  />
</Playground>

## Visible password

<Playground>
  <Looper
    list={variants}
    render={variant => (
      <FormComponentDemo property="value" initialValue="Hello world">
        <Password id={`demo-visible-password-${variant}`} initialType="text" onChange={(event, value) => console.log(value)} variant={variant} />
      </FormComponentDemo>
    )}
  />
</Playground>

## Labeled password

<Playground>
  <Looper
    list={variants}
    render={variant => (
      <FormComponentDemo property="value" initialValue="Hello world">
        <Password
          id={`demo-labeled-password-${variant}`}
          onChange={(event, value) => console.log(value)}
          label="Super fantastic label"
          variant={variant}
        />
      </FormComponentDemo>
    )}
  />
</Playground>

## Transitional labeled password

<Playground>
  <Looper
    list={variants}
    render={variant => (
      <FormComponentDemo property="value">
        <Password
          id={`demo-transitional-labeled-password-${variant}`}
          onChange={(event, value) => console.log(value)}
          transitional
          label="Super fantastic label"
          variant={variant}
        />
      </FormComponentDemo>
    )}
  />
</Playground>

## Disabled password

<Playground>
  <Looper
    list={variants}
    render={variant => (
      <FormComponentDemo property="value" initialValue="Hello world">
        <Password
          disabled
          id={`demo-disabled-password-${variant}`}
          onChange={(event, value) => console.log(value)}
          label="Super fantastic label"
          variant={variant}
        />
      </FormComponentDemo>
    )}
  />
</Playground>

## Custom icons password

<Playground>
  <Looper
    list={variants}
    render={variant => (
      <FormComponentDemo property="value" initialValue="Hello world">
        <Password iconHide={CheckIcon} iconShow={<HelpIcon />} onChange={(event, value) => console.log(value)} variant={variant} />
      </FormComponentDemo>
    )}
  />
</Playground>

## Contrast password

<Playground>
  <FormComponentDemo property="value" contrast>
    <Password
      contrast
      id="demo-contrast-password"
      onChange={(event, value) => console.log(value)}
      transitional
      label="Super fantastic label"
    />
  </FormComponentDemo>
</Playground>

## Notifications

<Playground>
  <Looper
    list={variants}
    render={variant => (
      <Looper
        list={['success', 'danger', 'warning', 'primary', 'neutral']}
        render={level => (
          <FormComponentDemo property="value" key={variant}>
            <NotifiedPassword
              clearable
              id={`demo-notification-${variant}-${level}`}
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              transitional
              variant={variant}
              level={level}
              notification={`This is an example ${level} notification.`}
            />
          </FormComponentDemo>
        )}
      />
    )}
  />
</Playground>

## \[props\]

<Props of={Password} />
