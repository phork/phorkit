---
name: Password
menu: Forms
route: /components/FormPassword
---

import { Playground, Props } from 'docz';
import { CheckIcon } from 'icons/internal/CheckIcon';
import { HelpIcon } from 'icons/internal/HelpIcon';
import { Rhythm } from 'components/Rhythm';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { FormComponentDemo } from '../../docs/helpers/FormComponentDemo';
import { Password, NotifiedPassword } from '../index';
import { password as variants, sizes } from './helpers/variants';

<PageTitle src="components/Form/Textbox" title="Password" />

## Basic password

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="password">
            <Password onChange={(event, value) => console.log(value)} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Visible password

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="password">
            <Password initialType="text" onChange={(event, value) => console.log(value)} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled password

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="password">
            <Password label="Super fantastic label" onChange={(event, value) => console.log(value)} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Transitional labeled password

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped property="value" type="password">
            <Password
              transitional
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Disabled password

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="password">
            <Password
              disabled
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Custom icons password

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue="Hello world" property="value" type="password">
            <Password
              iconHide={CheckIcon}
              iconHideSize={12}
              iconShow={HelpIcon}
              iconShowSize={12}
              onChange={(event, value) => console.log(value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Contrast password

<Playground>
  <FormComponentDemo contrast property="value" type="password">
    <Password contrast transitional label="Super fantastic label" onChange={(event, value) => console.log(value)} />
  </FormComponentDemo>
</Playground>

## Notifications

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Looper
          list={['success', 'danger', 'warning', 'primary', 'neutral']}
          render={level => (
            <Rhythm key={variant} py={3}>
              <FormComponentDemo unwrapped property="value" type="password">
                <NotifiedPassword
                  clearable
                  transitional
                  label="Super fantastic label"
                  level={level}
                  notification={`This is an example ${level} notification.`}
                  onChange={(event, value) => console.log(value)}
                  onClear={(event, value) => console.log('cleared')}
                  variant={variant}
                />
              </FormComponentDemo>
            </Rhythm>
          )}
        />
      )}
    />
  </ThemeWrapper>
</Playground>

## Sizes

<Playground>
  <ThemeWrapper>
    <Looper
      list={sizes}
      render={size => (
        <Rhythm grouped my={3}>
          <Looper
            list={variants}
            render={variant => (
              <Rhythm key={variant} py={3}>
                <FormComponentDemo unwrapped property="value" type="password">
                  <Password
                    transitional
                    label={`Size ${size}`}
                    onChange={(event, value) => console.log(value)}
                    size={size}
                    variant={variant}
                  />
                </FormComponentDemo>
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={Password} />
