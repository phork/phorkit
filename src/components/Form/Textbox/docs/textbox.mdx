---
name: Textbox
menu: Forms
route: /components/FormTextbox
---

import { Playground, Props } from 'docz';
import { ArrowLeftIcon } from 'icons/ArrowLeftIcon';
import { ArrowRightIcon } from 'icons/ArrowRightIcon';
import { SearchIcon } from 'icons/SearchIcon';
import { SpinnerIcon } from 'icons/SpinnerIcon';
import { TimesIcon } from 'icons/TimesIcon';
import { Button } from 'components/Button';
import { Rhythm } from 'components/Rhythm';
import { FormComponentDemo } from '../../docs/helpers/FormComponentDemo';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Textbox, NotifiedTextbox } from '../index';
import { textbox as variants } from './helpers/variants.js';

<PageTitle title="Textbox" src="components/Form/Textbox" />

## Basic textbox

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" key={variant} unwrapped>
            <Textbox variant={variant} onChange={(event, value) => console.log(value)} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled textbox

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" key={variant} unwrapped>
            <Textbox
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              onSubmit={(event, value) => console.log('submit', value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Transitional labeled textbox

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" key={variant} unwrapped>
            <Textbox
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              transitional
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Transitional, clearable labeled textbox

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" key={variant} unwrapped>
            <Textbox
              clearable
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              transitional
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Transitional labeled textbox with icons

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" key={variant} unwrapped>
            <Textbox
              iconBefore={SearchIcon}
              iconAfter={<SpinnerIcon size={14} />}
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              transitional
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled textbox with placeholder

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" key={variant} unwrapped>
            <Textbox
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              onSubmit={(event, value) => console.log('submit', value)}
              placeholder="Hello world"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled textbox with HTML placeholder

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" key={variant} unwrapped>
            <Textbox
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              onSubmit={(event, value) => console.log('submit', value)}
              placeholder={<i>Hello world</i>}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Transitional labeled textbox HTML value

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <StateWrapper>
          {({ state: value, setState: setValue }) => (
            <Rhythm py={3} style={{ maxWidth: 400 }} grouped>
              <Textbox
                alwaysUseFormatting
                clearable
                formattedValue={value ? <i>{value}</i> : undefined}
                label="Super fantastic label"
                onChange={(event, value) => setValue(value)}
                transitional
                value={value}
                variant={variant}
              />
            </Rhythm>
          )}
        </StateWrapper>
      )}
    />
  </ThemeWrapper>
</Playground>

## Disabled textbox

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" key={variant} unwrapped>
            <Textbox
              disabled
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Clearable search textbox

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" key={variant} unwrapped>
            <Textbox
              clearable
              iconBefore={<SearchIcon />}
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              transitional
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Read only textbox

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" key={variant} unwrapped>
            <Textbox
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              readOnly
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Textbox with focus/blur callbacks

<Playground>
  <StateWrapper initialState={false}>
    {({ state: isFocused, setState: setFocused }) => (
      <FormComponentDemo property="value">
        <Textbox
          label={isFocused ? 'Focused label' : 'Blurred label'}
          onFocus={event => setFocused(true)}
          onBlur={event => setFocused(false)}
          placeholder="Hello world"
        />
      </FormComponentDemo>
    )}
  </StateWrapper>
</Playground>

## Danger textbox

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" key={variant} unwrapped>
            <Textbox
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              transitional
              validity="danger"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Warning textbox

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" key={variant} unwrapped>
            <Textbox
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              transitional
              validity="warning"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Success textbox

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue="Hello world" key={variant} unwrapped>
            <Textbox
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              transitional
              validity="success"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Contrast textbox

<Playground>
  <ThemeWrapper contrast>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" contrast key={variant} unwrapped>
            <Textbox
              clearable
              contrast
              iconBefore={<SearchIcon />}
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              placeholder="Hello world"
              transitional
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Styled textbox

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" key={variant} unwrapped>
            <Textbox
              clearable
              label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              placeholder={<i>Hello world</i>}
              transitional
              unthemed
              variant={variant}
              style={{
                '--formbox-icon-color': '#9D69D5',
                '--formbox-icon-hovered-color': '#642da0',
                '--formbox-input-container-background-color': 'transparent',
                '--formbox-input-container-border-color': '#9D69D5',
                '--formbox-input-container-focused-border-color': '#57278C',
                '--formbox-input-container-hovered-border-color': '#642da0',
                '--formbox-input-text-color': '#642da0',
                '--formbox-label-text-color': '#B995E1',
              }}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Notifications

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Looper
          list={['success', 'danger', 'warning', 'primary', 'neutral']}
          render={level => (
            <Rhythm py={3}>
              <FormComponentDemo property="value" initialValue="Hello world" key={variant} unwrapped>
                <NotifiedTextbox
                  clearable
                  label="Super fantastic label"
                  onChange={(event, value) => console.log(value)}
                  transitional
                  variant={variant}
                  level={level}
                  notification={`This is an example ${level} notification.`}
                />
              </FormComponentDemo>
            </Rhythm>
          )}
        />
      )}
    />
  </ThemeWrapper>
</Playground>

## Auto complete

<Playground>
  <ThemeWrapper>
    <form name="autoComplete" autoComplete="on">
      <Looper
        list={variants}
        render={variant => (
          <Rhythm p={3} grouped>
            <FormComponentDemo property="value" key={variant} unwrapped>
              <Textbox clearable label="Email" name={`email-${variant}`} transitional type="email" variant={variant} />
            </FormComponentDemo>
          </Rhythm>
        )}
      />
      <Rhythm mt={2}>
        <Button type="submit" color="primary" weight="shaded" shape="brick">
          Submit
        </Button>
      </Rhythm>
    </form>
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={Textbox} />
