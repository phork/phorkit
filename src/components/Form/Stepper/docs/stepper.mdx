---
name: Stepper
menu: Forms
route: /components/FormStepper
---

import { Playground, Props } from 'docz';
import { Rhythm } from 'components/Rhythm';
import { FormComponentDemo } from '../../docs/helpers/FormComponentDemo';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Stepper, NotifiedStepper } from '../index';
import { variants } from './helpers/variants';

<PageTitle title="Stepper" src="components/Form/Stepper" />

## Basic stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue={10} unwrapped>
            <Stepper
              onChange={(event, value) => console.log(value)}
              aria-label="Super fantastic label"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Ticked stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue={10} unwrapped>
            <Stepper
              id="demo-ticked-stepper"
              min={-100}
              max={100}
              step={10}
              onChange={(event, value) => console.log(value)}
              aria-label="Super fantastic label"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue={10} unwrapped>
            <Stepper
              label="Cool label"
              min={2}
              max={12}
              step={1}
              onChange={(event, value) => console.log(value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Stepper with placeholder

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" unwrapped>
            <Stepper
              label="Cool label"
              min={2}
              max={12}
              step={1}
              onChange={(event, value) => console.log(value)}
              placeholder={10}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Read only stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue={10} unwrapped>
            <Stepper
              label="Cool label"
              min={2}
              max={12}
              step={1}
              onChange={(event, value) => console.log(value)}
              readOnly
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Disabled stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue={30} unwrapped>
            <Stepper
              disabled
              onChange={(event, value) => console.log(value)}
              aria-label="Super fantastic label"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Danger stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue={60} unwrapped>
            <Stepper
              onChange={(event, value) => console.log(value)}
              validity="danger"
              aria-label="Super fantastic label"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Warning stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue={60} unwrapped>
            <Stepper
              onChange={(event, value) => console.log(value)}
              validity="warning"
              aria-label="Super fantastic label"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Success stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue={60} unwrapped>
            <Stepper
              onChange={(event, value) => console.log(value)}
              validity="success"
              aria-label="Super fantastic label"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Contrast stepper

<Playground>
  <ThemeWrapper contrast>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo property="value" initialValue={30} contrast unwrapped>
            <Stepper
              contrast
              onChange={(event, value) => console.log(value)}
              aria-label="Super fantastic label"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Notifications

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Looper
          list={['success', 'danger', 'warning', 'primary', 'neutral']}
          render={level => (
            <Rhythm py={3}>
              <FormComponentDemo property="value" initialValue={10} unwrapped>
                <NotifiedStepper
                  onChange={(event, value) => console.log(value)}
                  aria-label="Super fantastic label"
                  variant={variant}
                  level={level}
                  notification={`This is an example ${level} notification.`}
                />
              </FormComponentDemo>
            </Rhythm>
          )}
        />
      )}
    />
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={Stepper} />
