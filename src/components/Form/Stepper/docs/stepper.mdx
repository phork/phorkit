---
name: Stepper
menu: Forms
route: /components/FormStepper
---

import { Playground, Props } from 'docz';
import { Looper } from 'components/Looper';
import { Rhythm } from 'components/Rhythm';
import { PageTitle } from 'docs/helpers/PageTitle';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { FormComponentDemo } from '../../docs/helpers/FormComponentDemo';
import { Stepper, NotifiedStepper } from '../index';
import { variants, sizes } from './helpers/variants';

<PageTitle src="components/Form/Stepper" title="Stepper" />

## Basic stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue={10} property="value" type="stepper">
            <Stepper aria-label="Cool label" onChange={(event, value) => console.log(value)} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Ticked stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue={10} property="value" type="stepper">
            <Stepper
              label="Cool label"
              max={100}
              min={-100}
              onChange={(event, value) => console.log(value)}
              step={10}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue={10} property="value" type="stepper">
            <Stepper
              label="Cool label"
              max={12}
              min={2}
              onChange={(event, value) => console.log(value)}
              step={1}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled stepper with placeholder

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped property="value" type="stepper">
            <Stepper
              label="Cool label"
              max={12}
              min={2}
              onChange={(event, value) => console.log(value)}
              placeholder={10}
              step={1}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

### Labeled stepper with HTML value

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <StateWrapper initialState={10} key={variant}>
          {({ state: value, setState: setValue }) => (
            <Rhythm grouped py={3} style={{ maxWidth: 400 }}>
              <Stepper
                alwaysUseFormatting
                formattedValue={value || value === 0 ? <i>{value}</i> : undefined}
                label="Cool label"
                onChange={(event, value) => setValue(value)}
                value={value}
                variant={variant}
              />
            </Rhythm>
          )}
        </StateWrapper>
      )}
    />
  </ThemeWrapper>
</Playground>

## Read only stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue={10} property="value" type="stepper">
            <Stepper
              readOnly
              label="Cool label"
              max={12}
              min={2}
              onChange={(event, value) => console.log(value)}
              step={1}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Disabled stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue={10} property="value" type="stepper">
            <Stepper disabled label="Cool label" onChange={(event, value) => console.log(value)} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Danger stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue={10} property="value" type="stepper">
            <Stepper
              label="Cool label"
              onChange={(event, value) => console.log(value)}
              validity="danger"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Warning stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue={10} property="value" type="stepper">
            <Stepper
              label="Cool label"
              onChange={(event, value) => console.log(value)}
              validity="warning"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Success stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo unwrapped initialValue={10} property="value" type="stepper">
            <Stepper
              label="Cool label"
              onChange={(event, value) => console.log(value)}
              validity="success"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Contrast stepper

<Playground>
  <ThemeWrapper contrast>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo contrast unwrapped initialValue={10} property="value" type="stepper">
            <Stepper contrast label="Cool label" onChange={(event, value) => console.log(value)} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Notifications

<Playground>
  <ThemeWrapper>
    <Looper
      list={['danger', 'warning', 'success', 'primary', 'neutral']}
      render={level => (
        <Looper
          list={variants}
          render={variant => (
            <Rhythm py={3}>
              <FormComponentDemo unwrapped initialValue={10} property="value" type="stepper">
                <NotifiedStepper
                  label="Cool label"
                  level={level}
                  notification={`This is an example ${level} notification.`}
                  onChange={(event, value) => console.log(value)}
                  variant={variant}
                />
              </FormComponentDemo>
            </Rhythm>
          )}
        />
      )}
    />
  </ThemeWrapper>
</Playground>

## Sizes

<Playground>
  <ThemeWrapper>
    <Looper
      list={sizes}
      render={size => (
        <Rhythm grouped my={3}>
          <Looper
            list={variants}
            render={variant => (
              <Rhythm key={variant} py={3}>
                <FormComponentDemo unwrapped property="value" type="textbox">
                  <Stepper
                    inputWidth="auto"
                    label={`Size ${size}`}
                    max={12}
                    min={2}
                    onChange={(event, value) => console.log(value)}
                    placeholder={10}
                    size={size}
                    step={1}
                    variant={variant}
                  />
                </FormComponentDemo>
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={Stepper} />
