---
name: Stepper
menu: Forms
route: /components/FormStepper
---

import { Playground, Props } from 'docz';
import { Rhythm } from 'components/Rhythm';
import { FormComponentDemo } from '../../docs/helpers/FormComponentDemo';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Stepper, NotifiedStepper } from '../index';
import { variants, sizes } from './helpers/variants';

<PageTitle title="Stepper" src="components/Form/Stepper" />

## Basic stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="stepper" property="value" initialValue={10} unwrapped>
            <Stepper
              aria-label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Ticked stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="stepper" property="value" initialValue={10} unwrapped>
            <Stepper
              aria-label="Super fantastic label"
              id="demo-ticked-stepper"
              max={100}
              min={-100}
              onChange={(event, value) => console.log(value)}
              step={10}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Labeled stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="stepper" property="value" initialValue={10} unwrapped>
            <Stepper
              label="Cool label"
              max={12}
              min={2}
              onChange={(event, value) => console.log(value)}
              step={1}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Stepper with placeholder

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="stepper" property="value" unwrapped>
            <Stepper
              label="Cool label"
              max={12}
              min={2}
              onChange={(event, value) => console.log(value)}
              placeholder={10}
              step={1}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Read only stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="stepper" property="value" initialValue={10} unwrapped>
            <Stepper
              label="Cool label"
              max={12}
              min={2}
              onChange={(event, value) => console.log(value)}
              readOnly
              step={1}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Disabled stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="stepper" property="value" initialValue={30} unwrapped>
            <Stepper
              aria-label="Super fantastic label"
              disabled
              onChange={(event, value) => console.log(value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Danger stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="stepper" property="value" initialValue={60} unwrapped>
            <Stepper
              aria-label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              validity="danger"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Warning stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="stepper" property="value" initialValue={60} unwrapped>
            <Stepper
              aria-label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              validity="warning"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Success stepper

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="stepper" property="value" initialValue={60} unwrapped>
            <Stepper
              aria-label="Super fantastic label"
              onChange={(event, value) => console.log(value)}
              validity="success"
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Contrast stepper

<Playground>
  <ThemeWrapper contrast>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm py={3}>
          <FormComponentDemo type="stepper" property="value" initialValue={30} contrast unwrapped>
            <Stepper
              aria-label="Super fantastic label"
              contrast
              onChange={(event, value) => console.log(value)}
              variant={variant}
            />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## Notifications

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Looper
          list={['success', 'danger', 'warning', 'primary', 'neutral']}
          render={level => (
            <Rhythm py={3}>
              <FormComponentDemo type="stepper" property="value" initialValue={10} unwrapped>
                <NotifiedStepper
                  aria-label="Super fantastic label"
                  level={level}
                  notification={`This is an example ${level} notification.`}
                  onChange={(event, value) => console.log(value)}
                  variant={variant}
                />
              </FormComponentDemo>
            </Rhythm>
          )}
        />
      )}
    />
  </ThemeWrapper>
</Playground>

## Sizes

<Playground>
  <ThemeWrapper>
    <Looper
      list={sizes}
      render={size => (
        <Rhythm my={3} grouped>
          <Looper
            list={variants}
            render={variant => (
              <Rhythm py={3}>
                <FormComponentDemo type="textbox" property="value" key={variant} unwrapped>
                  <Stepper
                    inputWidth="auto"
                    label={`Size ${size}`}
                    max={12}
                    min={2}
                    onChange={(event, value) => console.log(value)}
                    placeholder={10}
                    size={size}
                    step={1}
                    variant={variant}
                  />
                </FormComponentDemo>
              </Rhythm>
            )}
          />
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={Stepper} />
