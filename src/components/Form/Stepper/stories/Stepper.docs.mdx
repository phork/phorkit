import { ArgsTable, Description, Primary, Stories, Subtitle, PRIMARY_STORY } from '@storybook/addon-docs';
import { Rhythm } from 'components/Rhythm';
import { Looper } from 'docs/helpers/Looper';
import { Canvas } from 'stories/helpers/Canvas';
import { PageTitle } from 'stories/helpers/PageTitle';
import { ThemeWrapper } from 'stories/helpers/ThemeWrapper';
import { FormComponentDemo } from '../../stories/helpers/FormComponentDemo';
import { NotifiedStepper } from '../NotifiedStepper';
import { Stepper } from '../Stepper';
import { variants } from './helpers/variants';

<PageTitle src="components/Stepper" title="Stepper" />
<Subtitle />
<Description />
<Primary />
<ArgsTable story={PRIMARY_STORY} />
<Stories />

## Examples

### Basic stepper

<Canvas>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm grouped py={3}>
          <FormComponentDemo initialValue={10} property="value" type="stepper">
            <Stepper aria-label="Super fantastic label" variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Canvas>

### Ticked stepper

<Canvas>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm grouped py={3}>
          <FormComponentDemo initialValue={10} property="value" type="stepper">
            <Stepper aria-label="Super fantastic label" max={100} min={-100} step={10} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Canvas>

### Labeled stepper

<Canvas>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm grouped py={3}>
          <FormComponentDemo initialValue={10} property="value" type="stepper">
            <Stepper label="Cool label" max={12} min={2} step={1} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Canvas>

### Stepper with placeholder

<Canvas>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm grouped py={3}>
          <FormComponentDemo property="value" type="stepper">
            <Stepper label="Cool label" max={12} min={2} placeholder={10} step={1} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Canvas>

### Read only stepper

<Canvas>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm grouped py={3}>
          <FormComponentDemo initialValue={10} property="value" type="stepper">
            <Stepper readOnly label="Cool label" max={12} min={2} step={1} variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Canvas>

### Disabled stepper

<Canvas>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm grouped py={3}>
          <FormComponentDemo initialValue={30} property="value" type="stepper">
            <Stepper disabled aria-label="Super fantastic label" variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Canvas>

### Danger stepper

<Canvas>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm grouped py={3}>
          <FormComponentDemo initialValue={60} property="value" type="stepper">
            <Stepper aria-label="Super fantastic label" validity="danger" variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Canvas>

### Warning stepper

<Canvas>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm grouped py={3}>
          <FormComponentDemo initialValue={60} property="value" type="stepper">
            <Stepper aria-label="Super fantastic label" validity="warning" variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Canvas>

### Success stepper

<Canvas>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm grouped py={3}>
          <FormComponentDemo initialValue={60} property="value" type="stepper">
            <Stepper aria-label="Super fantastic label" validity="success" variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Canvas>

### Contrast stepper

<Canvas contrast>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Rhythm grouped py={3}>
          <FormComponentDemo initialValue={30} property="value" type="stepper">
            <Stepper contrast aria-label="Super fantastic label" variant={variant} />
          </FormComponentDemo>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Canvas>

### Notifications

<Canvas>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <Looper
          list={['success', 'danger', 'warning', 'primary', 'neutral']}
          render={level => (
            <Rhythm grouped py={3}>
              <FormComponentDemo initialValue={10} property="value" type="stepper">
                <NotifiedStepper
                  aria-label="Super fantastic label"
                  level={level}
                  notification={`This is an example ${level} notification.`}
                  variant={variant}
                />
              </FormComponentDemo>
            </Rhythm>
          )}
        />
      )}
    />
  </ThemeWrapper>
</Canvas>
