---
name: Tag
menu: Display
route: /components/Tag
---

import { Playground, Props } from 'docz';
import { TimesIcon } from 'icons/TimesIcon';
import { BlobbrIcon } from 'icons/internal/BlobbrIcon';
import { CheckIcon } from 'icons/internal/CheckIcon';
import { Avatar } from 'components/Avatar';
import { IconButton } from 'components/Button';
import { Flex } from 'components/Flex';
import { IconText } from 'components/IconText';
import { Rhythm } from 'components/Rhythm';
import { Typography } from 'components/Typography';
import { ColorLooper } from 'docs/helpers/ColorLooper';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { ColoredTag, StyledTag, Tag, TagGroup } from '../index';

<PageTitle title="Tag" src="components/Tags" />

## Pill tags

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <Looper
      list={['xxsmall', 'xsmall', 'small', 'medium', 'large']}
      render={size => (
        <React.Fragment>
          <Rhythm m={2}>
            <Tag size={size} label="Read only tag" />
            <Tag size={size} label="Button tag" onClick={() => console.log('button tag')} actionable />
            <Tag size={size} label="Link tag" as="a" href="#tag" actionable />
            <Tag size={size} label="Transparent tag" variant="transparent" actionable />
            <Tag size={size} label="Filled tag" variant="filled" actionable />
          </Rhythm>
          <br />
        </React.Fragment>
      )}
    />
  </ThemeWrapper>
</Playground>

## Brick tags

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <Looper
      list={['xxsmall', 'xsmall', 'small', 'medium', 'large']}
      render={size => (
        <React.Fragment>
          <Rhythm m={2}>
            <Tag shape="brick" size={size} label="Read only tag" />
            <Tag shape="brick" size={size} label="Button tag" onClick={() => console.log('button tag')} actionable />
            <Tag shape="brick" size={size} label="Link tag" as="a" href="#tag" actionable />
            <Tag shape="brick" size={size} label="Transparent tag" variant="transparent" actionable />
            <Tag shape="brick" size={size} label="Filled tag" variant="filled" actionable />
          </Rhythm>
          <br />
        </React.Fragment>
      )}
    />
  </ThemeWrapper>
</Playground>

## Variations

### Contrast tag

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper contrast>
    <Rhythm m={2}>
      <Tag contrast label="Read only tag" />
      <Tag contrast label="Button tag" onClick={() => console.log('button tag')} actionable />
      <Tag contrast label="Link tag" as="a" href="#tag" actionable />
      <Tag contrast label="Transparent tag" variant="transparent" actionable />
      <Tag contrast label="Filled tag" variant="filled" actionable />
    </Rhythm>
  </ThemeWrapper>
</Playground>

## Colored tags

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Flex drection="row">
        <Looper
          list={[undefined, 'brick']}
          render={shape => (
            <Flex key={`shape-${shape}`} direction="column">
              <ColorLooper
                themeId={themeId}
                group="primary"
                render={({ id }) => (
                  <Rhythm key={`${shape}-${id}`} mb={1} mr={4}>
                    <TagGroup>
                      <ColoredTag
                        shape={shape}
                        colorId={id}
                        label={`Color tag ${id}`}
                        variant="transparent"
                        actionable
                      />
                      <ColoredTag shape={shape} colorId={id} label={`Color tag ${id}`} variant="shaded" actionable />
                      <ColoredTag shape={shape} colorId={id} label={`Color tag ${id}`} variant="filled" actionable />
                    </TagGroup>
                  </Rhythm>
                )}
              />
            </Flex>
          )}
        />
      </Flex>
    )}
  </ThemeWrapper>
</Playground>

### Custom tag labels

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <TagGroup>
        <Tag
          key="remove"
          label={
            <IconText
              icon={
                <IconButton
                  aria-label="Remove tag"
                  themeId={themeId}
                  color="neutral"
                  onClick={e => console.log('remove tag')}
                >
                  <TimesIcon scale="xsmall" />
                </IconButton>
              }
              text={
                <Rhythm mr={2}>
                  <span>Hello world</span>
                </Rhythm>
              }
              reverse
            />
          }
        />
        <Tag
          key="hello"
          label={
            <IconText
              icon={<BlobbrIcon scale="small" />}
              text={
                <Rhythm ml={2}>
                  <span>Hello world</span>
                </Rhythm>
              }
            />
          }
          onClick={() => console.log('tag')}
          shape="brick"
          actionable
        />
      </TagGroup>
    )}
  </ThemeWrapper>
</Playground>

### Chip Tag

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <TagGroup>
        <Tag
          flush
          key="close"
          size="medium"
          label={
            <Flex direction="row" alignItems="center" justifyContent="space-between">
              <Rhythm m={1} mr={2}>
                <Avatar color="primary" size="xsmall" initials="EC" />
              </Rhythm>
              <Typography variants={['line-height-smash']}>Hello world</Typography>
              <Rhythm ml={4} p={1} style={{ marginRight: 5 }}>
                <IconButton
                  aria-label="Remove tag"
                  color="neutral"
                  noHeight
                  onClick={e => console.log('remove tag')}
                  shape="round"
                  themeId={themeId}
                >
                  <TimesIcon scale="xsmall" />
                </IconButton>
              </Rhythm>
            </Flex>
          }
        />
        <Tag
          flush
          key="check"
          size="medium"
          label={
            <Flex direction="row" alignItems="center" justifyContent="space-between">
              <Rhythm m={1} mr={2}>
                <Avatar color="primary" size="xsmall" initials="EC" />
              </Rhythm>
              <Typography variants={['line-height-smash']}>Hello world</Typography>
              <Rhythm mr={3} ml={4}>
                <CheckIcon scale="small" />
              </Rhythm>
            </Flex>
          }
        />
      </TagGroup>
    )}
  </ThemeWrapper>
</Playground>

### Styled tag

<Playground>
  <Rhythm m={2}>
    <StyledTag contrast label="Shaded tag" inverseColor="#fff" primaryColor="#556270" actionable />
    <StyledTag
      contrast
      label="Transparent tag"
      inverseColor="#fff"
      primaryColor="#556270"
      variant="transparent"
      actionable
    />
    <StyledTag contrast label="Filled tag" inverseColor="#fff" primaryColor="#556270" variant="filled" actionable />
  </Rhythm>
</Playground>

## \[props\]

### Tag

<Props of={Tag} />

### ColoredTag

<Props of={ColoredTag} />
