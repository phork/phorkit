---
name: Tag
menu: Display
route: /components/Tag
---

import { Playground, Props } from 'docz';
import { TimesIcon } from 'icons/TimesIcon';
import { BlobbrIcon } from 'icons/internal/BlobbrIcon';
import { CheckIcon } from 'icons/internal/CheckIcon';
import { Avatar } from 'components/Avatar';
import { IconButton } from 'components/Button';
import { Flex } from 'components/Flex';
import { IconText } from 'components/IconText';
import { Rhythm } from 'components/Rhythm';
import { Typography } from 'components/Typography';
import { ColorLooper } from 'docs/helpers/ColorLooper';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { ColoredTag, StyledTag, Tag, TagGroup } from '../index';

<PageTitle src="components/Tag" title="Tag" />

## Pill tags

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <Looper
      list={['2xsmall', 'xsmall', 'small', 'medium', 'large']}
      render={size => (
        <React.Fragment>
          <Rhythm m={2}>
            <Tag label="Read only tag" size={size} variant="shaded" />
            <Tag actionable label="Button tag" onClick={() => console.log('button tag')} size={size} variant="shaded" />
            <Tag actionable as="a" href="#tag" label="Link tag" size={size} variant="shaded" />
            <Tag actionable label="Outlined tag" size={size} variant="outlined" />
            <Tag actionable label="Solid tag" size={size} variant="solid" />
          </Rhythm>
          <br />
        </React.Fragment>
      )}
    />
  </ThemeWrapper>
</Playground>

## Brick tags

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <Looper
      list={['2xsmall', 'xsmall', 'small', 'medium', 'large']}
      render={size => (
        <React.Fragment>
          <Rhythm m={2}>
            <Tag label="Read only tag" shape="brick" size={size} />
            <Tag actionable label="Button tag" onClick={() => console.log('button tag')} shape="brick" size={size} />
            <Tag actionable as="a" href="#tag" label="Link tag" shape="brick" size={size} />
            <Tag actionable label="Outlined tag" shape="brick" size={size} variant="outlined" />
            <Tag actionable label="Solid tag" shape="brick" size={size} variant="solid" />
          </Rhythm>
          <br />
        </React.Fragment>
      )}
    />
  </ThemeWrapper>
</Playground>

## Variations

### Contrast tag

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper contrast>
    <Rhythm m={2}>
      <Tag contrast label="Read only tag" />
      <Tag actionable contrast label="Button tag" onClick={() => console.log('button tag')} />
      <Tag actionable contrast as="a" href="#tag" label="Link tag" />
      <Tag actionable contrast label="Outlined tag" variant="outlined" />
      <Tag actionable contrast label="Solid tag" variant="solid" />
    </Rhythm>
  </ThemeWrapper>
</Playground>

## Colored tags

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <Flex drection="row">
        <Looper
          list={[undefined, 'brick']}
          render={shape => (
            <Flex direction="column" key={`shape-${shape || 'default'}`}>
              <ColorLooper
                group="primary"
                render={({ id }) => (
                  <Rhythm key={`${shape || 'default'}-${id}`} mb={1} mr={4}>
                    <TagGroup>
                      <ColoredTag actionable colorId={id} label={`Color tag ${id}`} shape={shape} variant="outlined" />
                      <ColoredTag actionable colorId={id} label={`Color tag ${id}`} shape={shape} variant="shaded" />
                      <ColoredTag actionable colorId={id} label={`Color tag ${id}`} shape={shape} variant="solid" />
                    </TagGroup>
                  </Rhythm>
                )}
                themeId={themeId}
              />
            </Flex>
          )}
        />
      </Flex>
    )}
  </ThemeWrapper>
</Playground>

### Custom tag labels

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <TagGroup>
        <Tag
          key="remove"
          label={
            <IconText
              reverse
              icon={
                <IconButton
                  aria-label="Remove tag"
                  color="neutral"
                  onClick={e => console.log('remove tag')}
                  themeId={themeId}
                >
                  <TimesIcon scale="xsmall" />
                </IconButton>
              }
              text={
                <Rhythm mr={2}>
                  <span>Hello world</span>
                </Rhythm>
              }
            />
          }
        />
        <Tag
          actionable
          key="hello"
          label={
            <IconText
              icon={<BlobbrIcon scale="small" />}
              text={
                <Rhythm ml={2}>
                  <span>Hello world</span>
                </Rhythm>
              }
            />
          }
          onClick={() => console.log('tag')}
          shape="brick"
        />
      </TagGroup>
    )}
  </ThemeWrapper>
</Playground>

### Chip tag

_This is an example of how to make a custom chip from the Tag component. There is also a pre-defined [Chip](./Chip) component._

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <TagGroup>
        <Tag
          flush
          key="close"
          label={
            <Flex alignItems="center" direction="row" justifyContent="space-between">
              <Rhythm m={1} mr={2}>
                <Avatar color="primary" initials="EC" size="xsmall" />
              </Rhythm>
              <Typography variants={['line-height-smash']}>Hello world</Typography>
              <Rhythm ml={4} p={1} style={{ marginRight: 5 }}>
                <IconButton
                  noHeight
                  aria-label="Remove tag"
                  color="neutral"
                  onClick={e => console.log('remove tag')}
                  shape="round"
                  themeId={themeId}
                >
                  <TimesIcon scale="xsmall" />
                </IconButton>
              </Rhythm>
            </Flex>
          }
          size="medium"
        />
        <Tag
          flush
          key="check"
          label={
            <Flex alignItems="center" direction="row" justifyContent="space-between">
              <Rhythm m={1} mr={2}>
                <Avatar color="primary" initials="EC" size="xsmall" />
              </Rhythm>
              <Typography variants={['line-height-smash']}>Hello world</Typography>
              <Rhythm ml={4} mr={3}>
                <CheckIcon scale="small" />
              </Rhythm>
            </Flex>
          }
          size="medium"
        />
      </TagGroup>
    )}
  </ThemeWrapper>
</Playground>

### Styled tag

<Playground>
  <Rhythm m={2}>
    <StyledTag actionable contrast inverseColor="#fff" label="Shaded tag" primaryColor="#556270" />
    <StyledTag actionable contrast inverseColor="#fff" label="Outlined tag" primaryColor="#556270" variant="outlined" />
    <StyledTag actionable contrast inverseColor="#fff" label="Solid tag" primaryColor="#556270" variant="solid" />
  </Rhythm>
</Playground>

## \[props\]

### Tag

<Props of={Tag} />

### ColoredTag

<Props of={ColoredTag} />
