---
name: Panels
menu: Surfaces
route: /components/Panels
---

import { Playground, Props } from 'docz';
import { PageTitle } from 'docs/helpers/PageTitle';
import { PermanentSidePanel } from '../SidePanel/PermanentSidePanel';
import { SidePanel } from '../SidePanel/SidePanel';
import { PermanentStackPanel } from '../StackPanel/PermanentStackPanel';
import { StackPanel } from '../StackPanel/StackPanel';
import { MainPanel, PanelContainer } from '../index';
import { SidePanelDemo } from './helpers/SidePanelDemo';
import { StackPanelDemo } from './helpers/StackPanelDemo';

<PageTitle src="components/Panels" title="Panels" />

## SidePanel

### Permanent right side panel

<Playground style={{ display: 'flex', margin: '-40px', height: '200px' }}>
  <PanelContainer orientation="vertical">
    <MainPanel
      style={{
        background: 'linear-gradient(90deg, rgba(244,17,80,1) 0%, rgba(255,50,50,1) 25%, rgba(248,190,7,1) 100%)',
        color: '#fff',
      }}
    >
      Main panel
    </MainPanel>
    <PermanentSidePanel position="right" width={200}>
      Side panel
    </PermanentSidePanel>
  </PanelContainer>
</Playground>

### Opened right side panel

<Playground style={{ display: 'flex', margin: '-40px', height: '200px' }}>
  <PanelContainer orientation="vertical">
    <MainPanel
      style={{
        background: 'linear-gradient(90deg, rgba(244,17,80,1) 0%, rgba(255,50,50,1) 25%, rgba(248,190,7,1) 100%)',
        color: '#fff',
      }}
    >
      Main panel
    </MainPanel>
    <SidePanel open position="right" width={200}>
      Side panel
    </SidePanel>
  </PanelContainer>
</Playground>

### Shiftable side panels

<Playground>
  <SidePanelDemo style={{ height: '200px' }}>
    {(isOpen, panelState, setPanelState) => (
      <PanelContainer orientation="vertical">
        <SidePanel
          onCloseFinish={() => setPanelState('closed')}
          onCloseStart={() => setPanelState('closing')}
          onOpenFinish={() => setPanelState('open')}
          onOpenStart={() => setPanelState('opening')}
          open={isOpen}
          position="left"
          width={200}
        >
          Side panel
        </SidePanel>
        <MainPanel
          style={{
            background: 'linear-gradient(90deg, rgba(244,17,80,1) 0%, rgba(255,50,50,1) 25%, rgba(248,190,7,1) 100%)',
            color: '#fff',
          }}
        >
          <div>Main panel</div>
          <div>Side panel state: {panelState}</div>
        </MainPanel>
        <SidePanel open={isOpen} position="right" width={200}>
          Side panel
        </SidePanel>
      </PanelContainer>
    )}
  </SidePanelDemo>
</Playground>

### Shiftable fixed side panels

<Playground>
  <SidePanelDemo style={{ height: '200px' }}>
    {isOpen => (
      <PanelContainer orientation="vertical">
        <SidePanel fixed open={isOpen} position="left" style={{ background: '#fff' }} width={200}>
          Side panel
        </SidePanel>
        <MainPanel
          style={{
            background: 'linear-gradient(90deg, rgba(244,17,80,1) 0%, rgba(255,50,50,1) 25%, rgba(248,190,7,1) 100%)',
            color: '#fff',
          }}
        >
          Main panel
        </MainPanel>
        <SidePanel fixed open={isOpen} position="right" style={{ background: '#fff' }} width={200}>
          Side panel
        </SidePanel>
      </PanelContainer>
    )}
  </SidePanelDemo>
</Playground>

### Squashable side panels

<Playground>
  <SidePanelDemo style={{ height: '200px' }}>
    {isOpen => (
      <PanelContainer orientation="vertical">
        <SidePanel
          fixed
          open={isOpen}
          position="left"
          style={{ background: '#fff' }}
          transition="squashable"
          width={200}
        >
          Side panel
        </SidePanel>
        <MainPanel
          style={{
            background: 'linear-gradient(90deg, rgba(244,17,80,1) 0%, rgba(255,50,50,1) 25%, rgba(248,190,7,1) 100%)',
            color: '#fff',
          }}
        >
          Main panel
        </MainPanel>
        <SidePanel
          fixed
          open={isOpen}
          position="right"
          style={{ background: '#fff' }}
          transition="squashable"
          width={200}
        >
          Side panel
        </SidePanel>
      </PanelContainer>
    )}
  </SidePanelDemo>
</Playground>

### Side panel using percentages

<Playground>
  <SidePanelDemo style={{ height: '200px' }}>
    {isOpen => (
      <PanelContainer orientation="vertical">
        <MainPanel
          style={{
            background: 'linear-gradient(90deg, rgba(244,17,80,1) 0%, rgba(255,50,50,1) 25%, rgba(248,190,7,1) 100%)',
            color: '#fff',
          }}
        >
          Main panel
        </MainPanel>
        <SidePanel fixed open={isOpen} position="right" style={{ background: '#fff' }} unit="percent" width={100}>
          Side panel
        </SidePanel>
      </PanelContainer>
    )}
  </SidePanelDemo>
</Playground>

## StackPanel

### Permanent bottom stack panel

<Playground style={{ display: 'flex', margin: '-40px', height: '200px' }}>
  <PanelContainer orientation="horizontal">
    <MainPanel
      style={{
        background: 'linear-gradient(180deg, rgba(244,17,80,1) 0%, rgba(255,50,50,1) 25%, rgba(248,190,7,1) 100%)',
        color: '#fff',
      }}
    >
      Main panel
    </MainPanel>
    <PermanentStackPanel position="bottom">
      <div>Stack panel</div>
      <div>Stack panel</div>
      <div>Stack panel</div>
    </PermanentStackPanel>
  </PanelContainer>
</Playground>

### Opened bottom stack panel

<Playground style={{ display: 'flex', margin: '-40px', height: '200px' }}>
  <PanelContainer orientation="horizontal">
    <MainPanel
      style={{
        background: 'linear-gradient(180deg, rgba(244,17,80,1) 0%, rgba(255,50,50,1) 25%, rgba(248,190,7,1) 100%)',
        color: '#fff',
      }}
    >
      Main panel
    </MainPanel>
    <StackPanel open height={100} position="bottom">
      Stack panel
    </StackPanel>
  </PanelContainer>
</Playground>

### Shiftable stack panels

<Playground>
  <StackPanelDemo style={{ height: '200px' }}>
    {(isOpen, panelState, setPanelState) => (
      <PanelContainer orientation="horizontal">
        <StackPanel
          height={50}
          onCloseFinish={() => setPanelState('closed')}
          onCloseStart={() => setPanelState('closing')}
          onOpenFinish={() => setPanelState('open')}
          onOpenStart={() => setPanelState('opening')}
          open={isOpen}
          position="top"
        >
          Stack panel
        </StackPanel>
        <MainPanel
          style={{
            background: 'linear-gradient(180deg, rgba(244,17,80,1) 0%, rgba(255,50,50,1) 25%, rgba(248,190,7,1) 100%)',
            color: '#fff',
          }}
        >
          <div>Main panel</div>
          <div>Stack panel state: {panelState}</div>
        </MainPanel>
        <StackPanel height={50} open={isOpen} position="bottom">
          Stack panel
        </StackPanel>
      </PanelContainer>
    )}
  </StackPanelDemo>
</Playground>

### Shiftable fixed stack panels

<Playground>
  <StackPanelDemo style={{ height: '200px' }}>
    {isOpen => (
      <PanelContainer orientation="horizontal">
        <StackPanel fixed height={50} open={isOpen} position="bottom" style={{ background: '#fff' }}>
          Stack panel
        </StackPanel>
        <MainPanel
          style={{
            background: 'linear-gradient(180deg, rgba(244,17,80,1) 0%, rgba(255,50,50,1) 25%, rgba(248,190,7,1) 100%)',
            color: '#fff',
          }}
        >
          Main panel
        </MainPanel>
        <StackPanel fixed height={50} open={isOpen} position="top" style={{ background: '#fff' }}>
          Stack panel
        </StackPanel>
      </PanelContainer>
    )}
  </StackPanelDemo>
</Playground>

### Squashable stack panels

<Playground>
  <StackPanelDemo style={{ height: '200px' }}>
    {isOpen => (
      <PanelContainer orientation="horizontal">
        <StackPanel height={50} open={isOpen} position="top" transition="squashable">
          Stack panel
        </StackPanel>
        <MainPanel
          style={{
            background: 'linear-gradient(180deg, rgba(244,17,80,1) 0%, rgba(255,50,50,1) 25%, rgba(248,190,7,1) 100%)',
            color: '#fff',
          }}
        >
          Main panel
        </MainPanel>
        <StackPanel height={50} open={isOpen} position="bottom" transition="squashable">
          Stack panel
        </StackPanel>
      </PanelContainer>
    )}
  </StackPanelDemo>
</Playground>

### Stack panel using percentages

<Playground>
  <StackPanelDemo style={{ height: '200px' }}>
    {isOpen => (
      <PanelContainer orientation="horizontal">
        <MainPanel
          style={{
            background: 'linear-gradient(180deg, rgba(244,17,80,1) 0%, rgba(255,50,50,1) 25%, rgba(248,190,7,1) 100%)',
            color: '#fff',
          }}
        >
          Main panel
        </MainPanel>
        <StackPanel height={100} open={isOpen} position="bottom" transition="squashable" unit="percent">
          Stack panel
        </StackPanel>
      </PanelContainer>
    )}
  </StackPanelDemo>
</Playground>

## \[props\]

### SidePanel

<Props of={SidePanel} />

### PermanentSidePanel

<Props of={PermanentSidePanel} />

### StackPanel

<Props of={StackPanel} />

### PermanentStackPanel

<Props of={PermanentStackPanel} />
