import { Theme } from '../../types';
import darkTheme from '../../postcss/vars/darkTheme.json';
import { ThemeColors } from './types';

export const themeId: Theme = 'dark';
export const theme: ThemeColors = darkTheme;
