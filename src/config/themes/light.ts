import { Theme } from '../../types';
import lightTheme from '../../postcss/vars/lightTheme.json';
import { ThemeColors } from './types';

export const themeId: Theme = 'light';
export const theme: ThemeColors = lightTheme;
