---
name: Pagination
menu: Navigation
route: /compositions/Pagination
---

import { Playground, Props } from 'docz';
import { PageTitle } from 'docs/helpers/PageTitle';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Pagination } from '../index';

<PageTitle src="compositions/Pagination" title="Pagination" />

The pagination is a wrapper for the [ButtonGroup](../components/ButtonGroup). It can use either `ButtonGroup` styles or custom styles on top of an unstyled `ButtonGroup`.

## Pagination as buttons

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withIcons
          withPageLinks
          withPreviousAndNext
          activePageProps={{ weight: 'solid', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          jumpProps={{ weight: 'shaded', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageLinks={6}
          pageProps={{ weight: 'shaded', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          pageSize={10}
          spacing="joined"
          totalItems={300}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## Pagination as links

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withIcons
          withPageLinks
          withPreviousAndNext
          activePageProps={{ weight: 'solid', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          getHref={page => `#page${page}`}
          jumpProps={{ weight: 'shaded', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          page={currentPage}
          pageLinks={6}
          pageProps={{ weight: 'shaded', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          pageSize={10}
          spacing="joined"
          totalItems={300}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## Pagination as links and onClick

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          allowRightClickLinks
          withIcons
          withPageLinks
          withPreviousAndNext
          activePageProps={{ weight: 'solid', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          getHref={page => `#page${page}`}
          jumpProps={{ weight: 'shaded', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageLinks={6}
          pageProps={{ weight: 'shaded', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          pageSize={10}
          spacing="joined"
          totalItems={300}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## Pagination with custom styles

### Page links with icons

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withIcons
          withPageLinks
          withPreviousAndNext
          activePageProps={{ weight: 'solid', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          jumpProps={{ weight: 'shaded', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageLinks={6}
          pageProps={{ weight: 'shaded', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          pageSize={10}
          spacing="joined"
          totalItems={300}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Page links with text

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withPageLinks
          withPreviousAndNext
          activePageProps={{ weight: 'solid', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          jumpProps={{ weight: 'shaded', color: 'primary', shape: 'brick' }}
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageLinks={6}
          pageProps={{ weight: 'shaded', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          pageSize={10}
          spacing="joined"
          totalItems={300}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Everything with icons

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withEllipsis
          withFirstAndLast
          withIcons
          withPageLabel
          withPageLinks
          withPreviousAndNext
          activePageProps={{
            weight: 'shaded',
            color: 'primary',
            shape: 'brick',
            noPadding: true,
            style: { marginLeft: 4, marginRight: 4, width: 32 },
          }}
          ellipsisProps={{
            disabled: true,
            weight: 'ghost',
            color: 'neutral',
            shape: 'brick',
            noPadding: true,
            style: { width: 32 },
          }}
          jumpProps={{ weight: 'ghost', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageLabelProps={{ size: 'medium', style: { marginRight: 30 }, variants: ['no-wrap'] }}
          pageLinks={6}
          pageProps={{ weight: 'ghost', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          pageSize={10}
          totalItems={300}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Everything with text

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withEllipsis
          withFirstAndLast
          withPageLabel
          withPageLinks
          withPreviousAndNext
          activePageProps={{ weight: 'solid', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          ellipsisProps={{
            disabled: true,
            weight: 'ghost',
            color: 'neutral',
            shape: 'brick',
            noPadding: true,
            style: { marginLeft: 8, marginRight: 8, width: 32 },
          }}
          jumpProps={{ weight: 'shaded', color: 'primary', shape: 'brick' }}
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageLabelProps={{ size: 'medium', style: { marginRight: 30 }, variants: ['no-wrap'] }}
          pageLinks={6}
          pageProps={{ weight: 'shaded', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          pageSize={10}
          spacing="joined"
          totalItems={300}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Minimal with icons

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={1}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withIcons
          withPreviousAndNext
          jumpProps={{ weight: 'shaded', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          justify="end"
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageSize={10}
          spacing="joined"
          totalItems={128}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Minimal with text

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={1}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withPreviousAndNext
          jumpProps={{
            weight: 'ghost',
            color: 'primary',
            shape: 'brick',
            noPadding: true,
            style: { paddingLeft: 8, paddingRight: 8, marginLeft: 8, marginRight: 8 },
          }}
          justify="end"
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageSize={10}
          totalItems={128}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## Unstyled pagination

### Page links with icons

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withIcons
          withPageLinks
          withPreviousAndNext
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageLinks={6}
          pageSize={10}
          totalItems={300}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Page links with text

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withPageLinks
          withPreviousAndNext
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageLinks={6}
          pageSize={10}
          totalItems={300}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Everything with icons

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withEllipsis
          withFirstAndLast
          withIcons
          withPageLabel
          withPageLinks
          withPreviousAndNext
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageLinks={6}
          pageSize={10}
          totalItems={300}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Everything with text

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withEllipsis
          withFirstAndLast
          withPageLabel
          withPageLinks
          withPreviousAndNext
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageLinks={6}
          pageSize={10}
          totalItems={300}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Minimal with icons

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={1}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withIcons
          withPreviousAndNext
          justify="end"
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageSize={10}
          totalItems={128}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Minimal with text

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={1}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          withPreviousAndNext
          justify="end"
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageSize={10}
          totalItems={128}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Current page only

<Playground>
  <ThemeWrapper>
    <Pagination withPageLabel justify="center" page={8} pageSize={10} totalItems={83} />
    <br />
    <Pagination withPageAndTotalLabel justify="center" page={8} pageSize={10} totalItems={83} />
  </ThemeWrapper>
</Playground>

## Contrast pagination

### Everything with icons

<Playground>
  <ThemeWrapper contrast>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          contrast
          withEllipsis
          withFirstAndLast
          withIcons
          withPageLabel
          withPageLinks
          withPreviousAndNext
          activePageProps={{
            weight: 'shaded',
            color: 'primary',
            shape: 'brick',
            noPadding: true,
            style: { marginLeft: 4, marginRight: 4, width: 32 },
          }}
          ellipsisProps={{
            disabled: true,
            weight: 'ghost',
            color: 'neutral',
            shape: 'brick',
            noPadding: true,
            style: { width: 32 },
          }}
          jumpProps={{ weight: 'ghost', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageLabelProps={{ size: 'medium', style: { marginRight: 30 }, variants: ['no-wrap'] }}
          pageLinks={6}
          pageProps={{ weight: 'ghost', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          pageSize={10}
          totalItems={300}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Everything with text

<Playground>
  <ThemeWrapper contrast>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          contrast
          withEllipsis
          withFirstAndLast
          withPageLabel
          withPageLinks
          withPreviousAndNext
          activePageProps={{ weight: 'solid', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          ellipsisProps={{
            disabled: true,
            weight: 'ghost',
            color: 'neutral',
            shape: 'brick',
            noPadding: true,
            style: { marginLeft: 8, marginRight: 8, width: 32 },
          }}
          jumpProps={{ weight: 'shaded', color: 'primary', shape: 'brick' }}
          onChangePage={(event, pageTo) => setCurrentPage(pageTo)}
          page={currentPage}
          pageLabelProps={{ size: 'medium', style: { marginRight: 30 }, variants: ['no-wrap'] }}
          pageLinks={6}
          pageProps={{ weight: 'shaded', color: 'primary', shape: 'brick', noPadding: true, style: { width: 32 } }}
          pageSize={10}
          spacing="joined"
          totalItems={300}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={Pagination} />
