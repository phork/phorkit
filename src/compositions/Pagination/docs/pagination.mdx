---
name: Pagination
menu: Navigation
route: /compositions/Pagination
---

import { Playground, Props } from 'docz';
import { Pagination } from '../index';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { PageTitle } from 'docs/helpers/PageTitle';

<PageTitle title="Pagination" src="compositions/Pagination" />

## Page links

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          onChangePage={pageTo => setCurrentPage(pageTo)}
          page={currentPage}
          pageLinks={6}
          pageSize={10}
          totalItems={300}
          withPreviousAndNext
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## Current page only

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={8}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          justify="center"
          onChangePage={pageTo => setCurrentPage(pageTo)}
          page={currentPage}
          pageSize={10}
          totalItems={83}
          withFirstAndLast
          withPageLabel
          withPreviousAndNext
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## Minimal

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={1}>
      {({ state: currentPage, setState: setCurrentPage }) => (
        <Pagination
          justify="end"
          onChangePage={pageTo => setCurrentPage(pageTo)}
          page={currentPage}
          pageSize={10}
          totalItems={128}
          withPreviousAndNext
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={Pagination} />
