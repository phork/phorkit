---
name: Toasts
menu: Feedback
route: /compositions/Toasts
---

import { Playground, Props } from 'docz';
import { Toast, ToastConsumer, ToastContainer, ToastProvider, Toasts } from '../index';
import { Button } from 'components/Button';
import { Rhythm } from 'components/Rhythm';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { PageTitle } from 'docs/helpers/PageTitle';

<PageTitle title="Toast" src="compositions/Toast" />

## Basic toast

<Playground>
  <ThemeWrapper>
    <Toast id="demo-basic-toast" immediate>
      This is an example toast.
    </Toast>
  </ThemeWrapper>
</Playground>

## Toast with a title

<Playground>
  <ThemeWrapper>
    <Toast id="demo-title-toast" title="Hello world" level="success" immediate>
      This is a success toast.
    </Toast>
  </ThemeWrapper>
</Playground>

## Full context

<Playground>
  <ThemeWrapper>
    <StateWrapper>
      {({ state: variant, setState: setVariant }) => (
        <Toasts position="top-right" variant={variant}>
          <ToastConsumer>
            {({ createNotification, clearNotifications }) => (
              <Rhythm m={2}>
                <Button
                  color="primary"
                  onClick={() =>
                    createNotification(
                      <Toast level="info" title="Hello world">
                        This is an info toast.
                      </Toast>,
                    )
                  }
                >
                  Info toast
                </Button>
                <Button
                  color="danger"
                  onClick={() =>
                    createNotification(
                      <Toast level="danger" title="Hello world">
                        This is a danger toast.
                      </Toast>,
                    )
                  }
                >
                  Danger toast
                </Button>
                <Button
                  color="warning"
                  onClick={() =>
                    createNotification(
                      <Toast level="warning" title="Hello world" permanent>
                        This is a permanent warning toast.
                      </Toast>,
                    )
                  }
                >
                  Warning toast
                </Button>
                <Button
                  color="success"
                  onClick={() =>
                    createNotification(
                      <Toast id="demo-success" level="success" title="Hello world">
                        This is an overwriting success toast.
                        <div
                          style={{ position: 'absolute', bottom: '4px', right: '8px', fontSize: '10px', opacity: 0.4 }}
                        >
                          {Date.now()}
                        </div>
                      </Toast>,
                    )
                  }
                >
                  Success toast
                </Button>
                <Button color="neutral" weight="outline" onClick={() => clearNotifications()}>
                  Clear all
                </Button>
                <Button color="neutral" onClick={() => setVariant(variant === 'colored' ? undefined : 'colored')}>
                  Toggle variant
                </Button>
              </Rhythm>
            )}
          </ToastConsumer>
        </Toasts>
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## \[props\]

### Toast

<Props of={Toast} />

### ToastContainer

<Props of={ToastContainer} />

### ToastProvider

<Props of={ToastProvider} />

### Toasts

<Props of={Toasts} />
