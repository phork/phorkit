---
name: Navigation
menu: Navigation
route: /compositions/Navigation
---

import { Playground, Props } from 'docz';
import { PhorkIcon } from 'icons/PhorkIcon';
import { Rhythm } from 'components/Rhythm';
import { Typography } from 'components/Typography';
import { PageTitle } from 'docs/helpers/PageTitle';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Navigation, StyledNavigation } from '../index';

<PageTitle src="compositions/Navigation" title="Navigation" />

Keyboard navigation is available for this component. Navigate with `Up`, `Down`, `Left`, `Right`, `Home` or `End`. Select with `Space` or `Enter`.

## Basic navigation

### Horizontal navigation

<Playground>
  <ThemeWrapper>
    <div style={{ height: '50px' }}>
      <StateWrapper initialState={'first'}>
        {({ state: selectedId, setState: setSelectedId }) => (
          <Navigation
            items={[
              { id: 'first', label: 'First' },
              { id: 'second', label: 'Second' },
              { id: 'third', label: 'Third', disabled: true },
              { id: 'fourth', label: 'Fourth' },
            ]}
            onSelect={(event, item) => {
              setSelectedId(item.id);
              console.log(item.id);
            }}
            selectedId={selectedId}
          />
        )}
      </StateWrapper>
    </div>
  </ThemeWrapper>
</Playground>

### Vertical navigation

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={'first'}>
      {({ state: selectedId, setState: setSelectedId }) => (
        <Navigation
          items={[
            { id: 'first', label: 'First' },
            { id: 'second', label: 'Second' },
            { id: 'third', label: 'Third', disabled: true },
            { id: 'fourth', label: 'Fourth' },
          ]}
          onSelect={(event, item) => setSelectedId(item.id)}
          orientation="vertical"
          selectedId={selectedId}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## Animated navigation

### Horizontal navigation

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={'second'}>
      {({ state: selectedId, setState: setSelectedId }) => (
        <Navigation
          animated
          items={[
            { id: 'first', label: 'First' },
            { id: 'second', label: 'Second' },
            { id: 'third', label: 'Third', disabled: true },
            { id: 'fourth', label: 'Fourth' },
          ]}
          onSelect={(event, item) => setSelectedId(item.id)}
          selectedId={selectedId}
          style={{ height: '50px' }}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Vertical navigation

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={'second'}>
      {({ state: selectedId, setState: setSelectedId }) => (
        <Navigation
          animated
          items={[
            { id: 'first', label: 'First' },
            { id: 'second', label: 'Second' },
            { id: 'third', label: 'Third', disabled: true },
            { id: 'fourth', label: 'Fourth' },
          ]}
          onSelect={(event, item) => setSelectedId(item.id)}
          orientation="vertical"
          selectedId={selectedId}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## Secondary navigation

### Horizontal navigation

<Playground>
  <ThemeWrapper>
    <div style={{ height: '50px' }}>
      <StateWrapper initialState={'first'}>
        {({ state: selectedId, setState: setSelectedId }) => (
          <Navigation
            animated
            items={[
              { id: 'first', label: 'First' },
              { id: 'second', label: 'Second' },
              { id: 'third', label: 'Third', disabled: true },
              { id: 'fourth', label: 'Fourth' },
            ]}
            onSelect={(event, item) => setSelectedId(item.id)}
            selectedId={selectedId}
            variant="secondary"
          />
        )}
      </StateWrapper>
    </div>
  </ThemeWrapper>
</Playground>

### Vertical navigation

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={'first'}>
      {({ state: selectedId, setState: setSelectedId }) => (
        <Navigation
          animated
          items={[
            { id: 'first', label: 'First' },
            { id: 'second', label: 'Second' },
            { id: 'third', label: 'Third', disabled: true },
            { id: 'fourth', label: 'Fourth' },
          ]}
          onSelect={(event, item) => setSelectedId(item.id)}
          orientation="vertical"
          selectedId={selectedId}
          variant="secondary"
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## Tertiary navigation

### Horizontal navigation

<Playground>
  <ThemeWrapper>
    <div style={{ height: '50px' }}>
      <StateWrapper initialState={'first'}>
        {({ state: selectedId, setState: setSelectedId }) => (
          <Navigation
            animated
            items={[
              { id: 'first', label: 'First' },
              { id: 'second', label: 'Second' },
              { id: 'third', label: 'Third', disabled: true },
              { id: 'fourth', label: 'Fourth' },
            ]}
            onSelect={(event, item) => setSelectedId(item.id)}
            selectedId={selectedId}
            variant="tertiary"
          />
        )}
      </StateWrapper>
    </div>
  </ThemeWrapper>
</Playground>

### Vertical navigation

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={'first'}>
      {({ state: selectedId, setState: setSelectedId }) => (
        <Navigation
          animated
          items={[
            { id: 'first', label: 'First' },
            { id: 'second', label: 'Second' },
            { id: 'third', label: 'Third', disabled: true },
            { id: 'fourth', label: 'Fourth' },
          ]}
          onSelect={(event, item) => setSelectedId(item.id)}
          orientation="vertical"
          selectedId={selectedId}
          variant="tertiary"
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Vertical icon navigation with border radius

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={'first'}>
      {({ state: selectedId, setState: setSelectedId }) => (
        <Navigation
          animated
          highlightRadius={4}
          items={[
            {
              id: 'first',
              label: <PhorkIcon size={24} />,
              flush: true,
              style: { padding: '8px', marginBottom: '4px' },
            },
            {
              id: 'second',
              label: <PhorkIcon size={24} />,
              flush: true,
              style: { padding: '8px', marginBottom: '4px' },
            },
            {
              id: 'third',
              label: <PhorkIcon size={24} />,
              flush: true,
              style: { padding: '8px', marginBottom: '4px' },
              disabled: true,
            },
            {
              id: 'fourth',
              label: <PhorkIcon size={24} />,
              flush: true,
              style: { padding: '8px' },
            },
          ]}
          onSelect={(event, item) => setSelectedId(item.id)}
          orientation="vertical"
          selectedId={selectedId}
          variant="tertiary"
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Navigation with custom labels

<Playground>
  <ThemeWrapper>
    <div style={{ height: '50px' }}>
      <StateWrapper initialState={'first'}>
        {({ state: selectedId, setState: setSelectedId }) => (
          <Navigation
            animated
            highlightRadius={4}
            items={[
              {
                id: 'first',
                label: (
                  <Rhythm px={2}>
                    <Typography size="xsmall" variants={['letter-spacing-comfy', 'uppercase']}>
                      First
                    </Typography>
                  </Rhythm>
                ),
              },
              {
                id: 'second',
                label: (
                  <Rhythm px={2}>
                    <Typography size="xsmall" variants={['letter-spacing-comfy', 'uppercase']}>
                      Second
                    </Typography>
                  </Rhythm>
                ),
              },
              {
                id: 'third',
                label: (
                  <Rhythm px={2}>
                    <Typography size="xsmall" variants={['letter-spacing-comfy', 'uppercase']}>
                      Third
                    </Typography>
                  </Rhythm>
                ),
                disabled: true,
              },
              {
                id: 'fourth',
                label: (
                  <Rhythm px={2}>
                    <Typography size="xsmall" variants={['letter-spacing-comfy', 'uppercase']}>
                      Fourth
                    </Typography>
                  </Rhythm>
                ),
              },
            ]}
            onSelect={(event, item) => setSelectedId(item.id)}
            selectedId={selectedId}
            variant="tertiary"
          />
        )}
      </StateWrapper>
    </div>
  </ThemeWrapper>
</Playground>

## Variations

### Navigation with triggerOnly

<Playground>
  <StateWrapper initialState={'second'}>
    {({ state: selectedId, setState: setSelectedId }) => (
      <Navigation
        animated
        items={[
          { id: 'first', label: 'First' },
          { id: 'second', label: 'Second' },
          { id: 'third', label: 'Third', disabled: true },
          { id: 'fourth', label: 'Trigger', triggerOnly: () => console.log('triggered') },
        ]}
        onSelect={(event, item) => setSelectedId(item.id)}
        selectedId={selectedId}
        style={{ height: '50px' }}
      />
    )}
  </StateWrapper>
</Playground>

### Navigation as links

_Note that the links will cause the page to reload so the state will appear unchanged._

<Playground>
  <ThemeWrapper>
    <Navigation
      animated
      triggerLinks
      items={[
        {
          id: 'first',
          label: (
            <a href="#first" tabIndex={-1}>
              First
            </a>
          ),
        },
        {
          id: 'second',
          label: (
            <a href="#second" tabIndex={-1}>
              Second
            </a>
          ),
        },
        {
          id: 'third',
          label: (
            <a href="#third" tabIndex={-1}>
              Third
            </a>
          ),
          disabled: true,
        },
        {
          id: 'fourth',
          label: (
            <a href="#fourth" tabIndex={-1}>
              Fourth
            </a>
          ),
        },
      ]}
      selectedId={'first'}
      style={{ height: '50px' }}
    />
  </ThemeWrapper>
</Playground>

### Navigation with links for right click only

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={'second'}>
      {({ state: selectedId, setState: setSelectedId }) => (
        <Navigation
          allowRightClickLinks
          animated
          items={[
            { id: 'first', label: 'First', href: '#first' },
            { id: 'second', label: 'Second', href: '#second' },
            { id: 'third', label: 'Third', href: '#third', disabled: true },
            { id: 'fourth', label: 'Fourth', href: '#fourth' },
            { id: 'fifth', label: 'Trigger', triggerOnly: () => console.log('triggered') },
          ]}
          onSelect={(event, item) => setSelectedId(item.id)}
          selectedId={selectedId}
          style={{ height: '50px' }}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Navigation with label functions

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={'second'}>
      {({ state: selectedId, setState: setSelectedId }) => (
        <Navigation
          animated
          items={[
            { id: 'first', label: ({ focused }) => `First${focused ? ' is focused' : ''}` },
            { id: 'second', label: ({ focused }) => `Second${focused ? ' is focused' : ''}` },
            { id: 'third', label: ({ focused }) => `Third${focused ? ' is focused' : ''}`, disabled: true },
            { id: 'fourth', label: ({ focused }) => `Fourth${focused ? ' is focused' : ''}` },
          ]}
          onSelect={(event, item) => setSelectedId(item.id)}
          selectedId={selectedId}
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

### Styled navigation

<Playground>
  <StateWrapper initialState={'second'}>
    {({ state: selectedId, setState: setSelectedId }) => (
      <StyledNavigation
        animated
        focusedBorderColor="#f41150"
        itemFocusedBackgroundColor="#f41150"
        itemFocusedSelectedBackgroundColor="#F86F95"
        itemFocusedSelectedTextColor="#fff"
        itemFocusedTextColor="#fff"
        itemHoveredBorderColor="#f41150"
        itemSelectedBackgroundColor="#F86F95"
        itemSelectedBorderColor="#f41150"
        itemSelectedTextColor="#fff"
        itemTextColor="#f41150"
        items={[
          { id: 'first', label: ({ focused }) => `First${focused ? ' is focused' : ''}` },
          { id: 'second', label: ({ focused }) => `Second${focused ? ' is focused' : ''}` },
          { id: 'third', label: ({ focused }) => `Third${focused ? ' is focused' : ''}`, disabled: true },
          { id: 'fourth', label: ({ focused }) => `Fourth${focused ? ' is focused' : ''}` },
        ]}
        onSelect={(event, item) => setSelectedId(item.id)}
        selectedId={selectedId}
      />
    )}
  </StateWrapper>
</Playground>

### Example with `react-router-dom`

```
import React, { useCallback, useMemo } from 'react';
import PropTypes from 'prop-types';
import { withRouter } from 'react-router-dom';
import { Navigation } from '@phork/phorkit';

export function GlobalNavigation({ history, location }) {
  const items = useMemo(
    () => [
      {
        id: 'dashboard',
        selected: location => location.pathname === '/',
        url: '/',
        label: 'Dashboard',
      },
      {
        id: 'awesome',
        selected: location => location.pathname.startsWith('/awesome'),
        url: '/awesome',
        label: 'Awesome',
      },
      {
        id: 'example',
        selected: location => location.pathname.startsWith('/example'),
        url: '/example',
        label: 'Example',
      },
      {
        id: 'things',
        selected: location => location.pathname.startsWith('/things'),
        url: '/things',
        label: 'Things',
      },
    ],
    [],
  );

  const handleSelect = useCallback((event, { url }) => history.push(url), [history]);

  return (
    <Navigation
      animated
      fullWidth
      onSelect={handleSelect}
      selectedId={items.find(({ selected }) => selected(location)).id}
      items={items}
    />
  );
}

GlobalNavigation.propTypes = {
  history: PropTypes.shape({
    push: PropTypes.func.isRequired,
  }).isRequired,
  location: PropTypes.object.isRequired,
};

export const EnhancedGlobalNavigation = withRouter(GlobalNavigation);
```

## \[props\]

### Navigation

<Props of={Navigation} />

### StyledNavigation

<Props of={StyledNavigation} />
