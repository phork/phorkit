---
name: Dropdown
menu: Input
route: /compositions/Dropdown
---

import { Playground, Props } from 'docz';
import { themes } from 'config/themes';
import { SearchIcon } from 'icons/SearchIcon';
import { TagIcon } from 'icons/internal/TagIcon';
import { Flex } from 'components/Flex';
import { Rhythm } from 'components/Rhythm';
import { ListProps } from 'docs/helpers/ListProps';
import { Looper } from 'docs/helpers/Looper';
import { PageTitle } from 'docs/helpers/PageTitle';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Dropdown, DropdownContent, DropdownEmpty, ControlledDropdown, NotifiedControlledDropdown } from '../index';
import { MultiVariantDropdown } from './helpers/MultiVariantDropdown';
import { options, handleFilter, handleEmptyFilter } from './helpers/options';
import { variants } from './helpers/variants.js';

<PageTitle title="Dropdown" src="compositions/Dropdown" />

Keyboard navigation is available for this component. Navigate with `Up`, `Down`, `Left`, `Right`, `Home` or `End`. Toggle with `Space`. Select with `Enter`. Close with `Escape`.

## Raised dropdown

<Playground>
  <ThemeWrapper>
    <Flex direction="row" justifyContent="space-between" full wrap>
      <Looper
        list={variants}
        render={variant => (
          <div style={{ height: 300, width: '30%', maxWidth: 300, minWidth: 200, marginLeft: 20, marginRight: 20 }}>
            <ControlledDropdown
              initialSelected={[options[3]]}
              inputVariant={variant}
              label="Super awesome dropdown"
              layout="raised"
              onItemFocus={(event, value) => console.log('focus', value)}
              onSelect={value => console.log('select', value)}
              onUnselect={value => console.log('unselect', value)}
              onSelectionChange={value => console.log('selection change', value)}
              options={options}
              transitional
            />
          </div>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

## Contained dropdown

<Playground>
  <ThemeWrapper>
    <br />
    <Flex direction="row" justifyContent="space-between" full wrap>
      <Looper
        list={variants}
        render={variant => (
          <div style={{ height: 300, width: '30%', maxWidth: 300, minWidth: 200, marginLeft: 20, marginRight: 20 }}>
            <ControlledDropdown
              initialSelected={[options[3]]}
              inputVariant={variant}
              label="Super awesome dropdown"
              layout="contained"
              onSelect={value => console.log('select', value)}
              onUnselect={value => console.log('unselect', value)}
              onSelectionChange={value => console.log('selection change', value)}
              options={options}
              transitional
            />
          </div>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

## Dropdown with placeholder

<Playground>
  <ThemeWrapper>
    <br />
    <Flex direction="row" justifyContent="space-between" full wrap>
      <Looper
        list={variants}
        render={variant => (
          <div style={{ height: 300, width: '30%', maxWidth: 300, minWidth: 200, marginLeft: 20, marginRight: 20 }}>
            <ControlledDropdown
              inputVariant={variant}
              label="Super awesome dropdown"
              layout="contained"
              onSelect={value => console.log('select', value)}
              onUnselect={value => console.log('unselect', value)}
              onSelectionChange={value => console.log('selection change', value)}
              options={options}
              placeholder="Select one"
              transitional
            />
          </div>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

## Multi-select dropdown

<Playground>
  <ThemeWrapper>
    <br />
    <Flex direction="row" justifyContent="space-between" full wrap>
      <Looper
        list={variants}
        render={variant => (
          <div style={{ height: 300, width: '30%', maxWidth: 300, minWidth: 200, marginLeft: 20, marginRight: 20 }}>
            <ControlledDropdown
              initialSelected={[options[3]]}
              inputVariant={variant}
              label="Super awesome dropdown"
              layout="contained"
              maxSelect={-1}
              onSelect={value => console.log('select', value)}
              onUnselect={value => console.log('unselect', value)}
              onSelectionChange={value => console.log('selection change', value)}
              options={options}
              transitional
            />
          </div>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

## Multi-select dropdown with minSelect, maxSelect

<Playground>
  <ThemeWrapper>
    <br />
    <Flex direction="row" justifyContent="space-between" full wrap>
      <Looper
        list={variants}
        render={variant => (
          <div style={{ height: 300, width: '30%', maxWidth: 300, minWidth: 200, marginLeft: 20, marginRight: 20 }}>
            <ControlledDropdown
              initialSelected={[options[3], options[4], options[5]]}
              inputVariant={variant}
              label="Super awesome dropdown"
              layout="contained"
              maxSelect={3}
              minSelect={1}
              onSelect={value => console.log('select', value)}
              onUnselect={value => console.log('unselect', value)}
              onSelectionChange={value => console.log('selection change', value)}
              options={options}
              transitional
            />
          </div>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

## Filterable dropdown

<Playground>
  <ThemeWrapper>
    <Flex direction="row" justifyContent="space-between" full wrap>
      <Looper
        list={variants}
        render={variant => (
          <div style={{ height: 300, width: '30%', maxWidth: 300, minWidth: 200, marginLeft: 20, marginRight: 20 }}>
            <ControlledDropdown
              getFilteredOptions={handleFilter}
              initialSelected={[options[3]]}
              inputVariant={variant}
              label="Super awesome dropdown"
              onClear={value => console.log('clear')}
              onInputChange={value => console.log(value)}
              onSelect={value => console.log('select', value)}
              onSubmit={(event, value) =>
                !options.some(option => option.id === value) && options.push({ id: value, label: value })
              }
              options={options}
              transitional
            />
          </div>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

## Searchable dropdown

A searchable dropdown is similar to a filterable dropdown except that it starts with no initial values.

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState={''}>
      {({ state: filter, setState: setFilter }) => (
        <div style={{ height: 300, maxWidth: 300 }}>
          <ControlledDropdown
            emptyNotification={filter ? `No options were found for "${filter}".` : ''}
            getFilteredOptions={handleFilter}
            label="Super awesome dropdown"
            onClear={value => console.log('clear')}
            onInputChange={setFilter}
            onSelect={value => console.log('select', value)}
            onSubmit={(event, value) =>
              !options.some(option => option.id === value) && options.push({ id: value, label: value })
            }
            searchable
            transitional
          />
        </div>
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## Dropdown without options

<Playground>
  <ThemeWrapper style={{ height: '300px' }}>
    <div style={{ maxWidth: 300 }}>
      <ControlledDropdown
        label="Super awesome dropdown"
        onSelect={value => console.log('select', value)}
        options={[]}
        transitional
      />
    </div>
  </ThemeWrapper>
</Playground>

### Filterable dropdown without options

<Playground>
  <ThemeWrapper style={{ height: '300px' }}>
    <div style={{ maxWidth: 300 }}>
      <ControlledDropdown
        getFilteredOptions={handleEmptyFilter}
        iconBefore={<SearchIcon size={14} />}
        label="Super awesome dropdown"
        onInputChange={value => console.log('change', value)}
        onSelect={value => console.log('select', value)}
        options={[]}
        transitional
      />
    </div>
  </ThemeWrapper>
</Playground>

## Variations

### Danger dropdown

<Playground>
  <ThemeWrapper>
    <Flex direction="row" justifyContent="space-between" full wrap>
      <Looper
        list={variants}
        render={variant => (
          <div style={{ height: 300, width: '30%', maxWidth: 300, minWidth: 200, marginLeft: 20, marginRight: 20 }}>
            <ControlledDropdown
              id="demo-danger-dropdown"
              initialSelected={[options[3]]}
              inputVariant={variant}
              label="Super awesome dropdown"
              onSelect={value => console.log('select', value)}
              options={options}
              validity="danger"
            />
          </div>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

### Warning dropdown

<Playground>
  <ThemeWrapper>
    <Flex direction="row" justifyContent="space-between" full wrap>
      <Looper
        list={variants}
        render={variant => (
          <div style={{ height: 300, width: '30%', maxWidth: 300, minWidth: 200, marginLeft: 20, marginRight: 20 }}>
            <ControlledDropdown
              initialSelected={[options[3]]}
              inputVariant={variant}
              label="Super awesome dropdown"
              onInputChange={value => console.log('change', value)}
              onSelect={value => console.log('select', value)}
              options={options}
              transitional
              validity="warning"
            />
          </div>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

### Success dropdown

<Playground>
  <ThemeWrapper>
    <Flex direction="row" justifyContent="space-between" full wrap>
      <Looper
        list={variants}
        render={variant => (
          <div style={{ height: 300, width: '30%', maxWidth: 300, minWidth: 200, marginLeft: 20, marginRight: 20 }}>
            <ControlledDropdown
              initialSelected={[options[3]]}
              inputVariant={variant}
              label="Super awesome dropdown"
              onSelect={value => console.log('select', value)}
              options={options}
              transitional
              validity="success"
            />
          </div>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

### Contrast dropdown

<Playground>
  <ThemeWrapper contrast>
    <Flex direction="row" justifyContent="space-between" full wrap>
      <Looper
        list={variants}
        render={variant => (
          <div style={{ height: 300, width: '30%', maxWidth: 300, minWidth: 200, marginLeft: 20, marginRight: 20 }}>
            <ControlledDropdown
              contrast
              initialSelected={[options[3]]}
              inputVariant={variant}
              label="Super awesome dropdown"
              onSelect={value => console.log('select', value)}
              options={options}
              transitional
            />
          </div>
        )}
      />
    </Flex>
  </ThemeWrapper>
</Playground>

### Styled dropdown

<Playground>
  <div style={{ height: 300, maxWidth: 300, marginLeft: 20, marginRight: 20 }}>
    <ControlledDropdown
      id="demo-contrast-dropdown"
      initialSelected={[options[3]]}
      label="Super awesome dropdown"
      layout="contained"
      onSelect={value => console.log('select', value)}
      options={options}
      style={{
        '--dropdown-options-bottom-border-color': '#9D69D5',
        '--dropdown-options-focused-border-color': '#57278C',
        '--dropdown-options-contained-background-color': '#fff',
        '--dropdown-options-contained-box-shadow': '0px 1px 2px 0px rgba(0, 0, 0, 0.3)',
        '--dropdown-options-contained-border-color': '#B995E1',
        '--dropdown-options-raised-box-shadow': '0px 1px 2px 0px rgba(0, 0, 0, 0.3)',
        '--dropdown-options-scrollbar-thumb-color': '#DDDDE0',
        '--list-focused-border-color': '#642da0',
        '--list-item-selected-background-color': '#642da0',
        '--list-item-selected-text-color': '#ffffff',
        '--list-item-background-color': 'transparent',
        '--list-item-border-color': '#642da0',
        '--list-item-focused-border-color': '#57278C',
        '--list-item-focused-text-color': '#642da0',
        '--list-item-text-color': '#B995E1',
        '--formbox-icon-color': '#9D69D5',
        '--formbox-icon-hovered-color': '#642da0',
        '--formbox-input-container-background-color': 'transparent',
        '--formbox-input-container-border-color': '#9D69D5',
        '--formbox-input-container-focused-border-color': '#57278C',
        '--formbox-input-container-hovered-border-color': '#642da0',
        '--formbox-input-text-color': '#642da0',
        '--formbox-label-text-color': '#B995E1',
      }}
      unthemed
      variant="underline"
    />
  </div>
</Playground>

### Multi-variant dropdown

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <div style={{ height: 300, maxWidth: 300, marginLeft: 20, marginRight: 20 }}>
        <MultiVariantDropdown
          getFilteredOptions={handleFilter}
          iconBefore={<SearchIcon size={14} />}
          initialSelected={[options[3]]}
          label="Super awesome dropdown"
          layout="contained"
          onClear={value => console.log('clear')}
          onInputChange={value => console.log('change', value)}
          onSelect={value => console.log('select', value)}
          options={options}
          outline="unboxed"
          style={{ width: '100%', '--contrast-color': themes[themeId]['color-FG40'] }}
        />
      </div>
    )}
  </ThemeWrapper>
</Playground>

## Notifications

<Playground>
  <ThemeWrapper>
    <Looper
      list={['success', 'danger', 'warning', 'primary', 'neutral']}
      render={level => (
        <Rhythm py={3}>
          <Flex direction="row" justifyContent="space-between" full wrap>
            <Looper
              list={variants}
              render={variant => (
                <div
                  style={{ height: 300, width: '30%', maxWidth: 300, minWidth: 200, marginLeft: 20, marginRight: 20 }}
                >
                  <NotifiedControlledDropdown
                    initialSelected={[options[3]]}
                    inputVariant={variant}
                    label="Super awesome dropdown"
                    layout="raised"
                    onItemFocus={(event, value) => console.log('focus', value)}
                    onSelect={value => console.log('select', value)}
                    onUnselect={value => console.log('unselect', value)}
                    onSelectionChange={value => console.log('selection change', value)}
                    options={options}
                    transitional
                    level={level}
                    notification={`This is an example ${level} notification.`}
                  />
                </div>
              )}
            />
          </Flex>
        </Rhythm>
      )}
    />
  </ThemeWrapper>
</Playground>

<div style={{ height: 50 }} />

<Playground>
  <ThemeWrapper style={{ height: '300px' }}>
    <div style={{ maxWidth: 300 }}>
      <NotifiedControlledDropdown
        label="Super awesome dropdown"
        onSelect={value => console.log('select', value)}
        options={[]}
        transitional
        level="primary"
        notification={`This dropdown has no options.`}
      />
    </div>
  </ThemeWrapper>
</Playground>

## DropdownEmpty

### Notification types

<Playground>
  <ThemeWrapper>
    <h3>String</h3>
    <DropdownEmpty>This is a string notification</DropdownEmpty>

    <h3>Component</h3>
    <DropdownEmpty filter="Foobar"><ListProps /></DropdownEmpty>

    <h3>Function</h3>
    <DropdownEmpty filter="Barfoo">{({ filter }) => `This is a functional notification passed "${filter}"`}</DropdownEmpty>

  </ThemeWrapper>
</Playground>

## \[props\]

### Dropdown

<Props of={Dropdown} />

### ControlledDropdown

<Props of={ControlledDropdown} />

### DropdownEmpty

<Props of={DropdownEmpty} />
