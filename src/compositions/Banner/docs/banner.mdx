---
name: Banners
menu: Feedback
route: /compositions/Banners
---

import { Playground, Props } from 'docz';
import { Button } from 'components/Button';
import { Rhythm } from 'components/Rhythm';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Banner, BannerConsumer, BannerContainer, BannerFromContext, Banners, BannersFromContext } from '../index';

<PageTitle title="Banner" src="compositions/Banner" />

## Page banners

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <BannerContainer themeId={themeId}>
        <Banner immediate level="primary" onClose={() => console.log('close banner')}>
          This is a primary banner.
        </Banner>
        <Banner immediate level="primary" onClose={() => console.log('close banner')}>
          This is another primary banner. It has a border to distinguish it from the one above.
        </Banner>
        <Banner immediate level="secondary" onClose={() => console.log('close banner')}>
          This is a secondary banner.
        </Banner>
        <Banner immediate level="secondary" onClose={() => console.log('close banner')}>
          This is another secondary banner. It has a border to distinguish it from the one above.
        </Banner>
        <Banner immediate level="tertiary" onClose={() => console.log('close banner')}>
          This is a tertiary banner.
        </Banner>
        <Banner immediate level="tertiary" onClose={() => console.log('close banner')}>
          This is another tertiary banner. It has a border to distinguish it from the one above.
        </Banner>
        <Banner immediate level="transparent" onClose={() => console.log('close banner')}>
          This is a transparent banner.
        </Banner>
        <Banner immediate level="transparent" onClose={() => console.log('close banner')}>
          This is another transparent banner. It has a border to distinguish it from the one above.
        </Banner>
        <Banner immediate level="transparent">
          This banner can't be removed because it doesn't have an onClose prop.
        </Banner>
      </BannerContainer>
    )}
  </ThemeWrapper>
</Playground>

## State banners

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <BannerContainer themeId={themeId}>
        <Banner immediate level="info" onClose={() => console.log('close banner')}>
          This is an info banner because something interesting happened. Exciting!
        </Banner>
        <Banner immediate level="info" onClose={() => console.log('close banner')}>
          This is another info banner. It has a border to distinguish it from the one above.
        </Banner>
        <Banner immediate level="danger" onClose={() => console.log('close banner')}>
          This is a danger banner because something bad happened. Uh oh!
        </Banner>
        <Banner immediate level="danger" onClose={() => console.log('close banner')}>
          This is another danger banner. It has a border to distinguish it from the one above.
        </Banner>
        <Banner immediate level="warning" onClose={() => console.log('close banner')}>
          This is a warning banner because something not so great happened. Meh.
        </Banner>
        <Banner immediate level="warning" onClose={() => console.log('close banner')}>
          This is another warning banner. It has a border to distinguish it from the one above.
        </Banner>
        <Banner immediate level="success" onClose={() => console.log('close banner')}>
          This is a success banner because something excellent happened. Yay!
        </Banner>
        <Banner immediate level="success" onClose={() => console.log('close banner')}>
          This is another success banner. It has a border to distinguish it from the one above.
        </Banner>
        <Banner immediate level="contrast" onClose={() => console.log('close banner')}>
          This is a contrast banner.
        </Banner>
        <Banner immediate level="contrast" onClose={() => console.log('close banner')}>
          This is another contrast banner. It has a border to distinguish it from the one above.
        </Banner>
      </BannerContainer>
    )}
  </ThemeWrapper>
</Playground>

## Full context

<Playground>
  <ThemeWrapper>
    <Banners style={{ marginTop: '18px' }}>
      <BannerConsumer>
        {({ createNotification, clearNotifications }) => (
          <Rhythm m={2}>
            <Button
              color="primary"
              onClick={() =>
                createNotification(
                  <Banner level="info">
                    This is an info banner because something interesting happened. Exciting!
                  </Banner>,
                )
              }
            >
              Info banner
            </Button>
            <Button
              color="danger"
              onClick={() =>
                createNotification(
                  <Banner level="danger">This is a danger banner because something bad happened. Uh oh!</Banner>,
                )
              }
            >
              Danger banner
            </Button>
            <Button
              color="warning"
              onClick={() =>
                createNotification(
                  <Banner level="warning" permanent>
                    This is a permanent warning banner because something not so great happened. Meh.
                  </Banner>,
                )
              }
            >
              Warning banner
            </Button>
            <Button
              color="success"
              onClick={() =>
                createNotification(
                  <Banner id="demo-success" level="success">
                    This is an overwriting success banner because something excellent happened. Yay!
                    <div style={{ position: 'absolute', right: '8px', fontSize: '10px', opacity: 0.4 }}>
                      {Date.now()}
                    </div>
                  </Banner>,
                )
              }
            >
              Success banner
            </Button>
            <Button color="neutral" onClick={() => clearNotifications()}>
              Clear all
            </Button>
          </Rhythm>
        )}
      </BannerConsumer>
    </Banners>
  </ThemeWrapper>
</Playground>

## Contextual banners with custom padding

<Playground>
  <ThemeWrapper>
    <Banners style={{ marginTop: '18px' }} bannerStyle={{ paddingLeft: '24px' }}>
      <BannerConsumer>
        {({ createNotification, clearNotifications }) => (
          <Rhythm m={2}>
            <Button
              color="primary"
              onClick={() =>
                createNotification(
                  <Banner level="info">
                    This is an info banner because something interesting happened. Exciting!
                  </Banner>,
                )
              }
            >
              Info banner
            </Button>
            <Button
              color="danger"
              onClick={() =>
                createNotification(
                  <Banner level="danger">This is a danger banner because something bad happened. Uh oh!</Banner>,
                )
              }
            >
              Danger banner
            </Button>
            <Button
              color="warning"
              onClick={() =>
                createNotification(
                  <Banner level="warning" permanent>
                    This is a permanent warning banner because something not so great happened. Meh.
                  </Banner>,
                )
              }
            >
              Warning banner
            </Button>
            <Button
              color="success"
              onClick={() =>
                createNotification(
                  <Banner id="demo-success" level="success">
                    This is an overwriting success banner because something excellent happened. Yay!
                    <div style={{ position: 'absolute', right: '8px', fontSize: '10px', opacity: 0.4 }}>
                      {Date.now()}
                    </div>
                  </Banner>,
                )
              }
            >
              Success banner
            </Button>
            <Button color="neutral" onClick={() => clearNotifications()}>
              Clear all
            </Button>
          </Rhythm>
        )}
      </BannerConsumer>
    </Banners>
  </ThemeWrapper>
</Playground>

## \[props\]

### Banner

<Props of={Banner} />

### BannerContainer

<Props of={BannerContainer} />

## \[context\]

### Banners

<Props of={Banners} />

### BannersFromContext

<Props of={BannersFromContext} />

### BannerFromContext

<Props of={BannerFromContext} />
