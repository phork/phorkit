---
name: TextboxGroup
menu: Forms
route: /compositions/TextboxGroup
---

import { Playground, Props } from 'docz';
import { FormboxContainer } from 'components/Form/Formbox';
import { Looper } from 'components/Looper';
import { Rhythm } from 'components/Rhythm';
import { PageTitle } from 'docs/helpers/PageTitle';
import { StateWrapper } from 'docs/helpers/StateWrapper';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { TextboxGroup2FA } from '../index';
import { variants } from './helpers/variants';

<PageTitle src="compositions/TextboxGroup" title="TextboxGroup" />

## Two factor authentication

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <StateWrapper initialState="">
          {({ state, setState }) => (
            <Rhythm py={3}>
              <TextboxGroup2FA
                inputWidth={64}
                length={6}
                onChange={(event, value) => setState(value)}
                size="7xlarge"
                value={state}
                variant={variant}
              />
            </Rhythm>
          )}
        </StateWrapper>
      )}
    />
  </ThemeWrapper>
</Playground>

## Two factor authentication, labeled

<Playground>
  <ThemeWrapper>
    <Looper
      list={variants}
      render={variant => (
        <StateWrapper initialState="">
          {({ state, setState }) => (
            <Rhythm grouped py={3}>
              <FormboxContainer
                focused
                as="label"
                label="Two-factor code"
                size="7xlarge"
                type="input"
                variant="minimal"
              >
                <TextboxGroup2FA
                  inputWidth={64}
                  length={6}
                  onChange={(event, value) => setState(value)}
                  size="7xlarge"
                  value={state}
                  variant={variant}
                />
              </FormboxContainer>
            </Rhythm>
          )}
        </StateWrapper>
      )}
    />
  </ThemeWrapper>
</Playground>

## Two factor authentication, joined

<Playground>
  <ThemeWrapper>
    <StateWrapper initialState="">
      {({ state, setState }) => (
        <TextboxGroup2FA
          inputWidth={32}
          length={6}
          onChange={(event, value) => setState(value)}
          size="3xlarge"
          style={{ '--textbox-group-input-spacing': '0px' }}
          value={state}
          variant="filled"
        />
      )}
    </StateWrapper>
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={TextboxGroup2FA} />
