---
name: IconCount
menu: Display
route: /compositions/IconCount
---

import { Playground, Props } from 'docz';
import { BlobbrIcon } from 'icons/internal/BlobbrIcon';
import { CheckIcon } from 'icons/internal/CheckIcon';
import { Flex } from 'components/Flex';
import { Rhythm } from 'components/Rhythm';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { IconCount } from '../index';

# IconCount

## Basic IconCount

<Playground>
  <ThemeWrapper>
    <IconCount count={24} icon={BlobbrIcon} label="Example" />
  </ThemeWrapper>
</Playground>

## Active IconCount

<Playground>
  <ThemeWrapper>
    <IconCount active count={24} icon={BlobbrIcon} label="Example" />
  </ThemeWrapper>
</Playground>

## Reversed IconCount

<Playground>
  <ThemeWrapper>
    <IconCount active inline reverse count={24} icon={CheckIcon} label="Example" />
  </ThemeWrapper>
</Playground>

## IconCount with custom sizes

<Playground>
  <ThemeWrapper>
    <Flex wrap direction="row" justifyContent="space-betwee">
      <Rhythm m={2}>
        <IconCount count={24} icon={BlobbrIcon} iconSize={16} label="Example" typographySize="s" />
        <IconCount count={24} icon={BlobbrIcon} iconSize={18} label="Example" typographySize="m" />
        <IconCount count={24} icon={BlobbrIcon} iconSize={20} label="Example" typographySize="l" />
        <IconCount count={24} icon={BlobbrIcon} iconSize={24} label="Example" typographySize="xl" />
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={IconCount} />
