---
name: IconCount
menu: Display
route: /compositions/IconCount
---

import { Playground, Props } from 'docz';
import { CheckIcon } from 'icons/internal/CheckIcon';
import { HeartIcon } from 'icons/internal/HeartIcon';
import { Flex } from 'components/Flex';
import { Rhythm } from 'components/Rhythm';
import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { IconCount } from '../index';

# IconCount

## Basic IconCount

<Playground>
  <ThemeWrapper>
    <IconCount count={24} icon={HeartIcon} label="Example" />
  </ThemeWrapper>
</Playground>

## Active IconCount

<Playground>
  <ThemeWrapper>
    <IconCount active count={24} icon={HeartIcon} label="Example" />
  </ThemeWrapper>
</Playground>

## Reversed IconCount

<Playground>
  <ThemeWrapper>
    <IconCount active inline reverse count={24} icon={CheckIcon} label="Example" />
  </ThemeWrapper>
</Playground>

## IconCount with custom sizes

<Playground>
  <ThemeWrapper>
    <Flex wrap direction="row" justifyContent="space-between">
      <Rhythm m={2}>
        <IconCount count={24} icon={HeartIcon} iconSize={16} label="Example" typographySize="small" />
        <IconCount count={24} icon={HeartIcon} iconSize={18} label="Example" typographySize="medium" />
        <IconCount count={24} icon={HeartIcon} iconSize={20} label="Example" typographySize="large" />
        <IconCount count={24} icon={HeartIcon} iconSize={24} label="Example" typographySize="xlarge" />
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={IconCount} />
