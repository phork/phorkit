---
name: IconCount
menu: Display
route: /compositions/IconCount
---

import { ThemeWrapper } from 'docs/helpers/ThemeWrapper';
import { Playground, Props } from 'docz';
import { BlobbrIcon } from 'icons/internal/BlobbrIcon';
import { CheckIcon } from 'icons/internal/CheckIcon';
import { Flex } from 'components/Flex';
import { Rhythm } from 'components/Rhythm';
import { IconCount } from '../index';

# IconCount

## Basic IconCount

<Playground>
  <ThemeWrapper>
    <IconCount count={24} icon={BlobbrIcon} label="Example" />
  </ThemeWrapper>
</Playground>

## Active IconCount

<Playground>
  <ThemeWrapper>
    <IconCount active count={24} icon={BlobbrIcon} label="Example" />
  </ThemeWrapper>
</Playground>

## Reversed IconCount

<Playground>
  <ThemeWrapper>
    <IconCount active count={24} icon={CheckIcon} label="Example" inline reverse />
  </ThemeWrapper>
</Playground>

## IconCount with custom sizes

<Playground>
  <ThemeWrapper>
    <Flex direction="row" justifyContent="space-betwee" wrap>
      <Rhythm m={2}>
        <IconCount count={24} icon={BlobbrIcon} label="Example" iconSize={16} typographySize="s" />
        <IconCount count={24} icon={BlobbrIcon} label="Example" iconSize={18} typographySize="m" />
        <IconCount count={24} icon={BlobbrIcon} label="Example" iconSize={20} typographySize="l" />
        <IconCount count={24} icon={BlobbrIcon} label="Example" iconSize={24} typographySize="xl" />
      </Rhythm>
    </Flex>
  </ThemeWrapper>
</Playground>

## \[props\]

<Props of={IconCount} />
