---
name: Colors
route: /colors
---

import { Playground, Props } from 'docz';
import { BlobbrIcon } from 'icons/internal';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ColorSwatches } from './helpers/ColorSwatches';
import { ColorTheme } from './helpers/ColorTheme';
import { ThemeWrapper } from './helpers/ThemeWrapper';

<PageTitle src="postcss/vars" title="Colors" />

Clicking on any color block will copy that color ID to your clipboard.

## Primary colors

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <ColorSwatches
        direction="column"
        group="primary"
        labelHeight={20}
        swatchHeight={40}
        swatchWidth={60}
        themeId={themeId}
      >
        <BlobbrIcon size={24} />
      </ColorSwatches>
    )}
  </ThemeWrapper>
</Playground>

## Primary accent color

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <ColorSwatches joined group="state" swatchHeight={60} swatchWidth={80} themeId={themeId} variant="accent-primary">
        <BlobbrIcon size={30} />
      </ColorSwatches>
    )}
  </ThemeWrapper>
</Playground>

## Success color

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <ColorSwatches joined group="state" swatchHeight={60} swatchWidth={80} themeId={themeId} variant="success">
        <BlobbrIcon size={30} />
      </ColorSwatches>
    )}
  </ThemeWrapper>
</Playground>

## Warning color

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <ColorSwatches joined group="state" swatchHeight={60} swatchWidth={80} themeId={themeId} variant="warning">
        <BlobbrIcon size={30} />
      </ColorSwatches>
    )}
  </ThemeWrapper>
</Playground>

## Danger color

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <ColorSwatches joined group="state" swatchHeight={60} swatchWidth={80} themeId={themeId} variant="danger">
        <BlobbrIcon size={30} />
      </ColorSwatches>
    )}
  </ThemeWrapper>
</Playground>

## Neutral color

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <ColorSwatches joined group="state" swatchHeight={60} swatchWidth={80} themeId={themeId} variant="neutral">
        <BlobbrIcon size={30} />
      </ColorSwatches>
    )}
  </ThemeWrapper>
</Playground>

## Background colors

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => <ColorSwatches group="background" swatchHeight={40} swatchWidth={60} themeId={themeId} />}
  </ThemeWrapper>
</Playground>

## Foreground colors

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => <ColorSwatches group="foreground" swatchHeight={40} swatchWidth={60} themeId={themeId} />}
  </ThemeWrapper>
</Playground>

## Transparent colors

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => <ColorSwatches group="transparent" swatchHeight={40} swatchWidth={60} themeId={themeId} />}
  </ThemeWrapper>
</Playground>

## Primary theme

<Playground>
  <ThemeWrapper withThemeId>{({ themeId }) => <ColorTheme themeId={themeId} variant="primary" />}</ThemeWrapper>
</Playground>

## Secondary theme

<Playground>
  <ThemeWrapper withThemeId>{({ themeId }) => <ColorTheme themeId={themeId} variant="secondary" />}</ThemeWrapper>
</Playground>

## Tertiary theme

<Playground>
  <ThemeWrapper withThemeId>{({ themeId }) => <ColorTheme themeId={themeId} variant="tertiary" />}</ThemeWrapper>
</Playground>

## Contrast theme

<Playground>
  <ThemeWrapper withThemeId>{({ themeId }) => <ColorTheme themeId={themeId} variant="contrast" />}</ThemeWrapper>
</Playground>
