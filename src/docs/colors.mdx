---
name: Colors
route: /colors
---

import { Playground, Props } from 'docz';
import { BlobbrIcon } from 'icons/internal';
import { PageTitle } from 'docs/helpers/PageTitle';
import { ColorSwatch } from './helpers/ColorSwatch';
import { ColorTheme } from './helpers/ColorTheme';
import { ThemeWrapper } from './helpers/ThemeWrapper';

<PageTitle title="Colors" src="postcss/vars" />

Clicking on any color block will copy that color ID to your clipboard.

## Primary colors

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <ColorSwatch themeId={themeId} group="primary" direction="column" swatchWidth={60} swatchHeight={40}>
        <BlobbrIcon size={24} />
      </ColorSwatch>
    )}
  </ThemeWrapper>
</Playground>

## Primary accent color

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <ColorSwatch themeId={themeId} group="state" variant="accent-primary" joined swatchWidth={120} swatchHeight={120}>
        <BlobbrIcon size={84} />
      </ColorSwatch>
    )}
  </ThemeWrapper>
</Playground>

## Success color

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <ColorSwatch themeId={themeId} group="state" variant="success" joined swatchWidth={120} swatchHeight={120}>
        <BlobbrIcon size={84} />
      </ColorSwatch>
    )}
  </ThemeWrapper>
</Playground>

## Warning color

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <ColorSwatch themeId={themeId} group="state" variant="warning" joined swatchWidth={120} swatchHeight={120}>
        <BlobbrIcon size={84} />
      </ColorSwatch>
    )}
  </ThemeWrapper>
</Playground>

## Danger color

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <ColorSwatch themeId={themeId} group="state" variant="danger" joined swatchWidth={120} swatchHeight={120}>
        <BlobbrIcon size={84} />
      </ColorSwatch>
    )}
  </ThemeWrapper>
</Playground>

## Neutral color

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <ColorSwatch themeId={themeId} group="state" variant="neutral" joined swatchWidth={120} swatchHeight={120}>
        <BlobbrIcon size={84} />
      </ColorSwatch>
    )}
  </ThemeWrapper>
</Playground>

## Background colors

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => <ColorSwatch themeId={themeId} group="background" swatchWidth={60} swatchHeight={40} />}
  </ThemeWrapper>
</Playground>

## Foreground colors

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => <ColorSwatch themeId={themeId} group="foreground" swatchWidth={60} swatchHeight={40} />}
  </ThemeWrapper>
</Playground>

## Transparent colors

<Playground>
  <ThemeWrapper withThemeId>
    {({ themeId }) => (
      <React.Fragment>
        <ColorSwatch
          themeId={themeId}
          group="transparent"
          variant="accent-primary"
          swatchWidth={60}
          swatchHeight={40}
        />
        <ColorSwatch themeId={themeId} group="transparent" variant="FG0" swatchWidth={60} swatchHeight={40} />
        <ColorSwatch themeId={themeId} group="transparent" variant="BG50" swatchWidth={60} swatchHeight={40} />
        <ColorSwatch themeId={themeId} group="transparent" variant="BG0" swatchWidth={60} swatchHeight={40} />
      </React.Fragment>
    )}
  </ThemeWrapper>
</Playground>

## Primary theme

<Playground>
  <ThemeWrapper withThemeId>{({ themeId }) => <ColorTheme themeId={themeId} variant="primary" />}</ThemeWrapper>
</Playground>

## Secondary theme

<Playground>
  <ThemeWrapper withThemeId>{({ themeId }) => <ColorTheme themeId={themeId} variant="secondary" />}</ThemeWrapper>
</Playground>

## Tertiary theme

<Playground>
  <ThemeWrapper withThemeId>{({ themeId }) => <ColorTheme themeId={themeId} variant="tertiary" />}</ThemeWrapper>
</Playground>

## Contrast theme

<Playground>
  <ThemeWrapper withThemeId>{({ themeId }) => <ColorTheme themeId={themeId} variant="contrast" />}</ThemeWrapper>
</Playground>
